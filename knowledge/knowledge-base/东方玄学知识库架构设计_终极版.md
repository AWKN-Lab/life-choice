# 东方玄学知识库 - 数据库架构设计（终极版）

**版本**: v4.0
**更新**: 2026-06-05
**覆盖**: 八字、六爻、六壬、奇门、紫微、风水、梅花等

---

## 一、数据架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        知识层（Knowledge）                       │
│  干支│五行│纳音│长生│神煞│星曜│卦象│宫位│意象│规则│典籍│占类    │
├─────────────────────────────────────────────────────────────────┤
│                        案例层（Cases）                           │
│  命例│卦例│盘例│风水案例                                         │
├─────────────────────────────────────────────────────────────────┤
│                       实务层（Practice）                          │
│  阵法│化解│调候│取象│合婚│断法│流年判断                           │
├─────────────────────────────────────────────────────────────────┤
│                       对话层（Consult）                         │
│  咨询│分析│验证│反馈                                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、知识层 - 完整表结构

### 2.1 基础数据表（最底层）

```sql
-- 天干表（10条）
CREATE TABLE tian_gan (
    id              INTEGER PRIMARY KEY,
    name            TEXT NOT NULL UNIQUE,         -- 甲、乙、丙、丁、戊、己、庚、辛、壬、癸
    yin_yang        TEXT NOT NULL,                 -- 阳、阴
    wu_xing         TEXT NOT NULL,                 -- 木、火、土、金、水
    position_order  INTEGER NOT NULL,              -- 1-10

    -- 意象（新增）
    image_yi        TEXT,                         -- 意象：树林、花园、太阳、灯光、山...
    yi_image_detail TEXT,                        -- 意象详解

    -- 十二长生
    chang_sheng     TEXT,                         -- 长生位
    mu_yu           TEXT,                         -- 沐浴位
    guan_dai        TEXT,                         -- 冠带位
    lin_guan        TEXT,                         -- 临官位
    di_wang        TEXT,                         -- 帝旺位
    shuai          TEXT,                         -- 衰位
    bing           TEXT,                         -- 病位
    si            TEXT,                         -- 死位
    mu            TEXT,                         -- 墓位
    jue           TEXT,                         -- 绝位
    tai           TEXT,                         -- 胎位
    yang          TEXT,                         -- 养位

    xing_zhou       TEXT,                         -- 天干性质：刚、柔...
    tian_zhu_yi     TEXT,                         -- 天柱意
    description     TEXT
);

-- 地支表（12条）
CREATE TABLE di_zhi (
    id              INTEGER PRIMARY KEY,
    name            TEXT NOT NULL UNIQUE,         -- 子、丑、寅、卯、辰、巳、午、未、申、酉、戌、亥
    yin_yang        TEXT NOT NULL,
    wu_xing         TEXT NOT NULL,
    position_order  INTEGER NOT NULL,              -- 1-12
    shu_xiang       TEXT,                         -- 属相
    fang_wei        TEXT,                         -- 方位：北、南、东、西

    -- 地支藏干（人元）
    zang_gan_ben    TEXT,                         -- 本气
    zang_gan_zhong  TEXT,                         -- 中气
    zang_gan_yu     TEXT,                         -- 余气

    -- 地支关系
    he_zhi          TEXT,                         -- 合：子丑合化、寅亥合化...
    chong_zhi       TEXT,                         -- 冲：子午冲、丑未冲...
    xing_zhi        TEXT,                         -- 刑：寅刑巳、巳刑申...
    hai_zhi         TEXT,                         -- 害：子未害、丑午害...
    sheng_zhi       TEXT,                         -- 生：子丑互生...

    -- 意象（新增）
    image_yi        TEXT,                         -- 意象：小溪井、坟地、树林、库房...
    yi_image_detail TEXT,                        -- 意象详解

    -- 其他
    yue_jiang       TEXT,                         -- 月将名
    description     TEXT
);

-- 五行表（5条）
CREATE TABLE wu_xing (
    id              INTEGER PRIMARY KEY,
    name            TEXT NOT NULL UNIQUE,
    yin_yang        TEXT NOT NULL,
    sheng_group     TEXT,
    ke_group        TEXT,
    direction       TEXT,
    season          TEXT,
    wu_xing_color   TEXT,
    wu_xing_number  TEXT,
    -- 五行能量饰品（新增）
    energy_items    TEXT,                        -- JSON：[{item, price_range, material}...]
    -- 五行食物（新增）
    energy_foods    TEXT,                        -- JSON：[{food, category}...]
    -- 五行茶类（新增）
    energy_tea      TEXT,                        -- JSON：[{tea_type, wuxing}...]
    description     TEXT
);

-- 纳音五行表（60条）
CREATE TABLE na_yin (
    id              INTEGER PRIMARY KEY,
    gan_zhi         TEXT NOT NULL UNIQUE,
    na_yin_name     TEXT NOT NULL,
    na_yin_wu_xing  TEXT NOT NULL,
    na_yin_category TEXT NOT NULL,               -- 金系、木系、水系、火系、土系
    na_yin_sub      TEXT,                         -- 子类：海中金、沙中金...
    -- 纳音吉凶（新增）
    nature_jixiong TEXT,                         -- 宝物/顽矿/炉炭...
    likes           TEXT,                         -- 喜
    dislikes        TEXT,                         -- 忌
    -- 神煞标记
    shen_sha_marks  TEXT,                         -- 进神/福星/华盖/悬针/破字...
    description     TEXT
);

-- 十二长生表（120条 = 10天干 × 12地支）
CREATE TABLE shi_er_chang_sheng (
    id              INTEGER PRIMARY KEY,
    ri_gan          TEXT NOT NULL,
    di_zhi          TEXT NOT NULL,
    wei_zhi         TEXT NOT NULL,                -- 长生、沐浴、冠带、临官、帝旺、衰、病、死、墓、绝、胎、养
    wei_zhi_order   INTEGER NOT NULL,              -- 1-12
    is_ji_shen      TEXT,                         -- 是否吉神
    -- 长生意象（新增）
    chang_sheng_image TEXT,                      -- 长生意象
    description     TEXT,
    UNIQUE(ri_gan, di_zhi)
);

-- 空亡表（6组 × 10条）
CREATE TABLE kong_wang (
    id              INTEGER PRIMARY KEY,
    xun             TEXT NOT NULL,
    kong_wang_1     TEXT NOT NULL,
    kong_wang_2     TEXT NOT NULL,
    description     TEXT
);

-- 财库表（新增）
CREATE TABLE cai_ku (
    id              INTEGER PRIMARY KEY,
    ri_gan          TEXT NOT NULL,                -- 日干：甲乙丙丁戊己庚辛壬癸
    ri_gan_wu_xing  TEXT NOT NULL,
    cai_ku_di_zhi   TEXT NOT NULL,               -- 财库地支
    bi_jie_ku       TEXT NOT NULL,               -- 比劫库地支
    guan_sha_ku     TEXT,                         -- 官杀库
    yin_xiao_ku     TEXT,                         -- 印枭库
    shi_shang_ku    TEXT,                         -- 食伤库
    description     TEXT
);
```

### 2.2 神煞表（完整版）

```sql
-- 神煞主表（带完整字段）
CREATE TABLE shen_sha (
    id              INTEGER PRIMARY KEY,
    name            TEXT NOT NULL UNIQUE,
    category        TEXT NOT NULL,                -- 岁煞、月煞、旬煞、干煞、支煞、特殊煞
    wu_xing         TEXT,
    ji_xiong        TEXT NOT NULL,                -- 吉、凶、平、动、晦

    -- 起例规则
    qi_ju_rule      TEXT NOT NULL,
    qi_ju_gan       TEXT,                         -- 干煞起例
    qi_ju_zhi       TEXT,                         -- 支煞起例/岁煞起例
    lookup_type     TEXT NOT NULL,                -- 以日干查/以年支查/以月令查/以日支查

    -- 适用占类
    shi_yong_zhan   TEXT,                         -- 适用占类

    -- 含义
    base_meaning    TEXT NOT NULL,
    effect_ji       TEXT,
    effect_xiong    TEXT,
    effect_zhong    TEXT,

    -- 现代解读
    modern_desc     TEXT,

    -- 来源
    source          TEXT,

    -- 化解方法（新增）
    hua_jie_method  TEXT,                        -- 化解方法
    hua_jie_items   TEXT,                        -- 化解物品

    description     TEXT
);

-- 神煞分类表
CREATE TABLE shen_sha_category (
    id              INTEGER PRIMARY KEY,
    category        TEXT NOT NULL UNIQUE,
    category_desc   TEXT,
    quan_li_weight  TEXT,
    notes           TEXT
);

-- 神煞与占类关联表
CREATE TABLE shen_sha_zhan_lei (
    id              INTEGER PRIMARY KEY,
    shen_sha_name   TEXT NOT NULL,
    zhan_lei        TEXT NOT NULL,
    priority        INTEGER,
    description     TEXT
);
```

### 2.3 八字规则表

```sql
-- 十神关系表（计算用）
CREATE TABLE shi_shen_relation (
    id              INTEGER PRIMARY KEY,
    gan_zhi_1       TEXT NOT NULL,
    gan_zhi_2       TEXT NOT NULL,
    wu_xing_1       TEXT NOT NULL,
    wu_xing_2       TEXT NOT NULL,
    yin_yang_1      TEXT NOT NULL,
    yin_yang_2      TEXT NOT NULL,
    is_same_wuxing  INTEGER,
    is_sheng        INTEGER,
    is_被_sheng      INTEGER,
    is_ke            INTEGER,
    is_被_ke         INTEGER,
    shi_shen_name   TEXT,
    shi_shen_type   TEXT,
    is_tong_yin     INTEGER,
    UNIQUE(gan_zhi_1, gan_zhi_2)
);

-- 八字格局表
CREATE TABLE bazi_patterns (
    id              INTEGER PRIMARY KEY,
    pattern_name    TEXT NOT NULL UNIQUE,
    pattern_type    TEXT NOT NULL,
    condition       TEXT NOT NULL,
    yue_ling_req    TEXT,
    gan_tou_req     TEXT,
    chong_po_req    TEXT,
    characteristics TEXT,
    evaluation      TEXT,
    likes           TEXT,
    dislikes        TEXT,
    judgment        TEXT,
    source          TEXT,
    description     TEXT
);

-- 调候用神表（《穷通宝鉴》）
CREATE TABLE tiao_hou (
    id              INTEGER PRIMARY KEY,
    gan             TEXT NOT NULL,
    season          TEXT NOT NULL,
    tiao_hou_needed TEXT NOT NULL,
    yong_shen       TEXT NOT NULL,
    xi_shen         TEXT,
    detail_reason   TEXT,                        -- 详细理由
    source          TEXT,
    description     TEXT
);

-- 病药法则表
CREATE TABLE bing_yao_rules (
    id              INTEGER PRIMARY KEY,
    bing_type       TEXT NOT NULL,
    bing_desc       TEXT,
    yao_type        TEXT NOT NULL,
    yao_desc        TEXT,
    typical_combo   TEXT,
    case_example    TEXT,
    judgment        TEXT,
    description     TEXT
);

-- 通关法则表
CREATE TABLE tong_guan_rules (
    id              INTEGER PRIMARY KEY,
    zhan_dou_1      TEXT NOT NULL,
    zhan_dou_2      TEXT NOT NULL,
    tong_guan_sheng TEXT NOT NULL,
    tong_guan_method TEXT,
    case_example    TEXT,
    description     TEXT
);

-- 刑冲合害表
CREATE TABLE xing_chong_he_hai (
    id              INTEGER PRIMARY KEY,
    type            TEXT NOT NULL,                -- 刑/冲/合/害
    Zhi_1           TEXT NOT NULL,
    Zhi_2           TEXT NOT NULL,
    effect          TEXT,                         -- 作用
    judgment        TEXT,                        -- 断语
    case_example    TEXT,
    description     TEXT
);
```

### 2.4 占断分类编码表（新增）

```sql
-- 事件类型编码表
CREATE TABLE event_type_code (
    id              INTEGER PRIMARY KEY,
    code            TEXT NOT NULL UNIQUE,         -- I/C/R/H/T/O/D/L
    name            TEXT NOT NULL,
    description     TEXT
);

-- 颗粒度编码表
CREATE TABLE granularity_code (
    id              INTEGER PRIMARY KEY,
    code            TEXT NOT NULL UNIQUE,         -- 1/2/3
    name            TEXT NOT NULL,
    description     TEXT
);

-- 问事类别编码表
CREATE TABLE question_type_code (
    id              INTEGER PRIMARY KEY,
    code            TEXT NOT NULL UNIQUE,         -- P/F/M/S/J/W/T/R
    name            TEXT NOT NULL,
    description     TEXT
);

-- 占类类神表
CREATE TABLE zhan_lei_shi_sheng (
    id              INTEGER PRIMARY KEY,
    zhan_lei        TEXT NOT NULL,                -- 天时/婚姻/疾病/出行/仕宦/求财/田宅/公讼
    core_sheng      TEXT NOT NULL,               -- 核心类神
    event_type      TEXT,                         -- 事件类型
    granularity     TEXT,                         -- 颗粒度
    question_type   TEXT,                         -- 问事类别
    common_kou_jue  TEXT,                        -- 常用口诀
    judge_points    TEXT,                        -- 判断要点
    description     TEXT
);
```

---

## 三、实务层 - 阵法与化解表（新增重点）

```sql
-- 打底十四阵表
CREATE TABLE da_di_shi_si_zhen (
    id              INTEGER PRIMARY KEY,
    zhen_name       TEXT NOT NULL,                -- 阵法名称
    zhen_type       TEXT NOT NULL,               -- 阵法类型：饰品/食物/衣服/喝茶/风水/音乐...
    -- 对应五行
    wu_xing         TEXT NOT NULL,                -- 木/火/土/金/水
    -- 详细内容
    items           TEXT NOT NULL,               -- JSON：具体物品列表
    price_range     TEXT,                         -- 价格区间
    purchase_advice TEXT,                        -- 购买建议
    placement       TEXT,                         -- 摆放位置/使用方法
    effect_expect   TEXT,                         -- 效果预期
    side_effect     TEXT,                         -- 补多了不良反应
    case_example    TEXT,                        -- 案例
    notes           TEXT
);

-- 旺财阵法表
CREATE TABLE wang_cai_zhen (
    id              INTEGER PRIMARY KEY,
    zhen_name       TEXT NOT NULL,
    principle       TEXT NOT NULL,               -- 原理
   适用条件        TEXT,                         -- 适用条件
    steps           TEXT NOT NULL,               -- 步骤/流程
    items_needed    TEXT,                        -- 所需物品
    placement_rules TEXT,                        -- 摆放规则
   注意事项        TEXT,
    case_examples   TEXT,                        -- 案例
    notes           TEXT
);

-- 补财库表
CREATE TABLE bu_cai_ku (
    id              INTEGER PRIMARY KEY,
    ri_gan          TEXT NOT NULL,
    ri_gan_wu_xing  TEXT NOT NULL,
    cai_ku_zhi      TEXT NOT NULL,
    kai_ku_method   TEXT NOT NULL,               -- 开库方法
    feng_shui_items TEXT,                        -- 风水用品
   摆放位置        TEXT,
    time_selection  TEXT,                        -- 时间选择
   注意事项        TEXT,
    case_examples   TEXT,
    notes           TEXT
);

-- 桃花阵法表
CREATE TABLE tao_hua_zhen (
    id              INTEGER PRIMARY KEY,
    zhen_type       TEXT NOT NULL,               -- 男女桃花/社会桃花
    tao_hua_zhi     TEXT NOT NULL,               -- 桃花位
    steps           TEXT NOT NULL,
    items_needed    TEXT,
    placement       TEXT,
    energy_crystals  TEXT,                        -- 对应水晶
    time_selection  TEXT,
    effect_expect   TEXT,
    notes           TEXT
);

-- 八字合婚表
CREATE TABLE he_hun_rules (
    id              INTEGER PRIMARY KEY,
    rule_type       TEXT NOT NULL,                -- 配婚原理/合婚规则
    rule_content    TEXT NOT NULL,
    case_example    TEXT,
    description     TEXT
);

-- 牢狱化解表
CREATE TABLE lao_yu_hua_jie (
    id              INTEGER PRIMARY KEY,
    disaster_type   TEXT NOT NULL,                -- 牢狱/官非
    cause_info      TEXT NOT NULL,                -- 原因信息
    symptom         TEXT,                        -- 征兆
    hua_jie_method  TEXT NOT NULL,               -- 化解方法
    items_needed    TEXT,
    prevention      TEXT,                        -- 预防
    case_examples   TEXT,
    notes           TEXT
);

-- 官运提升表
CREATE TABLE guan_yun_ti_sheng (
    id              INTEGER PRIMARY KEY,
    method_name     TEXT NOT NULL,
    principle       TEXT,
    steps           TEXT,
    items_needed    TEXT,
    notes           TEXT
);

-- 文昌阵法表
CREATE TABLE wen_chang_zhen (
    id              INTEGER PRIMARY KEY,
    zhen_name       TEXT NOT NULL,
   适用条件        TEXT,
    steps           TEXT,
    items_needed    TEXT,
    placement       TEXT,
    notes           TEXT
);
```

---

## 四、案例层 - 完整表结构

### 4.1 八字命例表（终极版）

```sql
CREATE TABLE bazi_cases (
    -- ========== 核心ID ==========
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    case_no         TEXT NOT NULL UNIQUE,
    case_source     TEXT,
    source_detail    TEXT,                         -- 具体来源：哪本书、哪个案例

    -- ========== 基础信息 ==========
    name            TEXT,
    gender          TEXT NOT NULL,
    gender_source   TEXT,

    -- ========== 出生时间（原始） ==========
    birth_year      INTEGER NOT NULL,
    birth_month     INTEGER NOT NULL,
    birth_day       INTEGER NOT NULL,
    birth_hour      INTEGER,
    birth_minute    INTEGER,
    is_lunar        INTEGER DEFAULT 1,
    is_leap_month   INTEGER DEFAULT 0,
    timezone        TEXT DEFAULT 'Asia/Shanghai',
    birth_location  TEXT,
    longitude       REAL,
    true_solar_time INTEGER DEFAULT 0,

    -- ========== 排盘结果（四柱） ==========
    year_zhu        TEXT NOT NULL,
    month_zhu       TEXT NOT NULL,
    day_zhu         TEXT NOT NULL,
    hour_zhu        TEXT,

    year_gan        TEXT,
    year_zhi        TEXT,
    month_gan       TEXT,
    month_zhi       TEXT,
    day_gan         TEXT,
    day_zhi         TEXT,
    hour_gan        TEXT,
    hour_zhi        TEXT,

    -- ========== 藏干 ==========
    year_zang_ben   TEXT,
    year_zang_zhong TEXT,
    year_zang_yu    TEXT,
    month_zang_ben  TEXT,
    month_zang_zhong TEXT,
    month_zang_yu   TEXT,
    day_zang_ben    TEXT,
    day_zang_zhong  TEXT,
    day_zang_yu     TEXT,
    hour_zang_ben   TEXT,
    hour_zang_zhong TEXT,
    hour_zang_yu    TEXT,

    -- ========== 纳音 ==========
    year_na_yin     TEXT,
    month_na_yin    TEXT,
    day_na_yin      TEXT,
    hour_na_yin     TEXT,
    na_yin_relation TEXT,                         -- 纳音关系：上生下/上克下/下克上/下生上

    -- ========== 十神 ==========
    year_shi_sheng  TEXT,
    month_shi_sheng TEXT,
    hour_shi_sheng  TEXT,
    ri_zhi_shi_sheng TEXT,                       -- 日支十神

    -- ========== 十二长生 ==========
    year_chang_sheng TEXT,
    month_chang_sheng TEXT,
    day_chang_sheng TEXT,
    hour_chang_sheng TEXT,

    -- ========== 日主信息 ==========
    day_master      TEXT NOT NULL,
    day_master_yy   TEXT,
    day_master_wx   TEXT,

    -- ========== 强弱分析 ==========
    strength_score  REAL,
    strength_level  TEXT,
    strong_wx       TEXT,
    weak_wx         TEXT,
    sheng_ke_detail  TEXT,

    -- ========== 格局 ==========
    pattern_type    TEXT,
    pattern_name    TEXT,
    pattern_level   TEXT,
    pattern_condition TEXT,
    pattern_evaluate TEXT,

    -- ========== 用神 ==========
    yong_shen       TEXT,
    yong_shen_reason TEXT,
    xi_shen         TEXT,
    ji_shen         TEXT,
    chou_shen       TEXT,
    guan_shen       TEXT,

    -- ========== 调候 ==========
    tiao_hou_needed TEXT,
    tiao_hou_god    TEXT,
    tiao_hou_reason  TEXT,

    -- ========== 病药 ==========
    bing_type       TEXT,
    bing_detail     TEXT,
    yao_type        TEXT,
    yao_detail      TEXT,

    -- ========== 财库 ==========
    cai_ku_zhi      TEXT,
    bi_jie_ku_zhi   TEXT,
    has_cai_ku      INTEGER,                      -- 是否有财库
    kai_ku_status   TEXT,                         -- 库状态：开/闭

    -- ========== 桃花 ==========
    tao_hua_zhi     TEXT,                         -- 桃花位
    tao_hua_type    TEXT,                         -- 桃花类型

    -- ========== 刑冲合害 ==========
    chong_he_list   TEXT,
    xing_list       TEXT,
    hai_list        TEXT,
    he_list         TEXT,

    -- ========== 神煞 ==========
    shen_sha_list   TEXT,                         -- JSON

    -- ========== 空亡 ==========
    kong_wang       TEXT,
    tui_yan         TEXT,
    mai_xi          TEXT,

    -- ========== 大运 ==========
    yun_start_age   INTEGER,
    yun_direction   TEXT,
    yun_start_year  INTEGER,
    da_yun_detail   TEXT,                         -- JSON

    -- ========== 人生推断 ==========
    lifespan        TEXT,
    wealth_level    TEXT,
    official_rank   TEXT,
    marriage_desc   TEXT,
    children_desc   TEXT,
    health_desc     TEXT,
    career_desc     TEXT,
    personality_desc TEXT,

    -- ========== 配偶信息 ==========
    spouse_appear   TEXT,                         -- 配偶外貌
    spouse_character TEXT,                         -- 配偶性格
    marriage_age    TEXT,                         -- 结婚年龄

    -- ========== 分析总结 ==========
    summary         TEXT,
    key_events      TEXT,
    advice          TEXT,

    -- ========== 实务建议（新增） ==========
    recommended_zhen TEXT,                        -- 推荐阵法
    bu_cai_ku_advice TEXT,                        -- 补财库建议
    tao_hua_advice  TEXT,                        -- 桃花建议
    shi_yong_items   TEXT,                        -- 适用饰品
    shi_yong_foods   TEXT,                        -- 适用食物
    feng_shui_advice TEXT,                        -- 风水建议

    -- ========== 元信息 ==========
    source          TEXT,
    source_chapter  TEXT,
    page_no         TEXT,
    confidence      INTEGER DEFAULT 3,
    verified        INTEGER DEFAULT 0,
    verified_by     TEXT,
    verified_date   TEXT,
    notes           TEXT,
    created_at      TEXT DEFAULT CURRENT_TIMESTAMP,
    updated_at      TEXT DEFAULT CURRENT_TIMESTAMP
);
```

### 4.2 六壬案例表

```sql
CREATE TABLE liuren_cases (
    -- ========== 核心ID ==========
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    case_no         TEXT NOT NULL UNIQUE,
    case_source     TEXT,

    -- ========== 求测者 ==========
    name            TEXT,
    gender          TEXT,
    consult_time    TEXT NOT NULL,

    -- ========== 所问 ==========
    question        TEXT NOT NULL,
    question_type   TEXT,                         -- 疾病/官非/婚姻/出行/财运/事业...
    event_type_code TEXT,                         -- 事件类型编码：I/C/R/H/T/O/D/L
    granularity     TEXT,                         -- 颗粒度：1/2/3
    question_type_code TEXT,                      -- 问事类别编码：P/F/M/S/J/W/T/R

    -- ========== 起课信息 ==========
    yue_jiang       TEXT NOT NULL,
    zhan_shi        TEXT NOT NULL,
    ri_gan          TEXT NOT NULL,
    ri_zhi          TEXT NOT NULL,

    -- ========== 天地盘 ==========
    tian_panel      TEXT NOT NULL,
    di_panel        TEXT NOT NULL,

    -- ========== 四课 ==========
    si_ke1          TEXT,
    si_ke1_gan      TEXT,
    si_ke1_zhi      TEXT,
    si_ke2          TEXT,
    si_ke2_gan      TEXT,
    si_ke2_zhi      TEXT,
    si_ke3          TEXT,
    si_ke3_gan      TEXT,
    si_ke3_zhi      TEXT,
    si_ke4          TEXT,
    si_ke4_gan      TEXT,
    si_ke4_zhi      TEXT,

    -- ========== 三传 ==========
    chuan_1         TEXT,
    chuan_1_gan     TEXT,
    chuan_1_zhi     TEXT,
    chuan_2         TEXT,
    chuan_2_gan     TEXT,
    chuan_2_zhi     TEXT,
    chuan_3         TEXT,
    chuan_3_gan     TEXT,
    chuan_3_zhi     TEXT,
    chuan_type      TEXT,

    -- ========== 九宗门 ==========
    jiu_zong_name   TEXT,
    jiu_zong_condition TEXT,
    jiu_zong_method TEXT,

    -- ========== 神将 ==========
    tian_jiang_1    TEXT,
    tian_jiang_2    TEXT,
    tian_jiang_3    TEXT,
    tian_jiang_4    TEXT,
    tian_jiang_5    TEXT,
    tian_jiang_6    TEXT,
    tian_jiang_7    TEXT,
    tian_jiang_8    TEXT,
    tian_jiang_9    TEXT,
    tian_jiang_10   TEXT,
    tian_jiang_11   TEXT,
    tian_jiang_12   TEXT,

    -- ========== 神煞 ==========
    shen_sha_list   TEXT,

    -- ========== 分析 ==========
    analysis        TEXT,
    judgment        TEXT,
    ji_xiong        TEXT,

    -- ========== 类神判断 ==========
    lei_sheng_judge TEXT,                        -- 类神判断
    core_judge_points TEXT,                     -- 核心判断要点

    -- ========== 验证 ==========
    result          TEXT,
    verified        INTEGER DEFAULT 0,
    verify_date     TEXT,
    accuracy        TEXT,

    -- ========== 元信息 ==========
    source          TEXT,
    notes           TEXT,
    created_at      TEXT DEFAULT CURRENT_TIMESTAMP
);
```

---

## 五、索引设计

```sql
-- 基础表索引
CREATE INDEX idx_tian_gan_name ON tian_gan(name);
CREATE INDEX idx_di_zhi_name ON di_zhi(name);
CREATE INDEX idx_wu_xing_name ON wu_xing(name);
CREATE INDEX idx_na_yin_gan_zhi ON na_yin(gan_zhi);
CREATE INDEX idx_na_yin_category ON na_yin(na_yin_category);
CREATE INDEX idx_na_yin_name ON na_yin(na_yin_name);

-- 神煞索引
CREATE INDEX idx_shensha_name ON shen_sha(name);
CREATE INDEX idx_shensha_category ON shen_sha(category);
CREATE INDEX idx_shensha_lookup ON shen_sha(lookup_type);
CREATE INDEX idx_shensha_jixiong ON shen_sha(ji_xiong);

-- 八字案例索引
CREATE INDEX idx_bazi_case_no ON bazi_cases(case_no);
CREATE INDEX idx_bazi_birth ON bazi_cases(birth_year, birth_month, birth_day);
CREATE INDEX idx_bazi_gan ON bazi_cases(day_gan);
CREATE INDEX idx_bazi_pattern ON bazi_cases(pattern_name);
CREATE INDEX idx_bazi_na_yin ON bazi_cases(day_na_yin);
CREATE INDEX idx_bazi_cai_ku ON bazi_cases(cai_ku_zhi);
CREATE INDEX idx_bazi_tao_hua ON bazi_cases(tao_hua_zhi);

-- 六壬案例索引
CREATE INDEX idx_liuren_case_no ON liuren_cases(case_no);
CREATE INDEX idx_liuren_question_type ON liuren_cases(question_type);
CREATE INDEX idx_liuren_chuan_type ON liuren_cases(chuan_type);

-- 实务表索引
CREATE INDEX idx_da_di_zhen_wuxing ON da_di_shi_si_zhen(wu_xing);
CREATE INDEX idx_da_di_zhen_type ON da_di_shi_si_zhen(zhen_type);
CREATE INDEX idx_bu_cai_ku_gan ON bu_cai_ku(ri_gan);
CREATE INDEX idx_tao_hua_zhen_type ON tao_hua_zhen(zhen_type);
```

---

## 六、数据量估算

| 类别 | 数量 | 说明 |
|------|------|------|
| 天干地支五行 | 27条 | 基础数据，基本不变 |
| 纳音五行 | 60条 | 六十甲子，基本不变 |
| 十二长生 | 120条 | 10×12，基本不变 |
| 神煞 | 200+ | 包含各类型神煞 |
| 八字格局 | 50+ | 正格从格化格专旺等 |
| 调候规则 | 40+ | 十干×四季 |
| **命例案例** | 1000+ | 逐步积累 |
| **六壬案例** | 500+ | 逐步积累 |
| 打底阵法 | 14+ | 十四阵各类型 |
| 旺财阵法 | 10+ | 各种旺财法 |
| 桃花阵法 | 5+ | 男女/社会桃花 |
| 化解规则 | 50+ | 各种牢狱/官非化解 |

---

## 七、实施优先级

### Phase 1：基础数据（必须）
1. 完善天干地支表（含意象）
2. 纳音五行表（含吉凶）
3. 神煞表（含化解方法）
4. 占断分类编码表

### Phase 2：实务规则
1. 打底十四阵表
2. 旺财阵法表
3. 补财库表
4. 桃花阵法表
5. 牢狱化解表

### Phase 3：案例迁移
1. 修复现有命例数据库
2. 补充字段
3. 导入新案例

---

**维护者**：AWKN Lab - 天火
**版本**：v4.0
**状态**：设计完成，待实施
