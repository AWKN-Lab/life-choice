# P0 执行计划 — Step 03 Plan

> **PRD 来源**：PRD-C01-用户资产中台-v0.3 §19 P0 安全止损 8 项
> **Locate 依据**：`P0-Locate报告-爆炸半径评估-20260711.md`
> **执行模式**：严格可执行模式（饭要一口口吃 v2）
> **拆解视角**：模块边界（前端/后端/DB/部署）
> **执行单元**：按时间 ≤10 分钟划分，批次内多步骤合并报告
> **状态**：待用户确认后进入 Step 04 Patch

---

## A. 目标与边界

### A.1 目标一句话
执行 PRD-C01 P0 安全止损 8 项代码修改，本地修改 + 构建部署到生产服务器，确保用户资产（UserMemory/ConsultRecord/Dialogue/Chronicle）不被横向越权访问或误删除。

### A.2 本轮做（8 项）
1. P0-1：暂停 `cleanExpired()` 删除整行 UserMemory
2. P0-2：FollowupController 加 JWT Guard + 所有权校验
3. P0-3：Dialogue Controller/Service 加所有权校验
4. P0-4：Chronicle Controller/Service 加所有权校验
5. P0-5：补全 schema.prisma 的 MemoryEmbedding model 定义
6. P0-6：盘点匿名/孤立记录（只读）
7. P0-7：盘点并补全所有 Controller 的 @UseGuards
8. P0-8：为所有 delete 操作添加审计日志

### A.3 本轮不做
- 不做 P1 代码修改（P2 对话契约差异 3 等）
- 不做 PostgreSQL 迁移
- 不做 AuditLog 新表（P0-8 用日志文件方案）
- 不做全局 APP_GUARD（P0-7 用逐个 Controller 方案）
- 不重构现有代码（只加 Guard/校验/审计，不改业务逻辑）
- 不改前端（前端已传 JWT，无需改动）

### A.4 约束与假设
- C1：生产服务器 8.148.245.29，SSH root 可访问
- C2：SQLite 单实例，部署仅支持 PM2 fork 模式
- C3：前端 client.ts L116-121 已传 `Authorization: Bearer <token>`
- C4：后端 git 当前 commit `4bbcd7c`（master 分支）
- C5：P0-5 修复后需 `npx prisma generate` 重新生成 client
- C6：所有代码修改在本地进行，构建后 scp dist/ 部署
- C7：执行前必须备份生产 DB
- C8：每个批次完成后即时验证，不通过不进入下一批
- C9：高风险批次（P0-5）需用户确认后执行
- C10：部署后必须跑 5 件套验证

---

## B. Goal 契约（Loop Engineering 三要素）

### B.1 Goal
执行 PRD-C01 P0 安全止损 8 项修复，使后端代码满足：
- 所有资产接口（Followup/Dialogue/Chronicle）都有 JWT 认证 + 所有权校验
- UserMemory 过期清理不再删除整行
- schema.prisma 与 migration 同步
- 所有 delete 操作有审计日志
- 生产部署 5 件套验证全 PASS

### B.2 停止条件（满足全部才停止）
- S1：8 项 P0 代码修改完成
- S2：`npx tsc --noEmit` 类型检查 0 errors
- S3：`npm run build` 构建成功
- S4：生产部署 5 件套全 PASS（HTML title / JS hash / 健康检查 / API 404 / PM2 unstable_restarts=0）
- S5：关键接口认证验证通过（无 token → 401，他人 token → 403）

### B.3 验证方式
- V1：TypeScript 类型检查（`npx tsc --noEmit`）
- V2：本地构建（`npm run build`）
- V3：生产 5 件套（`verify-deploy.sh`）
- V4：认证场景测试（curl 无 token / 他人 token / 自己 token）
- V5：DB 完整性检查（`sqlite3 prod.db "PRAGMA integrity_check;"`）

---

## C. 最小闭环（MVP-0）

**MVP-0 定义**：在测试环境中，用 curl 验证 P0-2 修复 — 无 token 调用 `POST /consult/followup` 返回 401。

**MVP-0 的临时项**：
- 临时：用 curl 手动测试，不写自动化测试
- 临时：只测 P0-2，不测全部 8 项
- 移除条件：P0-2 修复 + 401 验证通过后，进入完整批次执行

---

## D. 分批清单（7 批次 + 1 部署）

### 批次 0：前置备份（强制，<5 分钟）

**Step 0.1｜生产 DB 备份**
- 动作：SSH 到服务器，备份 prod.db
- 产出：`/opt/awkn-life/awkn-life-backend/apps/api-server/prisma/prod.db.bak.20260711`
- 验收标准：备份文件存在且大小 > 0；`PRAGMA integrity_check` 返回 ok
- 验证方法：`ssh root@8.148.245.29 "ls -lh /opt/awkn-life/awkn-life-backend/apps/api-server/prisma/prod.db.bak.20260711"`
- 回滚方式：N/A（备份操作本身无风险）
- 风险标记：低

**Step 0.2｜后端代码备份**
- 动作：SSH 到服务器，git stash 或 commit 当前状态
- 产出：服务器后端代码有可回滚的 commit/stash
- 验收标准：`git log --oneline -1` 显示备份 commit
- 验证方法：`ssh root@8.148.245.29 "cd /opt/awkn-life/awkn-life-backend && git log --oneline -3"`
- 回滚方式：`git reset --hard <backup-commit>`
- 风险标记：低

**Step 0.3｜PM2 状态记录**
- 动作：记录当前 PM2 状态（pid/uptime/unstable_restarts）
- 产出：PM2 状态快照（用于部署后对比）
- 验收标准：记录输出
- 验证方法：`ssh root@8.148.245.29 "pm2 describe awkn-life-backend | head -20"`
- 回滚方式：N/A
- 风险标记：低

---

### 批次 1：数据盘点 P0-6（只读，<10 分钟）

**Step 1.1｜SSH 执行匿名记录盘点 SQL**
- 动作：在服务器执行 SQL 统计匿名/孤立记录
- 产出：盘点结果输出
- 验收标准：5 条 SQL 都有输出
- 验证方法：SQL 输出保存到 `docs/05审核与质量/取证/P0-6匿名记录盘点结果-20260711.md`
- 回滚方式：N/A（只读）
- 风险标记：低

**SQL 清单**：
```sql
-- 1. 匿名记录数（userId 为 null）
SELECT '匿名记录数' AS metric, COUNT(*) AS count FROM ConsultRecord WHERE userId IS NULL;
-- 2. 有 anonymousId 的记录数
SELECT '有 anonymousId 记录数' AS metric, COUNT(*) AS count FROM ConsultRecord WHERE anonymousId IS NOT NULL;
-- 3. 孤立记录数（userId 不在 User 表中）
SELECT '孤立记录数' AS metric, COUNT(*) AS count FROM ConsultRecord WHERE userId IS NOT NULL AND userId NOT IN (SELECT id FROM User);
-- 4. UserMemory 孤立记录数
SELECT 'UserMemory 孤立记录数' AS metric, COUNT(*) AS count FROM UserMemory WHERE userId NOT IN (SELECT id FROM User);
-- 5. ChronicleEntry 孤立记录数
SELECT 'ChronicleEntry 孤立记录数' AS metric, COUNT(*) AS count FROM ChronicleEntry WHERE userId IS NOT NULL AND userId NOT IN (SELECT id FROM User);
```

---

### 批次 2：迁移验证 P0-5（⚠️ 高风险，<15 分钟）

**Step 2.1｜检查生产 DB MemoryEmbedding 表**
- 动作：SSH 检查表是否存在
- 产出：表存在/不存在
- 验收标准：明确判定表是否存在
- 验证方法：`ssh root@8.148.245.29 "sqlite3 /opt/awkn-life/awkn-life-backend/apps/api-server/prisma/prod.db '.tables' | grep -i memory"`
- 回滚方式：N/A（只读）
- 风险标记：低

**Step 2.2｜本地补全 schema.prisma 的 MemoryEmbedding model**
- 动作：在 `apps/api-server/prisma/schema.prisma` 末尾添加 model 定义（对齐 migration.sql）
- 产出：schema.prisma 包含 MemoryEmbedding model
- 验收标准：`grep "model MemoryEmbedding" schema.prisma` 有输出
- 验证方法：Read 文件确认
- 回滚方式：Edit 删除添加的 model 定义
- 风险标记：中

**要添加的代码**：
```prisma
/// P0-5: 补全 model 定义（对齐 migration 20260703184000_add_memory_embedding）
model MemoryEmbedding {
  id        String   @id @default(uuid())
  memoryId  String
  userId    String
  content   String
  embedding String   // JSON number[]
  dim       Int      @default(256)
  createdAt DateTime @default(now())

  @@index([memoryId])
  @@index([userId])
}
```

**Step 2.3｜本地 prisma generate 验证**
- 动作：`cd apps/api-server && npx prisma generate`
- 产出：Prisma Client 重新生成，包含 memoryEmbedding 方法
- 验收标准：命令 exit 0，无 error
- 验证方法：检查输出含 "Generated Prisma Client"
- 回滚方式：恢复 schema.prisma + 重新 generate
- 风险标记：中
- Plan B：如果 generate 失败，检查 schema 语法 → 修复 → 重试

**Step 2.4｜生产 DB 同步（如需要）**
- 动作：如生产 DB 无表，SSH 执行 `npx prisma db push`
- 产出：生产 DB 有 MemoryEmbedding 表
- 验收标准：`sqlite3 prod.db ".schema MemoryEmbedding"` 有输出
- 验证方法：SSH 检查
- 回滚方式：恢复备份 DB（`cp prod.db.bak.20260711 prod.db`）
- 风险标记：🔴 高
- Plan B：失败信号=db push 报错｜兜底方案=恢复备份 DB + 重启服务｜证据=prisma db push 输出

**⚠️ 本批次需用户确认后执行 Step 2.4**

---

### 批次 3：认证修复 P0-2 + P0-3 + P0-4（<20 分钟）

**Step 3.1｜P0-2 FollowupController 加 Guard + userId**
- 动作：修改 `followup.controller.ts` + `followup.service.ts`
- 产出：
  - Controller 加 `@UseGuards(JwtAuthGuard)` + `@Req() req` + `userId = req.user?.id`
  - Service `handleFollowup` 和 `getFollowUpContext` 增加 userId 参数 + 所有权校验
- 验收标准：
  - `grep "UseGuards(JwtAuthGuard)" followup.controller.ts` 有输出
  - `grep "userId" followup.service.ts` 有输出
  - `npx tsc --noEmit` 0 errors
- 验证方法：Grep + tsc
- 回滚方式：恢复原文件
- 风险标记：中

**Controller 修改要点**：
```typescript
import { UseGuards, Req, ForbiddenException } from '@nestjs/common';
import { JwtAuthGuard } from '../../auth/guards/jwt-auth.guard';

@Controller('consult/followup')
@UseGuards(JwtAuthGuard)
export class FollowupController {
  @Post()
  async handleFollowup(@Body() dto: FollowupDto, @Req() req: any) {
    const userId = req.user?.id;
    return this.followupService.handleFollowup(dto.recordId, dto.question.trim(), dto.context, userId);
  }

  @Get(':followUpId')
  async getFollowUpContext(@Param('followUpId') followUpId: string, @Req() req: any) {
    const userId = req.user?.id;
    return this.followupService.getFollowUpContext(followUpId, userId);
  }
}
```

**Service 修改要点**：
- `handleFollowup(recordId, question, context, userId)`：查询 record 后校验 `record.userId === userId`，不匹配抛 `ForbiddenException`
- `getFollowUpContext(followUpId, userId)`：查询 followUp 后校验归属

**Step 3.2｜P0-3 Dialogue 加所有权校验**
- 动作：修改 `dialogue.controller.ts` + `dialogue.service.ts`
- 产出：
  - Controller 将 userId 传入 service
  - Service `advanceDialogue` 和 `getDialogueResult` 增加 userId 参数 + 所有权校验
- 验收标准：`npx tsc --noEmit` 0 errors
- 验证方法：tsc
- 回滚方式：恢复原文件
- 风险标记：中

**Step 3.3｜P0-4 Chronicle 加所有权校验**
- 动作：修改 `chronicle.controller.ts` + `chronicle.service.ts`
- 产出：
  - Controller `addReview` 加 `@Request() req` + userId
  - Service `addReview` 增加 userId 参数 + 所有权校验
- 验收标准：`npx tsc --noEmit` 0 errors
- 验证方法：tsc
- 回滚方式：恢复原文件
- 风险标记：低

**Step 3.4｜批次 3 类型检查**
- 动作：`cd apps/api-server && npx tsc --noEmit`
- 产出：类型检查结果
- 验收标准：0 errors
- 验证方法：命令 exit 0
- 回滚方式：逐个检查 error 并修复
- 风险标记：中
- Plan B：如果有 error，检查 import 路径 + 类型定义 → 修复 → 重试

---

### 批次 4：UserMemory 清理暂停 P0-1（<5 分钟）

**Step 4.1｜修改 cleanExpired 方法**
- 动作：修改 `memory-forget.service.ts` L98-109
- 产出：cleanExpired 改为暂停模式（方案 B）
- 验收标准：`grep "P0-1 安全止损" memory-forget.service.ts` 有输出
- 验证方法：Grep + Read
- 回滚方式：恢复原方法
- 风险标记：低

**修改后代码**：
```typescript
private async cleanExpired(): Promise<number> {
  // P0-1 安全止损：暂停清理，避免 deleteMany 删除整行 UserMemory
  // 原 deleteMany 会丢失用户所有历史档案（chartHistory/consultHistory/timelineEvents/insights）
  // 后续 P1 改为只清空 P2-1 扩展字段（type/content/sourceQuote/issueId/confidence/weight/expiresAt）
  this.logger.log('[MemoryForget] cleanExpired 已暂停（P0-1 安全止损）');
  return 0;
}
```

---

### 批次 5：统一授权守卫盘点 P0-7（<15 分钟）

**Step 5.1｜Grep 所有 Controller**
- 动作：Grep 所有 `@Controller` 装饰器
- 产出：Controller 清单 + @UseGuards 状态
- 验收标准：列出所有 Controller 及其 Guard 状态
- 验证方法：Grep 输出
- 回滚方式：N/A（只读）
- 风险标记：低

**Step 5.2｜盘点未启用 Guard 的 Controller**
- 动作：逐一检查每个 Controller 是否有 `@UseGuards(JwtAuthGuard)`
- 产出：未启用 Guard 的 Controller 清单
- 验收标准：清单完整
- 验证方法：Grep + Read
- 回滚方式：N/A
- 风险标记：低

**Step 5.3｜为未启用 Guard 的 Controller 添加 @UseGuards**
- 动作：逐一添加 `@UseGuards(JwtAuthGuard)`（排除 auth.controller.ts 等公开接口）
- 产出：所有需认证的 Controller 都有 @UseGuards
- 验收标准：`npx tsc --noEmit` 0 errors
- 验证方法：tsc
- 回滚方式：移除添加的 @UseGuards
- 风险标记：中
- Plan B：如果某 Controller 加 Guard 后报错，检查是否依赖公开访问 → 加 @Public 装饰器或回滚

---

### 批次 6：删除审计 P0-8（<15 分钟）

**Step 6.1｜Grep 所有 delete 操作**
- 动作：Grep `deleteMany\|delete(` 在 src/ 下
- 产出：delete 操作清单（文件+行号+上下文）
- 验收标准：清单完整
- 验证方法：Grep 输出
- 回滚方式：N/A
- 风险标记：低

**Step 6.2｜为每个 delete 操作添加审计日志**
- 动作：在每个 delete 前后添加 `this.logger.log('[AUDIT]...')`
- 产出：所有 delete 操作有审计日志
- 验收标准：`grep "\[AUDIT\]" src/` 输出覆盖所有 delete 操作
- 验证方法：Grep
- 回滚方式：移除审计日志
- 风险标记：中

**审计日志模板**：
```typescript
// delete 前
const count = await this.prisma.<model>.count({ where: {...} });
this.logger.log(`[AUDIT] DELETE <model> count=${count} by userId=${userId} reason=<reason>`);

// 执行 delete
const result = await this.prisma.<model>.deleteMany({ where: {...} });

// delete 后
this.logger.log(`[AUDIT] DELETE <model> executed, affected=${result.count}`);
```

**Step 6.3｜批次 6 类型检查**
- 动作：`npx tsc --noEmit`
- 产出：类型检查结果
- 验收标准：0 errors
- 验证方法：命令 exit 0
- 回滚方式：修复 error
- 风险标记：低

---

### 批次 7：本地构建 + 部署 + 验证（<30 分钟）

**Step 7.1｜本地全量类型检查**
- 动作：`cd apps/api-server && npx tsc --noEmit`
- 产出：类型检查结果
- 验收标准：0 errors
- 验证方法：命令 exit 0
- 回滚方式：修复 error
- 风险标记：中

**Step 7.2｜本地构建**
- 动作：`npm run build`
- 产出：`dist/` 目录
- 验收标准：`dist/main.js` 存在
- 验证方法：`ls dist/main.js`
- 回滚方式：修复 build error
- 风险标记：中
- Plan B：build 失败 → 检查 error → 修复 → 重试

**Step 7.3｜SCP 上传 dist/**
- 动作：`scp -r dist/ root@8.148.245.29:/opt/awkn-life/awkn-life-backend/apps/api-server/dist/`
- 产出：生产服务器 dist/ 更新
- 验收标准：scp exit 0
- 验证方法：SSH `ls -la dist/main.js`
- 回滚方式：恢复旧 dist/（从备份）
- 风险标记：中

**Step 7.4｜SCP 上传 prisma/schema.prisma（如 P0-5 修改）**
- 动作：上传修改后的 schema.prisma
- 产出：生产 schema.prisma 更新
- 验收标准：scp exit 0
- 验证方法：SSH `grep "model MemoryEmbedding" prisma/schema.prisma`
- 回滚方式：恢复原 schema.prisma
- 风险标记：中

**Step 7.5｜服务器 prisma generate + db push（如 P0-5 需要）**
- 动作：`cd /opt/awkn-life/awkn-life-backend/apps/api-server && npx prisma generate && npx prisma db push`
- 产出：Prisma Client 重新生成 + DB 同步
- 验收标准：两个命令都 exit 0
- 验证方法：命令输出
- 回滚方式：恢复备份 DB + 旧 schema
- 风险标记：🔴 高
- Plan B：失败 → 恢复备份 DB + 旧 dist + pm2 restart｜证据=命令输出

**⚠️ 本步骤需用户确认后执行**

**Step 7.6｜PM2 重启**
- 动作：`pm2 restart awkn-life-backend`
- 产出：后端服务重启
- 验收标准：`pm2 describe awkn-life-backend` 显示 status=online，unstable_restarts=0
- 验证方法：`pm2 describe awkn-life-backend | grep -E "status|unstable"`
- 回滚方式：`pm2 restart awkn-life-backend`（用旧 dist）
- 风险标记：中

**Step 7.7｜5 件套验证**
- 动作：执行 `verify-deploy.sh` 或手动 5 件套
- 产出：5 件套验证结果
- 验收标准：5 项全 PASS
- 验证方法：
  1. HTML title: `curl -s http://localhost:80/ | grep "<title>人生决策宗师</title>"`
  2. JS hash: `curl -s http://localhost:80/ | grep -oE 'src="[^"]+-[A-Za-z0-9_-]{8,}\.js"'`
  3. 健康检查: `curl -s http://localhost:3000/api/v1/health | grep '"code":0'`
  4. API 404: `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/v1/__nonexistent__`
  5. PM2 稳定: `pm2 describe awkn-life-backend | grep "unstable restarts" | awk '{print $4}'`
- 回滚方式：恢复旧 dist + pm2 restart
- 风险标记：中
- Plan B：任一项 FAIL → 回滚到备份 dist + 重启 → 排查

**Step 7.8｜认证场景验证**
- 动作：curl 测试 3 个场景
- 产出：认证验证结果
- 验收标准：
  1. 无 token 调用 `POST /consult/followup` → 401
  2. 用 A 的 token 调用 B 的 recordId → 403
  3. 用 A 的 token 调用 A 的 recordId → 200 或 4xx（非 401/403）
- 验证方法：curl 输出 HTTP status code
- 回滚方式：N/A（验证不修改数据）
- 风险标记：低

---

## E. 高风险清单（自动标红）

| 批次 | 风险项 | 风险等级 | Plan B |
|------|--------|---------|--------|
| 批次 2 Step 2.4 | prisma db push 修改生产 DB | 🔴 高 | 失败 → 恢复备份 DB + 重启服务 |
| 批次 7 Step 7.5 | 服务器 prisma generate + db push | 🔴 高 | 失败 → 恢复备份 DB + 旧 dist + pm2 restart |
| 批次 7 Step 7.6 | PM2 重启 | 🟡 中 | 失败 → 用旧 dist 重启 |
| 批次 3 | 前端兼容性 | 🟡 中 | 前端已传 JWT，无需改动（C3 约束） |
| 批次 5 | 误伤公开接口 | 🟡 中 | 加 Guard 前检查是否公开接口 |

---

## F. 最终验收（DoD）

### F.1 用户可见体验
1. 用户 A 无法访问用户 B 的 followup/dialogue/chronicle（403）
2. 未登录用户无法访问任何资产接口（401）
3. 用户过期记忆不再丢失（UserMemory 整行保留）

### F.2 系统可观测性
1. 所有 delete 操作有 `[AUDIT]` 日志
2. PM2 日志无异常崩溃
3. 5 件套验证全 PASS

### F.3 稳定性
1. 连续 3 次 5 件套验证通过
2. PM2 unstable_restarts=0 持续 5 分钟
3. 关键接口（health/followup/dialogue/chronicle）响应正常

### F.4 回归清单
1. ✅ 健康检查 `/api/v1/health` 返回 200
2. ✅ 登录接口 `/api/v1/auth/login` 正常
3. ✅ 咨询接口 `/api/v1/consult/create` 正常
4. ✅ Followup 接口需认证（401/403）
5. ✅ Dialogue 接口需认证 + 所有权（401/403）
6. ✅ Chronicle 接口需认证 + 所有权（401/403）

---

## G. 回滚总策略

### G.1 代码回滚
- 本地：`git checkout -- <file>` 恢复单个文件
- 服务器：`git reset --hard <backup-commit>` 恢复整个后端

### G.2 DB 回滚
```bash
ssh root@8.148.245.29 "cp /opt/awkn-life/awkn-life-backend/apps/api-server/prisma/prod.db.bak.20260711 /opt/awkn-life/awkn-life-backend/apps/api-server/prisma/prod.db"
```

### G.3 服务回滚
```bash
ssh root@8.148.245.29 "pm2 restart awkn-life-backend"
```

### G.4 完全回滚（代码 + DB + 服务）
```bash
# 1. 恢复 DB
ssh root@8.148.245.29 "cp /opt/awkn-life/awkn-life-backend/apps/api-server/prisma/prod.db.bak.20260711 /opt/awkn-life/awkn-life-backend/apps/api-server/prisma/prod.db"
# 2. 恢复代码
ssh root@8.148.245.29 "cd /opt/awkn-life/awkn-life-backend && git reset --hard 4bbcd7c"
# 3. 重启
ssh root@8.148.245.29 "pm2 restart awkn-life-backend"
```

---

## H. 批次执行顺序与暂停点

```
批次 0（前置备份）─ 自动执行
  ↓
批次 1（P0-6 盘点）─ 自动执行（只读）
  ↓
批次 2（P0-5 迁移验证）
  ├─ Step 2.1-2.3 自动执行
  └─ ⏸️ Step 2.4 暂停点 1：需用户确认是否 db push
  ↓
批次 3（P0-2/3/4 认证修复）─ 自动执行
  ↓
批次 4（P0-1 清理暂停）─ 自动执行
  ↓
批次 5（P0-7 统一守卫）─ 自动执行
  ↓
批次 6（P0-8 删除审计）─ 自动执行
  ↓
批次 7（构建 + 部署 + 验证）
  ├─ Step 7.1-7.4 自动执行
  ├─ ⏸️ Step 7.5 暂停点 2：需用户确认服务器 prisma generate + db push
  ├─ Step 7.6-7.7 自动执行（PM2 重启 + 5 件套）
  └─ Step 7.8 自动执行（认证场景验证）
```

### 暂停点说明
- **暂停点 1**（批次 2 Step 2.4）：生产 DB 表结构变更，需用户确认
- **暂停点 2**（批次 7 Step 7.5）：服务器端 prisma generate + db push，需用户确认

---

## I. 执行结果单模板

完成后将填写 `P0执行结果单-20260711.md`，包含：
- 8 项 P0 修复状态（PASS/FAIL）
- 每批次执行结果
- 验证结果（tsc/build/5件套/认证场景）
- 回滚方案（如需要）
- 用户决策记录

---

## J. 完成标准

- 8 项 P0 全部修复
- `npx tsc --noEmit` 0 errors
- `npm run build` 成功
- 生产 5 件套全 PASS
- 认证场景验证通过（401/403）
- DB 完整性检查通过
- 执行结果单填写完整

---

## K. 下一步

待用户确认本计划后，进入 **Step 04 Patch**，从批次 0 开始执行。
