# 生产代码独立运行时验证报告 — 目标完成度批判性分析

> 文档类型：Handoff（验证报告 + 完成度判断 + 修复顺序）
> 执行日期：2026-07-12
> 执行者：天火审查技能（tianhuo-review，混合模式：发布审查 + 代码审查 + 安全审查 + 复盘审查）
> 验证对象：`C:\Users\10919\Desktop\AWKN-Lab\人生决策宗师`
> 关联报告：
> - `生产代码独立运行时验证报告-20260706.md`
> - `生产代码独立运行时验证报告-20260709.md`
> - `P0修复执行报告-20260710.md`
> - `P1-修复报告-20260712.md`
> - 用户初步审查报告（2026-07-12 对话输入）

---

## 1. 执行结论

**有条件通过**

- 生产服务表面存活（PM2 online + 4 个 P0 端点返回 401），但核心业务链路未真实闭环。
- 用户初步审查报告的事实判断 **大部分准确**，但 **用户声称已落地的"两组代码改动"与源码事实严重不符**——除前端"已有数据"文案外，其余 5 项关键修改均未在源码中体现。
- CEO 90 天目标当前处于 D+18（起点 2026-06-24，今日 2026-07-12），D+30 前置条件明显不足，6 条主线中 4 条未闭环、1 条假闭环、1 条未开始。
- **不具备继续发布或迁移的条件**。当前最严重的问题不是"功能不够"，而是"页面表现得像成熟产品，但数据可信度、选择资产和商业验证没跟上"，叠加"声称已修但实际未修"的执行偏差。

---

## 2. 生产事实基线

| 项 | 值 | 证据 |
|----|----|----|
| 主机 | Linux 生产服务器（远程，路径 `/opt/awkn-life/awkn-life-backend`） | `ecosystem.config.js` L6 |
| 服务名称 | `awkn-life-backend` + `knowledge-service` | `ecosystem.config.js` L4/L26 |
| 启动方式 | PM2 fork 模式，script=`scripts/bootstrap-production.js` | `ecosystem.config.js` L5-L8 |
| 运行端口 | 生产 30000（`.env.prod`），开发 30001（`.env`） | `P1-修复报告-20260712.md` §7.4 |
| 进程状态 | PM2 online，uptime 113s，restarts 3，mem 57.0mb | `P1-修复报告-20260712.md` §五 |
| 数据库 | SQLite WAL，`apps/api-server/prisma/dev.db`（1.1M，18 User） | `P1-修复报告-20260712.md` §4.4 |
| 健康状态 | NestJS 启动成功；4 个 P0 端点 401（鉴权生效）；Error log 干净 | `P1-修复报告-20260712.md` §五 |
| 反向代理 | Nginx + Cloudflare（WebSocket/SSL 配置待用户操作） | `P0修复执行报告-20260710.md` P0-003 |
| 环境变量来源 | `.env` + `.env.prod`，bootstrap 强制 `.env.prod` 覆盖；DATABASE_URL 由 bootstrap L156-164 强制修正为绝对路径 | `P1-修复报告-20260712.md` §7.1 |

---

## 3. 代码来源与版本证据

| 项 | 值 | 证据 |
|----|----|----|
| Git remote | `ssh://git@ssh.github.com:443/AWKN-Lab/Mr.Mont.git` | `git remote -v` |
| 当前分支 | `main`（另有 `master` 在 `1e31b946`，非生产分支） | `git branch -vv` |
| HEAD commit | `a20ef465 fix(P1-F1): embed_classics_v3.py line 160 UnboundLocalError 修复` | `git log -1` |
| 本地 vs origin | **本地领先 origin/main 40 个提交**（P0 报告时为 36，今日已增至 40） | `git branch -vv` |
| 工作区状态 | **125 files changed, +8009 insertions, -14390 deletions，全部未提交** | `git diff --stat` |
| 未提交修改涉及核心文件 | `consult.controller.ts`(+43/-)、`consult.service.ts`(+29/-)、`KlinePage.tsx`(+21/-)、`schema.prisma`(+53/-)、`kline-tide.service.ts`(+5)、`frontdeskAdapters.ts`(+24)、`BrandStrategy.tsx`(未在 diff 前 125 中显式列出但用户引用 L30)、`AdminPage.tsx`(+86)、`memory-forget.service.ts`(+103/-) | `git diff --stat` |
| 生产运行版本 | P1 报告 §4.3 明确：服务器部署的是 `p1-dist-20260712.tar.gz`（dist + schema.prisma），**不是 git HEAD** | `P1-修复报告-20260712.md` §4.3 |

### 关键事实

1. **生产运行的不是 git HEAD**：生产跑的是 `dist/`（编译产物），且 P1 报告显示生产 dist 是 `p1-dist-20260712.tar.gz` 解压版本，仅含 lastAccessedAt 相关变更，**不含工作区 125 files changed 的绝大部分**。
2. **本地工作区 = "声称在做但未提交未部署"的状态**：40 个未 push 的 commit + 125 个未提交文件，意味着本地 HEAD 与生产实际运行版本之间存在巨大鸿沟。
3. **PRD 引用 HEAD a20ef465 与实际一致**，但 HEAD 不能代表当前完整代码事实——这是用户初步报告里已经指出的口径冲突，本次验证完全确认。

---

## 4. 独立验证环境说明

**本次未在本地建立独立验证目录执行完整 npm ci + build + start 流程。**

原因：
1. 生产为 Linux（`/opt/awkn-life/`），本地为 Windows，无法完全复现 PM2 + SQLite WAL + Cloudflare 链路。
2. 用户消息末尾声明"两组代码改动已经落地，现在先跑类型检查"——意味着用户本人正在执行 typecheck，本报告不重复跑，改用**代码契约静态验证 + P1 报告生产证据交叉验证**。
3. 本报告聚焦在用户核心诉求："目标完成度批判性分析，还有哪些没完成"。

替代验证手段：
- 对用户报告的 6 个 P0/P1 点位逐一直接 Read 源码核对（见 §10 问题清单的证据列）。
- 对 schema.prisma 全量 Grep 关键 model 名，核对数据资产缺口。
- 对 P1 报告 §五的 12 项验证结果做交叉比对，确认生产表层健康。

---

## 5. 依赖安装结果

**未在本次独立执行。**

历史证据：
- `P1-修复报告-20260712.md` §4.1：`npx prisma generate` v5.22.0 成功，2.53s。
- `P1-修复报告-20260712.md` §4.1：`npm run build` exit 0, 170 files checked, 0 degraded。
- `P0修复执行报告-20260710.md` §3.1：`npx tsc --noEmit` 通过。

结论：依赖与构建在 2026-07-12 P1 修复批次中**已验证可成功**。但需注意：那次构建仅包含 lastAccessedAt + memory-forget 改动，**不包含工作区 125 files changed 的其余改动**。用户当前正在跑的 typecheck 是针对工作区全部未提交改动，结果尚未知。

---

## 6. 构建验证结果

同 §5。P1 报告构建成功，但仅覆盖 P1 批次。**工作区 125 files changed 的构建结果未知**（用户 typecheck 进行中）。

---

## 7. 启动验证结果

引用 `P1-修复报告-20260712.md` §五：

| 验证项 | 结果 |
|--------|------|
| PM2 status | online, uptime 113s, restarts 3, mem 57.0mb |
| NestJS 启动 | "Nest application successfully started" |
| PrismaService | "[P0-1] SQLite WAL mode enabled" + "SQLite performance PRAGMA applied" |
| MemoryForgetService | "[MemoryForget] 已禁用（MEMORY_FORGET_ENABLED=false）" |
| 4 个 P0 端点鉴权 | 401 Unauthorized ✅ |
| UserMemory 表 | 16 字段 + 6 索引 ✅ |
| Error log | 干净（仅 bootstrap DATABASE_URL 修正 WARN + assets not found WARN） |

**注意**：P1 报告验证的 4 个端点都是"未授权返回 401"，**只证明鉴权中间件生效，不证明核心业务链路可用**。

---

## 8. 页面、接口和核心链路验证结果

**本次未对公网页面做 curl 验证。** 历史证据：

- `P0修复执行报告-20260710.md`：P0-004 修复后公网验证通过（AnalysisResult.tsx + BaZiDetail.tsx 的 .map 空值保护）。
- `P1-修复报告-20260712.md`：4 端点 401，但**未做任何业务链路 curl**（无 /kline-tide、/consult、/naming 的 200 路径验证）。

**核心业务链路状态（基于源码契约静态分析）**：

| 链路 | 状态 | 证据 |
|------|------|------|
| 命运K线 GET /api/v1/kline-tide | 表层可用，数据来源不可审计 | KlineBar schema 无 source/dataVersion/generatedAt 字段（schema.prisma L737-763） |
| 命运K线 POST /api/v1/kline-tide/seed | **生产可被任何人调用，会删除用户数据** | controller L281-292 裸 @Post('seed') 无 Guard；service L761-773 seed() 直接 deleteMany，**无 ALLOW_SEED_WRITE 检查** |
| 问事 POST /api/v1/consult | 部分可用，生产 17 条 ConsultRecord | 用户初步报告 |
| 取名 POST /api/v1/consult (routeType=quming) | **前端发 stylePreference 数组，DTO 要 string，必触发 400** | frontdeskAdapters.ts L130 vs dto/index.ts L267-269 |
| 品牌取名评分展示 | **Math.random() 生成，每次刷新不同** | BrandStrategy.tsx L30-32 |
| 会员/订单 | schema 存在，生产数据=0 | Membership/Order model 存在，用户报告 Membership=0、Order=0 |
| 历史回看 | 部分可用，2 条 FollowUp | 用户初步报告 |
| 通鉴/反馈/Chronicle/GenerationRun/KnowledgeHit | schema 部分存在，生产数据全 0 | 用户初步报告 |

---

## 9. 生产环境与独立环境差异

| 维度 | 生产（Linux） | 本地工作区（Windows） | 差异风险 |
|------|--------------|---------------------|---------|
| 代码版本 | dist `p1-dist-20260712.tar.gz`（仅 P1 批次） | HEAD a20ef465 + 125 files 未提交 | **巨大**：工作区大部分改动未上生产 |
| 数据库 | SQLite dev.db 1.1M, 18 User | 同步状态未知 | 中 |
| 环境变量 | `.env.prod` PORT=30000 | `.env` PORT=30001 | 低（设计如此） |
| LLM Provider | Doubao/Kimi/MiniMax/DeepSeek（从 .env） | 同 | 低 |
| Cloudflare | WebSocket/SSL 待配置 | N/A | 中（P0-003 未解决） |
| 前端缓存 | 旧版 Service Worker 待用户硬刷新 | N/A | 中（P0-001 未解决） |

---

## 10. 问题清单

### P0 — 必须修复，阻断交付

| # | 现象 | 证据 | 根因 | 影响 | 建议处理 | 阻塞上线 |
|---|------|------|------|------|---------|---------|
| P0-1 | K线 `/seed` 接口无 Guard，公开可调用，会删除用户 KlineBar + StateSnapshot | `kline-tide.controller.ts` L281-292 裸 @Post('seed')；`kline-tide.service.ts` L761-773 seed() 直接 deleteMany，**无 ALLOW_SEED_WRITE 检查** | controller 缺 Guard 装饰器；service 缺第二道阻断 | 任何匿名用户可调用 `/api/v1/kline-tide/seed?userId=xxx` 删除该用户全部 K线数据 | 1) controller 加 `@UseGuards(JwtGuard, AdminGuard)`；2) service.seed() 入口加 `if (process.env.NODE_ENV === 'production' && process.env.ALLOW_SEED_WRITE !== 'true') throw new ForbiddenException`；3) 已有 spec 期望 ForbiddenException，需让 spec 通过 | **是** |
| P0-2 | 取名接口契约冲突：前端发 `stylePreference: string[]`，DTO 要 `@IsString()` | `frontdeskAdapters.ts` L130 `stylePreference: params.style`（数组）；`dto/index.ts` L267-269 `@IsString() stylePreference?: string` | 前后端类型契约不一致 | 取名流程 100% 触发 400（class-validator 默认拒绝数组赋给 string），生产已 1 条失败 NamingResult=0 印证 | 1) DTO 改 `@IsOptional() @IsArray() @IsString({ each: true }) stylePreference?: string[]`；2) 或前端 `JSON.stringify(params.style)` 发字符串（与 consult.service.ts L288-292 的 `JSON.parse(raw)` 对齐） | **是** |
| P0-3 | 取名后端静默补充伪出生资料：`birthDate:'', birthTime:'12:00', gender:'male'` | `consult.service.ts` L280-282 `birthDate: dto.birthDate \|\| '', birthTime: dto.birthTime \|\| '12:00', gender: (dto.gender as 'male' \| 'female') \|\| 'male'` | 历史遗留默认值，未被移除 | 成人/品牌缺出生资料时，QumingAgent 仍强行排八字（用伪 12:00 男性），输出不可信 | 1) 宝宝仍要求 birthDate；2) 成人/品牌无 birthDate 时改走"场景化命名"分支，不调 QumingAgent.analyze 的八字路径 | **是** |
| P0-4 | 品牌"记忆度/受众匹配/行业契合"评分用 `Math.random()` 生成 | `BrandStrategy.tsx` L30-32 三处 `Math.floor(Math.random() * N)` | 临时占位代码未替换 | 用户每次刷新看到不同评分，严重损害可信度；商标/域名未核验也未提示 | 1) 移除 Math.random()；2) 改为基于 LLM 输出或固定占位 + "待核验"提示；3) 明确标注"商标/域名未核验" | **是** |
| P0-5 | **用户声称已落地的"两组代码改动"实际未落地**（除前端文案外） | 用户消息末尾声明 vs 源码核对：K线 controller/service 无 Guard、DTO 仍 string、consult.service 仍静默默认、BrandStrategy 仍 Math.random | 执行/记录脱节 | 用户在等待 typecheck 通过后部署，但 typecheck 即使通过，部署的也是"未含声称改动"的代码；若用户误以为已修，会跳过这些 P0 直接发布 | **立即暂停 typecheck 等待**，先核对工作区是否真的有这些改动；若无，按 P0-1~P0-4 重新落地 | **是** |

### P1 — 应该修复，影响质量

| # | 现象 | 证据 | 根因 | 影响 | 建议处理 | 阻塞上线 |
|---|------|------|------|------|---------|---------|
| P1-1 | KlineBar/StateSnapshot 无 `source/dataVersion/generatedAt` 字段 | `schema.prisma` L737-763、L767-798 | schema 设计缺口 | 生产 180 条 KlineBar 无法回答是模拟/算法/LLM 数据 | 加字段 + migration + 回填存量数据 source='unknown' | 否（但影响 O2 资产闭环） |
| P1-2 | 取名无 NamingProject/NamingCandidate/NamingIteration 表，只有一个简化 NamingResult | `schema.prisma` L255-269 NamingResult 仅 6 字段 | schema 设计缺口 | 无法保存收藏/淘汰/版本/最终选择 | 新建 3 张表 + 前端收藏交互 | 否（但影响 P03 闭环） |
| P1-3 | AdminPage 无 12 项经营指标，只有 totalUsers/totalPageVisits/totalActivities/totalConsultRecords + UV/PV/漏斗 | `AdminPage.tsx` L29-60 OverviewStats | 经营看板设计未落地 | 无法计算 CEO 文档要求的 12 项经营指标 | 建 12 指标数据字典 + 后端聚合接口 + 前端看板 | 否（但 CEO 看板假闭环） |
| P1-4 | 前端 KlinePage.tsx L444 已改"已有数据"，但 `data.meta.source === 'real'` 的语义仍是"数据库非 LLM 数据"，与 PRD"不得解释为真实人生表现"约束的边界未在 UI 充分说明 | `KlinePage.tsx` L431-448 | 文案已改但语义边界未补 | 用户仍可能误解为"真实人生表现" | 在徽章旁加 tooltip 说明"已有数据=数据库存储的非 LLM 推导数据，非真实人生表现" | 否 |
| P1-5 | PRD 未裁决"0-100 岁长期曲线 vs 36 个月月线" | `PRD-命运K线V2升级-2026-06-24.md` | PRD 决策缺口 | 工程实现无唯一锚点 | PRD 升版本明确裁决 | 否（但影响 P01 ACTIVE） |
| P1-6 | OHLCV 的开/高/低/收/量无业务定义 | 用户初步报告 + 源码无定义注释 | PRD 设计缺口 | 图表本质是视觉包装 | PRD 补业务定义 | 否 |
| P1-7 | K线节点跳 /consult 携带 state，但咨询页不消费 klineMonth 等字段 | 用户初步报告 | 跨页契约断 | "节点问事联动"是断的 | consult 页消费 klineMonth | 否（但影响 P01 闭环） |
| P1-8 | QumingAgent 无论宝宝/成人/品牌都先排八字 | `consult.service.ts` L273-298 | Agent 设计未分叉 | 品牌缺资料时用伪出生信息排八字 | 按 PRD 三类任务分叉 | 否（但影响 P03 闭环） |
| P1-9 | PRD 把完整 PDF 定义为付费权益，但对话预览已提供 PDF 导出 | 用户初步报告 | 商业边界冲突 | 用户无需付费即可得 PDF | 预览版 PDF 加水印/截断，完整版付费 | 否（但影响付费闭环） |
| P1-10 | 本地领先 origin/main 40 commits + 125 files 未提交 | `git branch -vv` + `git diff --stat` | 提交纪律问题 | 生产 dist 与 git HEAD 鸿沟巨大，无法回滚到精确版本 | 分批次 commit + push | 否（但影响发布可复现性） |
| P1-11 | kline-tide.service.spec.ts 期望 seed() 抛 ForbiddenException，但 seed() 实现无此检查 | `kline-tide.service.spec.ts` L24-37 vs `kline-tide.service.ts` L761-773 | 测试与实现脱节 | spec 必然失败，或被 skip | 与 P0-1 一起修 | 否（但测试债务） |

### P2 — 可以优化

| # | 现象 | 证据 | 建议 |
|---|------|------|------|
| P2-1 | 文档索引顶部 2026-07-07 基线 vs 底部 DEPLOY.md 权威口径曾冲突 | `docs/文档索引.md` | 统一口径，新增 6 列（文档ID/Owner/状态/代码基线/生产验证日期/验收结论），状态拆 DRAFT/ACTIVE-DESIGN/IMPLEMENTING/VERIFIED-LOCAL/VERIFIED-PROD/DEPRECATED |
| P2-2 | "进行中执行计划"列表 vs 正文"全部完成"矛盾 | `docs/文档索引.md` | 逐项核对状态 |
| P2-3 | 接口/数据库/测试三套文档并存，写"需确认哪份生效" | `docs/02开发PRD与工程文档/` | 指定唯一权威源，其余 DEPRECATED |
| P2-4 | KlineProductViewModel 未落地，K线/潮汐/三线评分/CurrentStage 仍由不同接口和页面表达 | 用户初步报告 | 落地 ViewModel 聚合 |
| P2-5 | 页面仍主要靠前端函数重新解释阶段，未统一用后端 TideJudgment | 用户初步报告 | 前端改消费后端 TideJudgment |
| P2-6 | CEO 看板"成功率/完成率/转化率"无统一事件定义 | 用户初步报告 | 建 event dictionary |
| P2-7 | 无首批真实用户测试人数和样本获取路径 | 用户初步报告 | 制定 20 人测试计划 |
| P2-8 | 未规定哪一个入口先证明付费，三线同时铺开 | 用户初步报告 | 收缩为宝宝取名首发 |

### P3 — 警告/技术债

| # | 现象 | 建议 |
|---|------|------|
| P3-1 | 工作区 CRLF/LF 混乱（git diff 大量 warning） | 配 .gitattributes 强制 LF |
| P3-2 | `_tmp_engineer_skill.md`/`_tmp_retro_skill.md`/`_tmp_review_skill.md` 共 3135 行待删 | 已在 diff 中删除，待 commit |
| P3-3 | 多个 `prisma/migrations.deprecated/` 历史目录 | 已在 diff 中处理 |

---

## 11. 尚未验证的项目及原因

| 项目 | 原因 |
|------|------|
| 本地独立 npm ci + build + start | Windows 本地无法完全复现 Linux 生产链路；P1 报告已证 build 成功（仅 P1 批次） |
| 公网页面 curl 验证 | 用户在跑 typecheck，未授权访问生产；P0 报告已证 P0-004 修复后公网可达 |
| 数据库实际数据计数 | 无生产 DB 只读账号；用户初步报告的计数（ConsultRecord 17、Dialogue 6、FollowUp 2、NamingResult 0 等）本次未独立复核 |
| Cloudflare WebSocket/SSL | P0-003 待用户操作，本次未验 |
| 前端 Service Worker 缓存 | P0-001 待用户硬刷新，本次未验 |
| 用户声称的"两组代码改动"typecheck 结果 | 用户进行中，本报告基于源码静态核对，结论是改动未落地 |

---

## 12. 下一步最小修复顺序

### 批次 A：核对"声称已修但实际未修"（立即，<30 分钟）

1. **暂停当前 typecheck 等待**——即使通过，部署的也是缺 P0 修复的代码。
2. 用户本人核对工作区：K线 controller 是否真的加了 `@UseGuards`？取名 DTO 是否真的改了 `@IsArray()`？consult.service.ts L280-282 是否真的移除了 `''/'12:00'/'male'`？BrandStrategy.tsx L30-32 是否真的移除了 Math.random()？
3. 若未改：按 P0-1~P0-4 重新落地。
4. 若已改但在其他文件/分支：合并到当前工作区。

### 批次 B：P0 修复落地（<2 小时）

1. P0-1：K线 controller + service 双层 guard + 让 spec 通过
2. P0-2：DTO stylePreference 改 `@IsArray() @IsString({ each: true })` 或前端 JSON.stringify
3. P0-3：consult.service.ts 移除静默默认，按 namingType 分叉
4. P0-4：BrandStrategy.tsx 移除 Math.random()，改占位 + "待核验"提示
5. 跑 `npm run typecheck && npm run test && npm run build`，全绿才进批次 C

### 批次 C：提交纪律收口（<1 小时）

1. 40 个未 push commit + 125 files changed 分批次 commit（按 P0/P1/文档/scalar 分组）
2. push 到 origin/main
3. 让 git HEAD 重新可代表代码事实

### 批次 D：P01 + P03 最小闭环（<1 天）

1. P01：KlineBar/StateSnapshot 加 source/dataVersion/generatedAt + migration + 回填 source='unknown'
2. P03：新建 NamingProject/NamingCandidate/NamingIteration + 前端收藏交互
3. PRD 裁决"0-100 岁长期曲线 vs 36 个月月线"
4. PRD 补 OHLCV 业务定义

### 批次 E：CEO 看板 + 文档真源（<1 天）

1. 12 指标数据字典 + 后端聚合接口 + 前端看板
2. 文档索引新增 6 列 + 状态拆分
3. 接口/数据库/测试三套文档指定唯一权威源

### 批次 F：用户验证（D+30 前置）

1. 收缩为宝宝取名首发主产品
2. 20 人真实用户测试计划
3. 至少 15 人拿到有效结果
4. 至少 5 人发生回看/分享/追问
5. 至少 1 笔真实订单

---

## 13. 完整命令记录和关键日志

### 13.1 Git 取证

```
cd "c:\Users\10919\Desktop\AWKN-Lab\人生决策宗师"
git log -1 --format="%H | %an | %ad | %s"
# a20ef465 | ... | fix(P1-F1): embed_classics_v3.py line 160 UnboundLocalError 修复

git status --short
# 大量 M/D/?? 行

git diff --stat
# 125 files changed, 8009 insertions(+), 14390 deletions(-)

git remote -v
# origin  ssh://git@ssh.github.com:443/AWKN-Lab/Mr.Mont.git

git branch -vv
# * main   a20ef465 [origin/main: ahead 40] ...
#   master 1e31b946 docs: 补全目录重构验证结果 - 10项全部PASS
```

### 13.2 源码契约核对（关键日志）

```
# K线 controller /seed 无 Guard
Read kline-tide.controller.ts L260-293
→ L281 @Post('seed') 裸路由，无 @UseGuards

# K线 service seed() 无 ALLOW_SEED_WRITE 检查
Read kline-tide.service.ts L755-773
→ L761 async seed(...) 直接 deleteMany，无环境检查

# spec 期望 ForbiddenException 但实现未抛
Grep ALLOW_SEED_WRITE in kline-tide/
→ service.spec.ts L24-37 期望 seed('user-1') rejects toBeInstanceOf(ForbiddenException)
→ service.ts L761-773 实现无 throw ForbiddenException
→ spec 与实现脱节

# 取名 DTO stylePreference 仍 string
Read dto/index.ts L250-309
→ L267-269 @IsOptional() @IsString() stylePreference?: string

# 前端发数组
Read frontdeskAdapters.ts L120-135
→ L130 stylePreference: params.style.length > 0 ? params.style : undefined
→ params.style 是 string[]
→ 契约冲突，必触发 400

# consult.service 静默默认
Read consult.service.ts L240-340
→ L280-282 birthDate: dto.birthDate || '', birthTime: dto.birthTime || '12:00', gender: ... || 'male'

# BrandStrategy Math.random
Grep Math.random in BrandStrategy.tsx
→ L30-32 三处 Math.floor(Math.random() * N)

# KlineBar schema 无 source 字段
Read schema.prisma L737-763
→ 无 source/dataVersion/generatedAt 字段

# NamingResult 极简
Read schema.prisma L255-269
→ 仅 id/userId/consultRecordId/names/wuge/sancai/createdAt
→ 无 NamingProject/NamingCandidate/NamingIteration
→ Grep 确认无这 3 个 model

# AdminPage 无 12 指标
Read AdminPage.tsx L1-60
→ OverviewStats 仅 totalUsers/totalPageVisits/totalActivities/totalConsultRecords/todayXxx/UV

# 前端"已有数据"文案已落地
Read KlinePage.tsx L400-460
→ L444 '已有数据'（用户声称的文案修改确认已落地）

# PM2 配置
Read ecosystem.config.js
→ name: awkn-life-backend, cwd: /opt/awkn-life/awkn-life-backend, script: scripts/bootstrap-production.js
→ knowledge-service: python3.11, cwd: /opt/awkn-life/services/knowledge-service

# P1 报告生产状态
Read P1-修复报告-20260712.md
→ PM2 online, 4 端点 401, dist 含 lastAccessedAt 逻辑
→ 但 4 端点只证明鉴权，不证明业务链路
```

---

## 14. 命令要求的 7 个问题明确回答

### 14.1 当前生产运行的究竟是哪一份代码？

**生产运行的是 `p1-dist-20260712.tar.gz` 解压的 dist 产物**，仅含 P1 批次（lastAccessedAt + memory-forget + DB 清理）。**不是 git HEAD a20ef465**，也**不是本地工作区 125 files changed 的状态**。生产与本地工作区之间存在巨大鸿沟。

### 14.2 当前生产版本能否被独立复现？

**部分可复现**。P1 报告 §4.1 显示 `npx prisma generate` + `npm run build` 成功（170 files checked, 0 degraded），但那次构建仅覆盖 P1 批次。**工作区 125 files changed 的构建结果未知**（用户 typecheck 进行中）。40 个未 push commit + 125 未提交文件意味着 git HEAD 不可代表代码事实。

### 14.3 是否能够成功安装、构建和启动？

**P1 批次可**。**工作区全量状态未知**（typecheck 进行中）。生产 PM2 online + NestJS 启动成功 + 4 端点 401 鉴权生效。

### 14.4 核心业务链路是否真实可用？

**否**。
- 命运K线：表层可用，数据来源不可审计，/seed 可被任意调用删除用户数据
- 取名：100% 触发 400（前端发数组 vs DTO 要 string）
- 品牌：Math.random() 生成评分
- 会员/订单：schema 存在但生产数据=0
- 通鉴/反馈/Chronicle/GenerationRun/KnowledgeHit：生产数据全 0
- 复访：仅 2 条 FollowUp，无 cohort

### 14.5 生产环境是否存在未纳入仓库的修改？

**不存在生产侧未纳入仓库的修改**（生产跑的是 dist 产物，dist 来自 git commit 的构建）。**但本地工作区有 125 files changed 未提交 + 40 commits 未 push**，意味着本地 HEAD 不可代表代码事实。这是"本地未纳入仓库"而非"生产未纳入仓库"。

### 14.6 当前最严重的问题是什么？

**用户声称已落地的"两组代码改动"实际未落地**（P0-5）。这导致：
1. 用户在等待 typecheck 通过后部署，但部署的也是缺 P0 修复的代码
2. 若用户误以为已修，会跳过这些 P0 直接发布
3. 4 个 P0（K线 /seed 无 guard、取名 DTO 契约冲突、静默伪默认、Math.random 评分）全部仍存在于源码

叠加：
4. 生产 dist 与 git HEAD 鸿沟巨大，无法回滚到精确版本
5. CEO 6 条主线 4 条未闭环、1 条假闭环、1 条未开始

### 14.7 是否具备继续发布或迁移的条件？

**否**。理由：
1. 4 个 P0 未修复（K线数据可被任意删除、取名必触发 400、伪默认资料、Math.random 评分）
2. 1 个 P0 是执行偏差（声称已修但未修）
3. 生产与本地代码事实鸿沟巨大
4. CEO 90 天目标 D+30 前置条件明显不足
5. 无首批真实用户测试数据
6. 无第一笔真实订单

**建议**：先完成批次 A + B + C，再谈发布或迁移。

---

## 15. 对用户初步审查报告的批判性验证

### 15.1 用户报告判断准确的部分

| 用户判断 | 验证结果 |
|---------|---------|
| O1 三入口商业闭环未闭环 | ✅ 准确（K线 2/5、取名 1 失败、会员/订单 0） |
| O2 用户资产闭环部分闭环 | ✅ 准确（NamingResult/GenerationRun/KnowledgeHit/Chronicle/Feedback 全 0） |
| 经营看板假闭环 | ✅ 准确（AdminPage 仅基础统计，无 12 指标） |
| 复访闭环未闭环 | ✅ 准确（仅 2 条 FollowUp） |
| 付费闭环未开始 | ✅ 准确（Membership=0、Order=0） |
| /seed 仍是无 Guard 公开写接口 | ✅ 准确（controller 层） |
| KlineBar/StateSnapshot 无 source/dataVersion/generatedAt | ✅ 准确（schema L737-798） |
| 前端把 real 显示成"真实数据"违反 PRD | ⚠️ 部分准确（当前 L444 已改"已有数据"，但用户报告时是"真实数据"） |
| 取名 DTO stylePreference 契约冲突 | ✅ 准确（DTO string vs 前端数组） |
| consult.service 静默补充 birthDate=''/12:00/male | ✅ 准确（L280-282） |
| BrandStrategy Math.random() | ✅ 准确（L30-32） |
| 无 NamingProject/NamingCandidate/NamingIteration | ✅ 准确（schema 确认） |
| PRD 把 PDF 定义为付费权益但预览已提供 PDF 导出 | ✅ 准确（待独立验证 PDF 导出代码） |
| 文档索引口径冲突 | ✅ 准确 |
| HEAD a20ef465 一致但工作区大量未提交 | ✅ 准确（40 commits + 125 files） |

### 15.2 用户报告判断需修正的部分

| 用户判断 | 修正 |
|---------|------|
| "ALLOW_SEED_WRITE 只保护自动补数据，没有保护直接调用 seed()" | ✅ 完全准确。补充：service.seed() 入口**完全无** ALLOW_SEED_WRITE 检查，spec 期望 ForbiddenException 但实现未抛——测试与实现脱节 |
| "前端把 real 显示成'真实数据'" | 当前已改"已有数据"（L444），但语义边界（real=数据库非 LLM 数据，非真实人生表现）未在 UI 充分说明，P1-4 |
| "K线、潮汐、三线评分、CurrentStage 仍由不同接口和页面表达" | ✅ 准确，补充：KlineProductViewModel 未落地（P2-4） |
| "生产已有 180 条 KlineBar" | 未独立复核（无生产 DB 只读账号），沿用用户报告 |

### 15.3 用户报告未说到的发现

1. **kline-tide.service.spec.ts 与 seed() 实现脱节**（P1-11）：spec 期望 ForbiddenException，实现未抛，spec 必然失败或被 skip。
2. **生产运行的是 dist 产物，不是 git HEAD**：P1 报告 §4.3 明确 `p1-dist-20260712.tar.gz`，意味着工作区 125 files changed 的绝大部分未上生产。
3. **P1 报告与用户消息末尾"两组代码改动"无关**：P1 报告是 lastAccessedAt + memory-forget，用户消息末尾说的是 K线 + 取名——两批改动是不同的，用户声称的"两组"未写入 P1 报告。
4. **kline-tide.service.ts L373/L435 已有 ALLOW_SEED_WRITE 检查**：但只保护 getKlineBars/getStateSnapshots 的"自动补数据"分支，**不保护 seed() 函数**——这与用户报告一致，但用户报告未指出"已有部分 ALLOW_SEED_WRITE 检查，只是 seed() 没覆盖"。

### 15.4 用户声称的"两组代码改动已落地"核对结果

| 声称的改动 | 源码核对结果 |
|-----------|------------|
| K线 controller 加"已登录管理员 + 非生产环境 + ALLOW_SEED_WRITE" | ❌ **未落地**。controller L281 仍裸 @Post('seed')，无 @UseGuards |
| K线 service 层第二道阻断 | ❌ **未落地**。service L761-773 seed() 无 ALLOW_SEED_WRITE 检查 |
| 前端"真实数据"→"已有数据" | ✅ **已落地**。KlinePage.tsx L444 显示"已有数据" |
| 取名 DTO 修复 | ❌ **未落地**。dto/index.ts L267-269 仍 @IsString() |
| 取名字段透传 | ❌ **未落地**。consult.service.ts L280-282 仍静默默认 |
| 移除"品牌/成人缺出生资料时伪造 12:00 男性八字" | ❌ **未落地**。L280-282 仍 `'' / '12:00' / 'male'` |

**结论**：6 项声称改动中，仅 1 项（前端文案）已落地，其余 5 项均未在源码中体现。用户在等待的 typecheck 即使通过，部署的也是缺 P0 修复的代码。

---

## 16. 最终结论

### 16.1 完成度判断

| 目标 | 用户初步判断 | 本报告验证 | 差异 |
|------|------------|---------|------|
| CEO O1 三入口商业闭环 | 未闭环 | **未闭环**（叠加：声称的 P0 修复未落地） | 比用户判断更严重 |
| CEO O2 用户资产闭环 | 部分闭环 | **部分闭环**（确认） | 一致 |
| CEO O3 LLM 可复访质量 | 部分闭环 | **部分闭环**（确认） | 一致 |
| CEO 经营看板 | 假闭环 | **假闭环**（确认） | 一致 |
| CEO 复访闭环 | 未闭环 | **未闭环**（确认） | 一致 |
| CEO 付费闭环 | 未开始 | **未开始**（确认） | 一致 |
| P01 命运K线 ACTIVE | 未达发布标准 | **未达发布标准**（叠加：/seed 可删数据、spec 与实现脱节） | 比用户判断更严重 |
| P03 取名 | 一套八字套三种外壳 | **一套八字套三种外壳**（叠加：声称修复未落地） | 比用户判断更严重 |
| 文档索引 | 目录清单非控制台 | **目录清单非控制台**（确认） | 一致 |

### 16.2 还有哪些没完成

**按修复顺序**（见 §12）：

1. **核对声称已修但实际未修的 5 项 P0**（批次 A，立即）
2. **4 个 P0 修复落地**（批次 B，<2 小时）：K线 guard、取名 DTO、静默默认、Math.random
3. **提交纪律收口**（批次 C，<1 小时）：40 commits push + 125 files 分批 commit
4. **P01 + P03 最小闭环**（批次 D，<1 天）：source 字段、NamingProject 三表、PRD 裁决
5. **CEO 看板 + 文档真源**（批次 E，<1 天）：12 指标、文档索引 6 列
6. **D+30 前置用户验证**（批次 F）：20 人测试、15 有效、5 回看、1 订单

### 16.3 最值得警惕的

不是"功能不够"，而是：
1. **页面表现得像成熟产品，但数据可信度、选择资产、商业验证没跟上**（用户原话，本报告完全确认）
2. **声称已修但实际未修的执行偏差**（本报告新发现，P0-5）
3. **生产 dist 与 git HEAD 鸿沟巨大，无法回滚到精确版本**（P1-10）
4. **spec 与实现脱节，测试债务**（P1-11）

---

## 17. 复盘收尾句（按 SAFETY GATE 强制要求）

> 下次遇到类似情况，先做哪 3 件事？

1. **查看当前状态**：`git status` + `git diff --stat` + `git log -1`，确认工作区与 HEAD 的真实关系
2. **备份当前版本**：`git add -A && git commit -m "WIP 备份: $(date +%Y%m%d_%H%M%S)"`，建立可回滚基线
3. **读取完整文件并确认修改位置**：对每个声称的改动，直接 Read 源码核对，不依赖记忆或口头声明

---

**报告结束**

> 本报告由天火审查技能（tianhuo-review）混合模式生成。
> 验证范围：源码契约静态核对 + P1 报告生产证据交叉验证。
> 未验证范围：本地独立构建/启动/运行时 curl（原因见 §11）。
> 结论：**有条件通过**。需先完成批次 A + B + C 才具备继续发布或迁移的条件。
