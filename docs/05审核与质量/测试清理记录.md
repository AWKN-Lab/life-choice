# 后端测试 it.skip/xit 清理记录

- 执行日期：2026-06-20
- 执行范围：`apps/AWKN-LABlife/awkn-life-backend/`
- 任务来源：Task 23（测试工程师清理 it.skip/xit）
- 基线：Task 13 报告显示 4 个 skipped，811 passed

## 1. 清理前 skip 清单

| # | 文件 | 行号 | 用例标题 | skip 原因（推断自上下文） |
|---|------|------|---------|------------------------|
| 1 | `apps/api-server/src/consult/__tests__/test-1-big-word.spec.ts` | 64 | When casual 状态 + 大词"事业" → Then 生成追问"事业具体指哪方面" | IntentRouterService 只做路由，"大词追问生成"逻辑未实现为独立可测服务 |
| 2 | `apps/api-server/src/consult/__tests__/test-2-wealth.spec.ts` | 80 | When 财运问题 → Then 生成追问"财运具体指正财还是偏财" | "财源拆解 + 追问"逻辑未实现为独立可测服务 |
| 3 | `apps/api-server/src/consult/__tests__/test-7-memory-trigger.spec.ts` | 172 | When 用户问创业 + 有现金流记忆 → Then 主动生成提醒"3个月前你说现金流紧张" | orchestrator 记忆注入逻辑未实现；且检索基于关键词匹配，无法语义关联"创业"与"现金流" |
| 4 | `apps/api-server/src/consult/__tests__/test-8-active-followup.spec.ts` | 215 | When 合同咨询 + 7 天后回访 → Then 提醒"3件事：确认条款/准备证件/预留资金" | scheduleFollowUp 是通用延迟回访，不含合同场景识别 + 3 件事清单生成 + 个性化提醒 |

清理前 skip 总数：**4**

## 2. 处理策略与判定依据

4 个 skip 均属于同一模式：对应 `docs/03开发过程稿/已完成执行计划/line-02-pipeline.md` 中 T1/T2/T7/T8 设计用例的"最后一公里"（追问生成、财源拆解、记忆注入、合同场景识别），而当前实现只覆盖了底层能力（路由、检索、调度），上层业务逻辑尚未实现。

| 策略 | 适用条件 | 本批是否适用 |
|------|---------|------------|
| 修复启用 | 对应功能已实现，测试可补全断言通过 | ❌ 不适用（功能未实现） |
| 保留 skip + 强化注释 | 对应功能未实现但有保留价值（设计用例文档化） | ✅ 适用（4 个全部） |
| 删除 | 已过时或无意义 | ❌ 不适用（均为设计用例） |

**保留价值判定**：
- 4 个用例均在 line-02-pipeline.md 中标记为 `[x]`（已完成），但实际只完成了底层能力
- 每个用例的"最后一公里"测试 skip 状态，正好文档化了"已实现 vs 未实现"的边界
- 删除会丢失设计意图，修复启用会引入失败测试

## 3. 处理结果（逐项）

### 3.1 test-1-big-word.spec.ts — T1 大词追问
- 处理方式：**保留 skip + 强化注释**
- 修改内容：在 `it.skip` 上方添加结构化注释块，含 SKIP 原因 / 保留策略 / 解除条件 / TODO / 负责人 / 关联文档 / 预期行为
- 解除条件：orchestrator/followup 层实现"大词检测 + casual 状态追问生成"独立服务

### 3.2 test-2-wealth.spec.ts — T2 财运拆解
- 处理方式：**保留 skip + 强化注释**
- 修改内容：同上模板
- 解除条件：orchestrator 实现财源拆解 + 追问生成独立服务

### 3.3 test-7-memory-trigger.spec.ts — T7 记忆触发
- 处理方式：**保留 skip + 强化注释**
- 修改内容：同上模板
- 解除条件：orchestrator 实现记忆注入逻辑 + 检索支持语义关联（embedding 或规则扩展）

### 3.4 test-8-active-followup.spec.ts — T8 主动回访
- 处理方式：**保留 skip + 强化注释**
- 修改内容：同上模板
- 解除条件：followup 实现场景识别 + 3 件事清单生成 + 个性化提醒独立服务

## 4. 清理后 skip 清单

清理后 skip 总数：**4**（数量未变，但每个 skip 均已补齐结构化注释）

> 数量未变的原因：4 个 skip 对应的功能均未实现，无法修复启用；且均为 line-02-pipeline.md 设计用例，不应删除。本次清理的价值在于：把模糊的 `// TODO` 注释升级为可追踪的结构化注释（含原因、解除条件、负责人、关联文档），使 skip 状态可治理、可认领、可追踪。

## 5. 注释模板（统一格式）

每个保留的 skip 均包含以下字段：

```
/**
 * SKIP 原因：[为什么 skip，指向未实现的能力]
 * 保留策略：保留 skip。[为什么不能删除/修复]
 * 解除条件：[实现什么后可以移除 skip]
 * TODO: 待 [服务] 实现后补全断言
 * 负责人: L2-pipeline 维护者（待认领）
 * 关联文档: docs/03开发过程稿/已完成执行计划/line-02-pipeline.md [T?]
 *
 * 预期行为：[用例应验证的行为]
 */
it.skip(...)
```

## 6. 遗留问题

1. **负责人待认领**：4 个 skip 的负责人均标注为"L2-pipeline 维护者（待认领）"，需由实际负责人认领并排期实现对应能力。
2. **设计文档与实现不一致**：line-02-pipeline.md 中 T1/T2/T7/T8 标记为 `[x]`（已完成），但实际"最后一公里"未实现。建议后续在 line-02-pipeline.md 中补充说明：T1/T2/T7/T8 的"已完成"指底层能力已覆盖，上层业务逻辑（追问生成/拆解/注入/场景识别）为后续迭代项。
3. **语义检索能力缺口**：T7 的解除条件依赖"语义关联"能力（embedding 或规则扩展），当前 searchRelevantMemory 仅支持关键词匹配，需在 L3-memory 层补齐。

## 7. 验证方式

```bash
cd apps/AWKN-LABlife/awkn-life-backend/apps/api-server
npx jest --testPathPattern="consult/__tests__"
```

预期：
- 4 个 it.skip 仍为 skipped（数量不变）
- 其余测试全部通过（不引入新失败）
- 与 Task 13 基线一致：4 skipped + 其余 passed

## 8. 改动文件清单

| 文件 | 改动类型 |
|------|---------|
| `apps/api-server/src/consult/__tests__/test-1-big-word.spec.ts` | 注释强化（保留 skip） |
| `apps/api-server/src/consult/__tests__/test-2-wealth.spec.ts` | 注释强化（保留 skip） |
| `apps/api-server/src/consult/__tests__/test-7-memory-trigger.spec.ts` | 注释强化（保留 skip） |
| `apps/api-server/src/consult/__tests__/test-8-active-followup.spec.ts` | 注释强化（保留 skip） |
| `docs/05审核与质量/测试清理记录.md` | 新增（本文件） |

## 9. 回滚方式

本次改动仅为注释增强，未修改任何测试逻辑。回滚方式：
```bash
cd apps/AWKN-LABlife/awkn-life-backend
git checkout -- apps/api-server/src/consult/__tests__/test-1-big-word.spec.ts \
                apps/api-server/src/consult/__tests__/test-2-wealth.spec.ts \
                apps/api-server/src/consult/__tests__/test-7-memory-trigger.spec.ts \
                apps/api-server/src/consult/__tests__/test-8-active-followup.spec.ts
```
（`docs/05审核与质量/测试清理记录.md` 为新增文件，可直接删除）
