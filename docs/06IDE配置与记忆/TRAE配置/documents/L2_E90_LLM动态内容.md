# E90｜LLM 动态内容不在代码库中

## 触发条件
LLM 动态内容 / 样式问题 / 找不到关键词 / 动态渲染

## 教训
搜索"训练目标"、"伤病"等关键词在整个项目中无匹配。这些是 LLM 动态生成的纯文本回复，不在代码库中。要控制样式需新增结构化消息类型（step-card）。

## 反例
花 30 分钟搜索代码库找"训练目标"等关键词，结果不存在。这些内容是 LLM 运行时生成的纯文本。

## 判断规则
搜索关键词无匹配时：
1. 先判断内容是静态代码还是动态生成
2. LLM 回复内容不在代码库中，是运行时生成
3. 要控制样式需新增结构化消息类型，不能通过搜索代码修改

## 验证次数
1 | 跨场景：是 | 置信度：medium

## 来源
2026-06-16 人生决策宗师项目全流程深度复盘

## 关联经验
- 无直接关联，项目特定事实
