# 修复计划：孤儿文件清理 + 文墨天机对标优化

> **日期**：2026-06-16
> **来源**：执行检查 + 文墨天机深度分析
> **状态**：待执行

---

## 一、执行摘要

| 维度 | 当前 | 目标 |
|------|------|------|
| 孤儿组件 | 8 个未接入 | 全部接入或删除 |
| 紫微流盘前端 | 0%（只展示本命盘） | 流盘切换 Tab |
| 四化飞星前端 | 0%（后端已实现） | 飞星图可视化 |
| ziwei-agent 测试 | 0 个 .spec.ts | 1 个核心流程测试 |
| growth.service 测试 | 0 个 .spec.ts | 1 个核心流程测试 |
| sxtwl | 未安装 | 已安装 |

---

## 二、当前状态分析

### 2.1 孤儿文件（验证后确认为 8 个）

| # | 文件 | 目录 | 从未被导入 |
|---|------|------|-----------|
| 1 | `ConclusionPreview.tsx` | `conclusion/` | 是 |
| 2 | `ChineseConclusionC.tsx` | `conclusion/` | 是 |
| 3 | `ModernConclusionB.tsx` | `conclusion/` | 是 |
| 4 | `TraditionalConclusionA.tsx` | `conclusion/` | 是 |
| 5 | `BaguaDiagram.tsx` | `home/` | 是 |
| 6 | `WuxingDiagram.tsx` | `home/` | 是 |
| 7 | `StarmapDiagram.tsx` | `home/` | 是 |
| 8 | `ConfidenceRing.tsx` | `form/` | 是 |

> 注：`CriticalMoments.tsx` 已被 [HomePage.tsx](file:///C:/Users/10919/Desktop/AWKN-Lab/人生决策宗师/apps/AWKN-LABlife/app/src/pages/HomePage.tsx#L16) 导入，不是孤儿。

### 2.2 文墨天机对标差距

- **紫微排盘引擎**：Python CLI 已完整实现（spawnSync 已修复），本命盘/流盘/飞星/格局均可输出
- **前端展示**：只展示本命盘，流盘（大限/流年/流月/流日/流时）和飞星图未透出
- **测试覆盖**：ziwei-agent 和 growth.service 两个核心模块零测试（E28 违反）

### 2.3 数据流现状

```
Python CLI (spawnSync) → ZiweiResult {
  palaces: ✅ 前端展示,
  liupan: ⚠️ 后端已返回但前端未消费,
  feixing: ⚠️ 后端已返回但前端未消费,
  geju: ⚠️ 后端已返回但前端未消费
}
```

---

## 三、修复步骤

### Step 1：安装 sxtwl（P0，1 分钟）

- **动作**：在 Python 环境执行 `pip install sxtwl`
- **产出**：sxtwl 已安装
- **验收**：`python -c "import sxtwl; print('OK')"` 输出 `OK`
- **验证**：运行 `test_liupan.py`，2 个 skipped 变为 pass
- **回滚**：`pip uninstall sxtwl`

---

### Step 2：ziwei-agent 核心流程测试（P0，30 分钟）

- **动作**：新建 `ziwei-agent/__tests__/ziwei-agent.spec.ts`
- **产出**：至少 1 条测试覆盖 `analyze()` 方法
- **测试内容**：
  - 模拟 spawnSync 返回有效 JSON
  - 验证 `ZiweiResult` 字段完整性（palaces, liupan, feixing）
  - 验证 fallback 到 iztro 的路径
- **验收**：`npm test -- --testPathPattern="ziwei-agent"` 通过
- **验证**：`npm test` 全量通过
- **风险**：低，只需 mock `spawnSync` 和 `LlmProvidersService`

---

### Step 3：ZiweiPage 流盘切换 Tab（P1，2 小时）

- **动作**：修改 [ZiweiPage.tsx](file:///C:/Users/10919/Desktop/AWKN-Lab/人生决策宗师/apps/AWKN-LABlife/app/src/pages/ZiweiPage.tsx)
- **产出**：
  - 新增 `liupanTab` state（`'benshen' | 'daxian' | 'liunian' | 'liuyue' | 'liuri'`）
  - 新增 Tab 切换 UI（命盘 / 大限 / 流年 / 流月）
  - 新增 `targetYear` 参数透传到 `consultApi.analyze()`
  - 渲染 `result.liupan` 数据
- **验收**：
  - 切换 Tab 后请求携带 `targetYear` 参数
  - 流盘数据正确渲染在页面上
  - 无 TypeScript 编译错误
- **验证**：`npx tsc --noEmit` 通过 + 浏览器手动切换 Tab
- **回滚**：git revert

---

### Step 4：四化飞星图前端可视化（P1，2 小时）

- **动作**：新建 `components/ziwei/FeixingChart.tsx`
- **产出**：
  - 接收 `ZiweiPalaceData[]` 渲染十二宫飞星关系图
  - 每个宫位显示化禄/化权/化科/化忌四化飞星方向
  - 在 ZiweiPage 中嵌入飞星 Tab
- **验收**：
  - 飞星图正确渲染十二宫
  - 四化星曜正确关联到目标宫位
  - 无 TypeScript 编译错误
- **验证**：`npx tsc --noEmit` 通过 + 浏览器手动查看飞星图
- **回滚**：git revert

---

### Step 5：接入 8 个孤儿组件（P2，4 小时）

#### 5a：3 个 home/ 图表组件接入 HomePage

- **动作**：修改 [HomePage.tsx](file:///C:/Users/10919/Desktop/AWKN-Lab/人生决策宗师/apps/AWKN-LABlife/app/src/pages/HomePage.tsx)
- **产出**：
  - 在 `MetaphysicsShowcase` 下方新增"玄学可视化"区块
  - 导入并渲染 `BaguaDiagram`、`WuxingDiagram`、`StarmapDiagram`
  - 3 列网格布局（大屏）或垂直堆叠（小屏）
- **验收**：首页出现 3 个图表，无编译错误
- **风险**：低，纯展示组件

#### 5b：ConfidenceRing 接入 ConsultPage 表单

- **动作**：修改 [ConsultPage.tsx](file:///C:/Users/10919/Desktop/AWKN-Lab/人生决策宗师/apps/AWKN-LABlife/app/src/pages/ConsultPage.tsx)
- **产出**：在表单提交前展示置信度环（根据用户填写完整度动态计算）
- **验收**：表单填写越完整，环值越高

#### 5c：4 个 conclusion/ 组件接入 ResultPage

- **动作**：修改 [ResultPage.tsx](file:///C:/Users/10919/Desktop/AWKN-Lab/人生决策宗师/apps/AWKN-LABlife/app/src/pages/ResultPage.tsx)
- **产出**：在结果页底部新增"结论风格"切换（传统/现代/中文/预览），根据 `ConclusionPreview` 渲染内容
- **验收**：切换结论风格后内容变化

---

### Step 6：growth.service 核心流程测试（P2，30 分钟）

- **动作**：新建 `growth/__tests__/growth.service.spec.ts`
- **产出**：至少 2 条测试
  - `getInviteStats` 3级链统计正确
  - `calculateReferralRewards` 积分计算正确（L1=5, L2=2, L3=1）
- **验收**：`npm test -- --testPathPattern="growth"` 通过
- **验证**：`npm test` 全量通过

---

### Step 7：清理 python-validator/ 审计临时文件（P2，5 分钟）

- **动作**：删除 `python-validator/` 下 4 个 `test_audit_*.py` 文件
- **产出**：审计文件已清理
- **验收**：`ls test_audit_*.py` 无结果
- **回滚**：git revert

---

## 四、执行顺序

```
Step 1 (sxtwl) ──┐
                  ├── 并行
Step 2 (ziwei测试) ┘
                  │
                  ▼
Step 3 (流盘Tab) ──┐
                    ├── 并行
Step 4 (飞星图) ───┘
                    │
                    ▼
Step 5 (孤儿组件) ──┐
                    ├── 并行
Step 6 (growth测试) ┘
                    │
                    ▼
Step 7 (清理审计文件)
```

## 五、最终验收

- [ ] `npx tsc --noEmit` 通过
- [ ] `npm run build` 通过
- [ ] `npm test` 全量通过（540+ 新增测试）
- [ ] HomePage 出现 3 个图表（八卦/五行/星图）
- [ ] ZiweiPage 出现流盘切换 Tab
- [ ] ZiweiPage 出现飞星图
- [ ] ConsultPage 表单出现置信度环
- [ ] ResultPage 结论区可切换风格
- [ ] `pip show sxtwl` 显示已安装

## 六、风险与约束

- **不改**：后端 API 接口、数据库 schema、路由结构
- **不改**：Python 排盘引擎代码（只安装依赖）
- **风险**：飞星图组件复杂度较高，如 2 小时超时则降级为纯文字表格
- **风险**：孤儿组件可能依赖未安装的库，接入前需先读组件 import 确认