# E73-E77 人生决策宗师项目质量提升复盘经验

> ⚠️ **编号冲突废弃**：E73-E77 与 awkn-agent 项目已有编号冲突，已重新编号为 E86-E91。
> 新文件：`.trae/documents/人生决策宗师-全流程深度复盘_E86-E91_2026-06-16.md`
> 废弃日期：2026-06-16

> 日期：2026-06-16
> 来源：人生决策宗师项目质量提升全流程深度复盘
> 项目路径：C:\Users\10919\Desktop\AWKN-Lab\人生决策宗师

---

## E73｜"改了代码≠功能通了"端到端验证门禁

- **教训**：spawn bug 存在数月未被发现，因为只验证了编译通过，没有验证功能是否生效。修代码是 1 行，但发现 bug 需要端到端验证。
- **反例**：`spawn()` 改为 `spawnSync()` 后只跑了 `tsc --noEmit`，没有实际调用 Python CLI 验证数据返回
- **触发词**：修复 bug / 改了代码 / 编译通过 / 功能不生效 / 降级 / fallback
- **验证次数**：1 | **跨场景**：是 | **置信度**：high
- **分流决策**：→ awkn-工程师 SKILL.md + 记忆 L2（改变"怎么验证"的规则）

## E74｜评分必须基于验证而非假设

- **教训**：部署就绪打了 50 分是因为"假设没有 Docker"，实际全都有。评分偏差 32 分（50→82），根因是没读文件就打分。
- **反例**：直接按"没有 CI/CD"假设打分，实际 .github/workflows/ci.yml 已存在
- **触发词**：评分 / 打分 / 评估 / 现状分析 / 质量评估
- **验证次数**：1 | **跨场景**：是 | **置信度**：high
- **分流决策**：→ awkn-复盘总结 SKILL.md + 记忆 L2（改变"怎么评估"的规则）

## E75｜前端页面≠空壳，需读代码再判断

- **教训**：3 个页面被标记为"空壳"，实际都有完整 UI 逻辑（317/296/210 行）。问题不是页面空，而是后端 API 未实现。
- **反例**：浏览器访问时只看到暗夜模式按钮就判断为"空壳"，没有读源码
- **触发词**：空壳 / 空页面 / 页面无内容 / 页面不完整
- **验证次数**：1 | **跨场景**：是 | **置信度**：high
- **分流决策**：→ 记忆 L2（执行偏好，不改规则）

## E76｜Prisma schema 变更后必须重新 generate

- **教训**：修改 schema.prisma 后 TypeScript 编译报类型不存在错误。需要运行 `npx prisma generate` 重新生成 Prisma Client 类型。
- **反例**：修改 schema 后直接 tsc --noEmit，14 个类型错误
- **触发词**：Prisma / schema 变更 / 类型不存在 / PrismaService / generate
- **验证次数**：1 | **跨场景**：是 | **置信度**：high
- **分流决策**：→ awkn-工程师 SKILL.md + 记忆 L2（改变"怎么做"的规则）

## E77｜LLM 动态内容不在代码库中

- **教训**：搜索"训练目标"、"伤病"等关键词在整个项目中无匹配。这些是 LLM 动态生成的纯文本回复。要控制样式需新增结构化消息类型。
- **反例**：花 30 分钟搜索代码库找关键词，结果不存在
- **触发词**：LLM 动态内容 / 样式问题 / 找不到关键词 / 动态渲染
- **验证次数**：1 | **跨场景**：是 | **置信度**：medium
- **分流决策**：→ 记忆 L1（项目特定事实）

---

## 进化方向评估

### 值得更新技能的（2 条）

1. **E73 → awkn-工程师**：新增"端到端验证门禁"规则 — 修复 bug 后必须验证功能生效，不能只验证编译通过
2. **E76 → awkn-工程师**：新增"Prisma schema 变更后必须 generate"规则

### 不值得更新技能的（3 条）

3. **E74**：评分偏差是复盘技能自身的问题，已在本次复盘中修正，不需要改 SKILL 规则
4. **E75**：执行偏好（读代码再判断），不是规则变更
5. **E77**：项目特定事实，不是通用规则

### 下次遇到类似情况，先做哪 3 件事？

1. **先读文件再判断**：不要基于假设打分或评估，先读关键文件确认事实
2. **端到端验证**：修复 bug 后不只验证编译，还要验证功能端到端生效
3. **区分"代码存在"和"功能生效"**：代码存在 ≠ 功能生效 ≠ 部署可用，三层验证缺一不可
