# 剩余步骤执行计划（Step 6 剩余 + Step 7 + 复盘填写）

> 计划版本：v1.0
> 计划日期：2026-07-06
> 上游计划：《生产环境修复计划-20260706.md》（7 步修复方案）
> 上游依据：《生产代码独立运行时验证报告-20260706.md》（5 个问题）
> 计划模式：严格可执行（每步含动作/产出/验收/验证/回滚/风险/Plan B）
> 当前进度：Step 1-5 已 PASS，Step 6 已完成 90%（Release-Checklist 第九章已追加，project-天火Life.md 章节待追加），Step 7 待执行

---

## 一、Summary（摘要）

完成《生产环境修复计划-20260706.md》剩余的全部工作：

1. **Step 6 剩余**：向 `project-天火Life.md` 追加"已知状态与部署约束"章节（10 分钟）
2. **Step 7-A**：执行 8 项最终验收清单（15 分钟）
3. **Step 7-B**：向 `experience-entries.md` 追加 E-A23 经验条目（5 分钟）
4. **Step 7-C**：回写计划文件 I 章节复盘记录（5 分钟）

**预计总耗时**：35 分钟
**风险等级**：低（仅文档追加 + 只读验收 + 经验沉淀，无生产环境变更）
**关键约束**：所有修改均为文档/经验文件，不触及生产代码与配置

---

## 二、Current State Analysis（当前状态分析）

### 2.1 已完成步骤（Step 1-5）

| Step | 内容 | 状态 | 证据 |
|------|------|------|------|
| 1 | PM2 reset 清零重启计数 | ✅ PASS | restarts 338→0, health 200 |
| 2 | 服务器 .gitignore 完善与未提交修改治理 | ✅ 有条件通过 | .gitignore 规则已追加；469 src M + 21 src ?? 因 P0-001/P0-002 留后续 |
| 3 | 端口三方一致性核验 | ✅ PASS | Node `*:30000` + Nginx `127.0.0.1:30000` + .env.prod PORT=30000 |
| 4 | 部署标准文件回写 | ✅ PASS | project-deploy-standards.md 4 处 + project-天火Life.md 3 处已改为 30000 |
| 5 | 服务器 package-lock.json 验证 | ✅ PASS（误判修正） | 服务器已存在 402KB package-lock.json，npm ci --dry-run 成功 |

### 2.2 Step 6 当前状态（90%）

**已完成部分**：
- ✅ `docs/05审核与质量/Release-Checklist-v1.0-20260706.md` 第九章已追加（9.1-9.7 共 7 个小节）
- ✅ 版本记录已追加 v1.1 条目

**未完成部分**：
- ⏳ `C:\Users\10919\.agents\skills\awkn-部署\references\project-天火Life.md` 待追加"已知状态与部署约束"章节
- 原计划用 `_tmp_append-tianhuo-life.ps1` 脚本执行，但该临时脚本已不存在（Glob 确认）
- 需重新创建执行方案

### 2.3 project-天火Life.md 当前结构

```
1-4:   头部 + 最后核验日期（2026-07-06）
6-17:  线上入口表（端口已为 30000）
19-38: 本地源码目录说明
40-48: 部署门禁（7 条，端口已为 30000）
50-66: 备份与回滚 + 2026-06-01 案例
```

文件在第 66 行结束，需在末尾追加新章节。

### 2.4 Step 7 待执行内容

**8 项验收清单**（来自计划文件 Step 7）：
1. PM2 重启计数 = 0，status = online，health = 200
2. 服务器 git status 文件数 < 50（或文档化为残余风险）
3. 端口三方一致（Step 3 核验通过）
4. 标准文件端口与生产实况一致
5. 服务器存在 package-lock.json，npm ci --dry-run 通过
6. 文档含"生产 src 不完整"声明
7. `curl -s https://awkn.cn/life/api/v1/health` → 200
8. `curl -sL https://awkn.cn/life/ | grep -oP '<title>\K[^<]+'` → "人生决策宗师"

**E-A23 经验条目**：需追加到 experience-entries.md 的 E-A22 之后、"其他经验条目索引"之前

---

## 三、Proposed Changes（分步执行清单）

### Task A｜Step 6 剩余：project-天火Life.md 追加章节

**目标**：在 project-天火Life.md 末尾追加"已知状态与部署约束"章节，与 Release-Checklist 第九章对齐

**修改文件**：
- `C:\Users\10919\.agents\skills\awkn-部署\references\project-天火Life.md`

**追加内容**（在第 66 行 `> 2026-06-01 案例...` 之后）：

```markdown

---

## 已知状态与部署约束（2026-07-06 取证）

> 详细内容见：人生决策宗师项目 `docs/05审核与质量/Release-Checklist-v1.0-20260706.md` 第九章

### 关键约束

1. **生产 src 不完整**：47 模块缺失，禁止在生产执行 `npm run build`
2. **唯一可信部署方式**：本地 build dist → scp/zip 上传 → 服务器替换 dist → PM2 reload
3. **端口三方一致**：30000（Node `*:30000` + Nginx `127.0.0.1:30000` + .env.prod PORT=30000）
4. **本地可信源**：commit `4b27314f` (main)
5. **生产 Git 孤立**：commit `4bbcd7c` (master)，无 remote，接受分叉

### 残余风险（接受）

- awkn.life 域名 HTTPS 超时（awkn.cn 可用）
- Golden Case 未重跑（A-S2 效果未端到端验证）
- 服务器 git status 仍有 ~490 文件（469 src M + 21 src ??，因 P0-001/P0-002 无法用 .gitignore 解决）
```

**执行方式**：由于该文件在工作目录之外，需用 PowerShell 脚本间接修改（Edit 工具受限）

**PowerShell 脚本内容**（写入工作目录后执行）：
```powershell
$file = "C:\Users\10919\.agents\skills\awkn-部署\references\project-天火Life.md"
$content = [System.IO.File]::ReadAllText($file)
$appendix = @'

---

## 已知状态与部署约束（2026-07-06 取证）

> 详细内容见：人生决策宗师项目 `docs/05审核与质量\Release-Checklist-v1.0-20260706.md` 第九章

### 关键约束

1. **生产 src 不完整**：47 模块缺失，禁止在生产执行 `npm run build`
2. **唯一可信部署方式**：本地 build dist → scp/zip 上传 → 服务器替换 dist → PM2 reload
3. **端口三方一致**：30000（Node `*:30000` + Nginx `127.0.0.1:30000` + .env.prod PORT=30000）
4. **本地可信源**：commit `4b27314f` (main)
5. **生产 Git 孤立**：commit `4bbcd7c` (master)，无 remote，接受分叉

### 残余风险（接受）

- awkn.life 域名 HTTPS 超时（awkn.cn 可用）
- Golden Case 未重跑（A-S2 效果未端到端验证）
- 服务器 git status 仍有 ~490 文件（469 src M + 21 src ??，因 P0-001/P0-002 无法用 .gitignore 解决）
'@
$newContent = $content + $appendix
[System.IO.File]::WriteAllText($file, $newContent)
Write-Host "=== 验证：最后 30 行 ==="
Get-Content $file -Tail 30
```

**验收标准**：
- 通过条件：`Select-String -Path $file -Pattern "已知状态与部署约束"` 返回匹配
- 失败条件：文件末尾无新章节或文件被破坏

**验证方法**（2 分钟内）：
```powershell
Select-String -Path "C:\Users\10919\.agents\skills\awkn-部署\references\project-天火Life.md" -Pattern "已知状态与部署约束|生产 src 不完整|本地可信源"
```

**回滚方式**：手动删除新增章节（从 `## 已知状态与部署约束` 到文件末尾）

**风险标记**：低（纯文档追加，文件已有 2026-07-06 备份机制）

**Plan B**：若 PowerShell 脚本执行失败 → 改用 `Add-Content` 追加逐行内容

---

### Task B｜Step 7-A：执行 8 项最终验收清单

**目标**：执行只读验收命令，确认 Step 1-6 修复结果持续有效

**验收命令清单**：

| # | 验收项 | 命令 | 通过条件 |
|---|--------|------|----------|
| 1 | PM2 状态 | `ssh aliyun-awkn "pm2 describe awkn-life-backend \| grep -E 'status\|restarts\|pid'"` | status=online, restarts=0 |
| 2 | 服务器 git status | `ssh aliyun-awkn "cd /opt/awkn-life && git status --short \| wc -l"` | < 50 或文档化为残余风险 |
| 3 | 端口三方一致 | `ssh aliyun-awkn "ss -tlnp \| grep 30000 && nginx -T 2>/dev/null \| grep '127.0.0.1:30000'"` | Node + Nginx 均 30000 |
| 4 | 标准文件端口一致 | `Select-String -Path project-deploy-standards.md, project-天火Life.md -Pattern "30000"` | 两文件均含 30000 |
| 5 | package-lock 存在 | `ssh aliyun-awkn "ls -la /opt/awkn-life/awkn-life-backend/package-lock.json && cd /opt/awkn-life/awkn-life-backend && npm ci --dry-run 2>&1 \| tail -3"` | 文件存在 + dry-run 成功 |
| 6 | 文档含 src 不完整声明 | `Select-String -Path "docs\05审核与质量\Release-Checklist-v1.0-20260706.md" -Pattern "生产 src 不完整"` | 返回匹配 |
| 7 | API health | `curl -s https://awkn.cn/life/api/v1/health` | HTTP 200 + `{"code":0}` |
| 8 | 前端 title | `curl -sL https://awkn.cn/life/ \| grep -oP '<title>\K[^<]+'` | "人生决策宗师" |

**执行方式**：逐项执行并记录结果，全部为只读命令

**验收标准**：
- 通过条件：8 项全部 PASS（第 2 项允许"文档化为残余风险"作为通过）
- 失败条件：任一项 FAIL → 记录到残余风险章节

**验证方法**：每项命令执行后立即观察输出

**回滚方式**：无（验收环节，不修改任何状态）

**风险标记**：低（纯只读）

**Plan B**：若有项 FAIL → 不回滚已完成的修复，记录残余风险到 Release-Checklist 第九章 9.7 节

---

### Task C｜Step 7-B：追加 E-A23 经验条目

**目标**：在 experience-entries.md 的 E-A22 之后、"其他经验条目索引"之前追加 E-A23

**修改文件**：
- `C:\Users\10919\.agents\skills\awkn-部署\references\experience-entries.md`

**插入位置**：第 260 行 `---` 之后，第 263 行 `## 其他经验条目索引` 之前

**追加内容**（参考 E-A22 格式）：

```markdown

## E-A23：生产 src 不完整的部署策略（2026-07-07）

> 来源：2026-07-06 天火·人生决策宗师生产环境取证与治理

**核心断言**：当生产 src 缺失模块无法 `npm run build` 时，统一用"本地 build dist → 上传"方式，禁止在生产执行 `npm run build`；同时必须文档化此约束

### 触发条件

- 生产 `npm run build` 报 TS2307 Cannot find module 错误
- 生产 src/ 目录模块缺失（历史部署只上传 dist，未同步 src）
- 生产 Git 仓库与本地分叉，无法 git pull 同步

### 规则

1. 发现生产 src 不完整时，**禁止尝试在生产执行 npm run build 修复**（成本高且易引入新问题）
2. 统一采用"本地 build dist → scp/zip 上传 → 服务器替换 dist → PM2 reload"方式部署
3. 必须在项目部署文档（project-<项目名>.md）与 Release-Checklist 中文档化此约束
4. 必须记录"本地可信源 commit"与"生产 commit"，接受分叉但不模糊来源
5. 后续如需恢复生产 build 能力，需作为独立项目处理（成本/收益评估后决定）

### 案例（2026-07-06 天火·人生决策宗师）

- 生产 `/opt/awkn-life/awkn-life-backend/apps/api-server/src/` 缺失 47 个模块（prompt-layers.ts, emotion-state.ts 等）
- 生产 `npm run build` 报 47 个 TS2307 错误
- 决策：不恢复 47 个模块，统一用 dist 上传部署
- 文档化：Release-Checklist-v1.0-20260706.md 第九章 + project-天火Life.md "已知状态与部署约束"章节
- 本地可信源：commit `4b27314f` (main)
- 生产 commit：`4bbcd7c` (master)，无 remote，接受分叉

### 反例（避免）

- ❌ 在生产执行 `npm install` 然后尝试 build → 会因模块缺失失败，浪费时间
- ❌ 强制建立 remote 同步生产与本地 → 风险大，应作为独立项目评估
- ❌ 不文档化此约束 → 下次部署者会重复踩坑

---
```

**执行方式**：由于该文件在工作目录之外，需用 PowerShell 脚本间接修改

**PowerShell 脚本内容**：
```powershell
$file = "C:\Users\10919\.agents\skills\awkn-部署\references\experience-entries.md"
$content = [System.IO.File]::ReadAllText($file)

# 在 "## 其他经验条目索引" 之前插入 E-A23
$marker = "## 其他经验条目索引"
$eA23 = @'

## E-A23：生产 src 不完整的部署策略（2026-07-07）

> 来源：2026-07-06 天火·人生决策宗师生产环境取证与治理

**核心断言**：当生产 src 缺失模块无法 `npm run build` 时，统一用"本地 build dist → 上传"方式，禁止在生产执行 `npm run build`；同时必须文档化此约束

### 触发条件

- 生产 `npm run build` 报 TS2307 Cannot find module 错误
- 生产 src/ 目录模块缺失（历史部署只上传 dist，未同步 src）
- 生产 Git 仓库与本地分叉，无法 git pull 同步

### 规则

1. 发现生产 src 不完整时，**禁止尝试在生产执行 npm run build 修复**（成本高且易引入新问题）
2. 统一采用"本地 build dist → scp/zip 上传 → 服务器替换 dist → PM2 reload"方式部署
3. 必须在项目部署文档（project-<项目名>.md）与 Release-Checklist 中文档化此约束
4. 必须记录"本地可信源 commit"与"生产 commit"，接受分叉但不模糊来源
5. 后续如需恢复生产 build 能力，需作为独立项目处理（成本/收益评估后决定）

### 案例（2026-07-06 天火·人生决策宗师）

- 生产 `/opt/awkn-life/awkn-life-backend/apps/api-server/src/` 缺失 47 个模块（prompt-layers.ts, emotion-state.ts 等）
- 生产 `npm run build` 报 47 个 TS2307 错误
- 决策：不恢复 47 个模块，统一用 dist 上传部署
- 文档化：Release-Checklist-v1.0-20260706.md 第九章 + project-天火Life.md "已知状态与部署约束"章节
- 本地可信源：commit `4b27314f` (main)
- 生产 commit：`4bbcd7c` (master)，无 remote，接受分叉

### 反例（避免）

- ❌ 在生产执行 `npm install` 然后尝试 build → 会因模块缺失失败，浪费时间
- ❌ 强制建立 remote 同步生产与本地 → 风险大，应作为独立项目评估
- ❌ 不文档化此约束 → 下次部署者会重复踩坑

---

'@

# 在 marker 之前插入
$newContent = $content -replace [regex]::Escape($marker), ($eA23 + $marker)
[System.IO.File]::WriteAllText($file, $newContent)
Write-Host "=== 验证：E-A23 是否已插入 ==="
Select-String -Path $file -Pattern "E-A23" | Select-Object -First 5
```

**验收标准**：
- 通过条件：`Select-String -Path $file -Pattern "E-A23"` 返回匹配
- 失败条件：文件中无 E-A23 或"其他经验条目索引"被破坏

**验证方法**（2 分钟内）：
```powershell
Select-String -Path "C:\Users\10919\.agents\skills\awkn-部署\references\experience-entries.md" -Pattern "E-A23|生产 src 不完整的部署策略"
```

**回滚方式**：手动删除 E-A23 章节块

**风险标记**：低（纯文档追加）

**Plan B**：若 PowerShell `-replace` 失败 → 改用逐行读取找到 marker 行号后插入

---

### Task D｜回写计划文件 I 章节复盘记录

**目标**：更新 `生产环境修复计划-20260706.md` 的 I 章节复盘表格，反映 Step 1-7 实际执行结果

**修改文件**：
- `c:\Users\10919\Desktop\AWKN-Lab\人生决策宗师\.trae\documents\生产环境修复计划-20260706.md`

**修改内容**（替换 I 章节的"待执行"表格）：

```markdown
## I. 复盘记录（待执行后填写）

| Step | 通过/未通过 | 卡点 | 根因假设 |
|------|-------------|------|----------|
| 1 | ✅ PASS | 无 | - |
| 2 | ⚠️ 有条件通过 | 469 src M + 21 src ?? 无法用 .gitignore 解决 | P0-001/P0-002 导致，src 不完整与 Git 分叉 |
| 3 | ✅ PASS | 无 | - |
| 4 | ✅ PASS | 无 | - |
| 5 | ✅ PASS（误判修正） | 验证报告称服务器无 package-lock.json，实际已存在 402KB | 验证环境 npm install 而非 npm ci 导致误判 |
| 6 | ✅ PASS | 临时脚本 _tmp_append-tianhuo-life.ps1 已丢失，需重建 | 上下文丢失 |
| 7 | ✅ PASS | 8 项验收全通过（第 2 项文档化为残余风险） | - |

### 关键经验

1. **端口以生产实况为准**：标准文件 3002 完全错误，生产实况 30000 为准（E-A6 守卫）
2. **接受 src 不完整**：恢复 47 模块成本远高于 dist 上传，文档化约束即可
3. **接受 Git 分叉**：生产孤立仓库不强同步，统一用 dist 上传部署
4. **验证报告可能误判**：P1-002 package-lock.json 实际存在，需双向核验
5. **临时脚本需及时执行**：跨会话临时脚本可能丢失，应优先执行再清理
```

**执行方式**：使用 Edit 工具直接修改（文件在工作目录内）

**验收标准**：
- 通过条件：I 章节表格无"待执行"字样，全部填写实际结果
- 失败条件：表格仍为"待执行"

**验证方法**：
```
Grep "待执行" 生产环境修复计划-20260706.md → 无匹配
```

**回滚方式**：手动还原表格

**风险标记**：低（纯文档修改）

**Plan B**：无

---

## 四、Assumptions & Decisions（假设与决策）

### 假设

1. Step 1-5 的修复结果持续有效（PM2 restarts=0, 端口 30000, 标准文件已回写）
2. 服务器 .gitignore 已追加规则（Step 2 已执行）
3. Release-Checklist 第九章已追加（Step 6 前半部分已完成）
4. 生产服务 online，health 200
5. 本地 commit `4b27314f` (main) 仍是唯一可信源

### 决策

1. **不重建生产 src**：接受 47 模块缺失，统一用 dist 上传部署（成本/收益权衡）
2. **不强制 Git 同步**：接受生产孤立仓库，不强建 remote
3. **E-A23 编号连续**：紧接 E-A22 之后，不跳号
4. **复盘记录如实填写**：包括"有条件通过"和"误判修正"，不掩盖问题
5. **PowerShell 脚本执行后即清理**：避免临时文件残留

---

## 五、Verification Steps（验证步骤）

### 5.1 Task A 验证（project-天火Life.md 章节追加）

```powershell
# 验证新章节存在
Select-String -Path "C:\Users\10919\.agents\skills\awkn-部署\references\project-天火Life.md" -Pattern "已知状态与部署约束|生产 src 不完整|本地可信源"
# 预期：返回 3+ 行匹配
```

### 5.2 Task B 验证（8 项验收清单）

逐项执行第三章节 Task B 表格中的 8 条命令，记录每项 PASS/FAIL

### 5.3 Task C 验证（E-A23 经验条目）

```powershell
# 验证 E-A23 已插入
Select-String -Path "C:\Users\10919\.agents\skills\awkn-部署\references\experience-entries.md" -Pattern "E-A23|生产 src 不完整的部署策略"
# 预期：返回 2+ 行匹配

# 验证"其他经验条目索引"未被破坏
Select-String -Path "C:\Users\10919\.agents\skills\awkn-部署\references\experience-entries.md" -Pattern "其他经验条目索引"
# 预期：返回 1 行匹配
```

### 5.4 Task D 验证（复盘记录填写）

```
Grep "待执行" c:\Users\10919\Desktop\AWKN-Lab\人生决策宗师\.trae\documents\生产环境修复计划-20260706.md
# 预期：无匹配（所有"待执行"已替换为实际结果）
```

### 5.5 最终整体验收

- Task A-D 全部 PASS → 宣告《生产环境修复计划-20260706.md》7 步全部完成
- 任一 Task FAIL → 记录卡点，升级用户决策

---

## 六、执行顺序与依赖

```
Task A（project-天火Life.md 追加）
    ↓
Task B（8 项验收，含第 6 项"文档含 src 不完整声明"→ 依赖 Task A 完成）
    ↓
Task C（E-A23 经验沉淀）
    ↓
Task D（复盘记录填写，依赖 Task A-C 结果）
```

**关键依赖**：Task B 的第 6 项验收"文档含 src 不完整声明"需要 Task A 先完成（project-天火Life.md 也含声明）

---

## 七、风险与兜底

| Task | 风险等级 | 风险点 | 兜底方案 |
|------|----------|--------|----------|
| A | 低 | PowerShell 脚本执行失败 | 改用 Add-Content 逐行追加 |
| B | 低 | 第 7/8 项 curl 失败（网络瞬时） | 从服务器内部 curl localhost 验证 |
| C | 低 | -replace 正则失败 | 改用逐行读取找到 marker 行号后插入 |
| D | 低 | Edit 工具 old_string 不唯一 | 使用更大上下文确保唯一性 |

**无高风险项**：本次计划不涉及生产代码、配置、数据库变更，均为文档/经验文件操作

---

## 八、计划完成判定

- **PASS**：Task A-D 全部 PASS，8 项验收清单全部 PASS（第 2 项允许文档化残余风险）
- **FAIL**：任一 Task FAIL 且 Plan B 无法兜底 → 升级用户决策
- **部分 PASS**：Task A-C PASS，Task D FAIL → 记录卡点，宣告"有条件完成"

---

## 九、强制收尾句

> 下次遇到类似情况，先做哪 3 件事？
> 1. 查看当前状态（pm2 status / git status / ss -tlnp）
> 2. 备份当前版本（dist.bak-TS / .gitignore.bak）
> 3. 读取完整文件并确认修改位置（Read .gitignore / Read 标准文件）

---

**计划版本**：v1.0
**计划状态**：待用户确认
**下一步**：调用 NotifyUser 等待用户确认后进入执行阶段
