# 人生决策宗师 — 上线前修复与开发落地计划

> **基于文档**：`docs/pre-launch-analysis.md`（2026-06-18 深度批评性分析报告）
> **计划版本**：v1
> **计划日期**：2026-06-18
> **执行范围**：P0 + P1 + P2 全量（16 项任务）
> **当前进度**：P0-1 已完成（ResultPage 渐进抽取 3 块独立逻辑）

---

## 一、计划摘要

### 1.1 目标
基于 `pre-launch-analysis.md` 识别的 16 项问题（P0×5 + P1×6 + P2×5），按优先级顺序修复，确保上线前消除阻断性问题（P0），上线后 1-2 周内解决体验痛点（P1），持续迭代优化项（P2）。

### 1.2 已确认决策
| 决策项 | 选择 | 理由 |
|--------|------|------|
| 执行范围 | P0 + P1 + P2 全量 | 用户明确要求全量执行 |
| ResultPage 拆分策略 | 渐进抽取（3 块独立逻辑） | 风险可控，每步可验收 |
| sessionStorage 持久化方案 | URL params + Zustand 双轨 | record_id 走 URL，完整数据走 Zustand persist |

### 1.3 执行顺序（按依赖关系）
1. **P0-1** ✅ 已完成 → **P0-4**（同一文件链路：localEngine.ts）
2. **P0-2**（独立，影响多文件：sessionStorage 迁移）
3. **P0-3 + P0-5**（可并行：入口扩展 + API 参数扩展）
4. **P1-6 + P1-5**（独立，简单清理）
5. **P1-1**（依赖 P0-3：首页重构）
6. **P1-2**（依赖 P0-2：取名结果闭环）
7. **P1-3**（依赖 P0-1：K 线加载优化）
8. **P1-4**（独立：意图路由升级）
9. **P2-1 ~ P2-5**（持续迭代）

---

## 二、当前状态分析

### 2.1 已完成工作
- **类型错误修复**：254 → 0（tsc 0 errors, vitest 71/71）
- **类型债务管理机制**：4 层防护（本地检测 + 提交门禁 + 趋势追踪 + 偿还流程）
- **P0-1 ResultPage 渐进抽取**（commit `f4faa3fa`）：
  - `callRealEngine` → `src/lib/localEngine.ts`（110 行）
  - K 线轮询 → `src/hooks/useKlinePolling.ts`（130 行）
  - 付费墙 Modal → `src/components/result/PaywallModal.tsx`（100 行）
  - ResultPage 行数：1525 → 1192（减少 21.8%）

### 2.2 待解决问题（按分析报告优先级）

#### P0 — 阻断性问题（上线前必须解决）
| # | 问题 | 当前状态 | 影响文件 |
|---|------|----------|----------|
| P0-1 | ResultPage 上帝组件 | ✅ 已完成（渐进抽取 3 块） | localEngine.ts, useKlinePolling.ts, PaywallModal.tsx |
| P0-2 | sessionStorage 传数据 | ⏳ 待办 | 8 个文件使用 `awkn_consult_data` |
| P0-3 | 6 个孤岛路由无首页入口 | ⏳ 待办 | MetaphysicsShowcase.tsx（当前仅 3 个入口） |
| P0-4 | Mock 模式 ziping 返回空壳 | ⏳ 待办 | localEngine.ts（ziping 分支返回 `ZIPING_UNAVAILABLE_`） |
| P0-5 | 无知识库调用证据 | ⏳ 待办 | consult.ts（analyze 接口无 knowledge_base 参数） |

#### P1 — 体验痛点（上线后 1-2 周内解决）
| # | 问题 | 影响文件 |
|---|------|----------|
| P1-1 | 首页信息层级混乱（6 层堆叠） | HomePage.tsx |
| P1-2 | 取名结果无"查看完整报告"路径 | FrontdeskChat.tsx |
| P1-3 | K 线加载慢 + 轮询机制 | useKlinePolling.ts |
| P1-4 | 意图路由纯关键词匹配 | intentRouter.ts |
| P1-5 | 付费墙体验生硬（window.alert） | MembershipPage.tsx |
| P1-6 | 硬编码图片 URL | feature-flags.config.ts |

#### P2 — 优化项（持续迭代）
| # | 问题 | 影响文件 |
|---|------|----------|
| P2-1 | BottomNav 未使用路由高亮 | BottomNav.tsx |
| P2-2 | 暗色模式星空背景外部图片 URL | 主题配置 |
| P2-3 | ComponentPreview 页面未移除 | App.tsx, ComponentPreview.tsx |
| P2-4 | 无离线/PWA 支持 | vite.config, manifest |
| P2-5 | Admin 检查逻辑散落 | 多个组件 |

---

## 三、详细执行计划

### P0-4：Mock ziping 空壳修复

**目标**：Mock 模式下 ziping 返回结构完整的模拟数据，而非空壳，确保开发环境与生产行为一致。

**文件**：`apps/AWKN-LABlife/app/src/lib/localEngine.ts`

**当前问题**（L94-107）：
```typescript
if (routeType === 'ziping') {
  return {
    route_type: 'ziping',
    summary_line: t('resultPage.localFallback.generic.summary'),
    summary_body: t('resultPage.localFallback.generic.body'),
    risks: [t('resultPage.localFallback.generic.risk1')],
    actions: [t('resultPage.localFallback.generic.action1')],
    time_window: t('resultPage.localFallback.generic.timeWindow'),
    evidence_fold: '',
    paywall_modules: [],  // ← 空，导致 VIP 模块不显示
    record_id: `ZIPING_UNAVAILABLE_${Date.now().toString(36).slice(2, 10)}`,  // ← "UNAVAILABLE" 误导
  };
}
```

**修复方案**：
1. 保留注释"子平命理不在前端本地排盘"的设计决策
2. 返回结构完整的 mock 数据，包含所有 UnifiedResult 必需字段：
   - `summary_line` / `summary_body` / `risks` / `actions` / `time_window`
   - `evidence_fold` / `paywall_modules` / `record_id` / `calc_result`
3. `record_id` 前缀改为 `ZIPING_MOCK_`（而非 `ZIPING_UNAVAILABLE_`）
4. `paywall_modules` 填充 `['breakthrough', 'morning', 'kline']`（与 liuren 一致）
5. 添加 i18n key：`resultPage.mockFallback.ziping.*`（summary/body/risk1-4/action1-4/timeWindow）
6. 提供 `calc_result` 基础结构（birth_date/gender/wuxing 占位）

**验收标准**：
- [ ] Mock 模式下 ziping 返回结构完整数据
- [ ] ResultPage 能正常渲染所有模块（summary/risks/actions/paywall）
- [ ] `tsc -p tsconfig.app.json --noEmit` 0 errors
- [ ] `npm run test:run` 71/71 通过
- [ ] record_id 前缀为 `ZIPING_MOCK_`

**风险**：低（仅影响 Mock 模式，不触及生产逻辑）

---

### P0-2：sessionStorage → URL params + Zustand 双轨持久化

**目标**：消除 sessionStorage 依赖，确保用户刷新/切后台/手机杀进程后数据不丢失。

**影响文件**（8 个，均使用 `awkn_consult_data`）：
1. `src/pages/ResultPage.tsx`（读取 + 写入，6 处）
2. `src/pages/HomePage.tsx`（写入，1 处）
3. `src/pages/InfoPage.tsx`（读取 + 写入，2 处）
4. `src/pages/ConsultPage.tsx`（读取 + 写入，4 处）
5. `src/pages/KlineIntroPage.tsx`（读取 + 写入，2 处）
6. `src/components/frontdesk/FrontdeskChat.tsx`（写入，3 处）
7. `src/components/naming/BrandStrategy.tsx`（读取，1 处）
8. `src/store/consultStore.ts`（写入，1 处）

**方案**：URL params + Zustand persist 双轨
- **record_id** 走 URL params：`/result?id=xxx&type=ziping`
- **完整咨询数据**走 Zustand persist（localStorage）：新建 `consultDraftStore`
- 保留 sessionStorage 作为向后兼容的读取兜底（读取时优先 URL → Zustand → sessionStorage）

**执行步骤**：

#### Step 1：新建 consultDraftStore
**文件**：`src/store/consultDraftStore.ts`（新建）
- 使用 Zustand `persist` 中间件，存储到 localStorage
- 状态结构：
  ```typescript
  interface ConsultDraft {
    route_type?: string;
    question?: string;
    birth_date?: string;
    birth_hour?: number;
    birth_minute?: number;
    city?: string;
    gender?: string;
    ask_time?: string;
    namingType?: string;
    originalName?: string;
    sourceEntry?: string;
    createdAt: string;  // ISO timestamp，用于过期判断
  }
  interface ConsultDraftState {
    draft: ConsultDraft | null;
    setDraft: (data: ConsultDraft) => void;
    clearDraft: () => void;
    getDraft: () => ConsultDraft | null;
  }
  ```
- 过期策略：24 小时未使用自动清除

#### Step 2：创建迁移工具函数
**文件**：`src/lib/consultDataMigration.ts`（新建）
- `saveConsultData(data)`：同时写入 Zustand 和 sessionStorage（向后兼容）
- `loadConsultData()`：优先级 URL → Zustand → sessionStorage
- `clearConsultData()`：清除所有存储

#### Step 3：替换 8 个文件中的 sessionStorage 调用
- 将 `sessionStorage.setItem('awkn_consult_data', ...)` 替换为 `saveConsultData(...)`
- 将 `sessionStorage.getItem('awkn_consult_data')` 替换为 `loadConsultData()`
- ResultPage 的 `loadResult` 逻辑调整：URL record_id 优先 → Zustand draft → sessionStorage 兜底

#### Step 4：URL params 增强
- HomePage/ConsultPage/InfoPage 提交后跳转：`/result?id=${recordId}&type=${routeType}`
- ResultPage 优先从 URL 读取 record_id（已有逻辑，L225-228），补充 type 参数消费

**验收标准**：
- [ ] 刷新 ResultPage 数据不丢失（从 Zustand 恢复）
- [ ] URL 带 record_id 时优先走 API 加载
- [ ] 8 个文件不再直接调用 `sessionStorage.getItem/setItem('awkn_consult_data')`
- [ ] `tsc` 0 errors，`vitest` 71/71 通过
- [ ] 新增 `consultDraftStore.test.ts` 覆盖 setDraft/getDraft/clearDraft/过期逻辑

**风险**：中（影响多文件，需逐步替换并测试）
**回滚方式**：保留 sessionStorage 兜底读取，出问题可立即回退到旧逻辑

---

### P0-3：暴露 6 个孤岛路由入口

**目标**：在首页 MetaphysicsShowcase 区域添加紫微/铜鉴/星图/潮汐/妙算/数秘元 6 个模块入口卡片。

**文件**：`src/components/home/MetaphysicsShowcase.tsx`

**当前状态**：CARDS 数组仅 3 个入口（starmap/wuxing/bagua → K线/取名/问事）

**修复方案**：
1. 扩展 CARDS 数组，新增 6 个入口：
   - `ziwei` → `/ziwei`（紫微斗数，使用 StarmapDiagram 或新建 ZiweiDiagram）
   - `tongjian` → `/tongjian`（铜鉴，新建 TongjianDiagram）
   - `xingtu` → `/xingtu`（星图，复用 StarmapDiagram 变体）
   - `tide` → `/tide`（人生潮汐，新建 TideDiagram）
   - `miaosuan` → `/miaosuan`（妙算，新建 MiaosuanDiagram）
   - `shumiyuan` → `/shumiyuan`（数秘元，新建 ShumiyuanDiagram）
2. 布局调整：3 列网格 → 响应式网格（移动端 2 列，桌面端 3-4 列）
3. 添加 i18n key：`home.metaphysicsShowcase.{ziwei|tongjian|xingtu|tide|miaosuan|shumiyuan}.{label|featureTitle|featureDesc|cta}`
4. trackEvent 上报：`metaphysics_${trackKey}_click`

**验收标准**：
- [ ] 首页显示 9 个入口卡片（3 原有 + 6 新增）
- [ ] 每个卡片点击跳转到对应路由
- [ ] 中英文 i18n 完整
- [ ] `tsc` 0 errors，`vitest` 71/71 通过
- [ ] 移动端布局正常（2 列），桌面端正常（3-4 列）

**风险**：低（纯 UI 扩展，不触及核心逻辑）

---

### P0-5：知识库调用链路打通

**目标**：前端传递知识库检索参数，为后端 RAG 提供入口，并在 ResultPage 展示引用来源。

**文件**：
1. `src/api/consult.ts`（扩展 analyze 参数）
2. `src/components/result/resultTypes.ts`（扩展 UnifiedResult）
3. `src/pages/ResultPage.tsx`（展示引用来源）
4. `src/components/result/EvidenceFold.tsx`（新建或扩展现有，展示引用）

**当前状态**：`consultApi.analyze` 参数无 `knowledge_base_id` / `retrieval_context` / `context_source`

**修复方案**：

#### Step 1：扩展 analyze 接口参数
```typescript
analyze: async (data: {
  routeType?: 'ziping' | 'liuren' | 'ziwei';
  question: string;
  birthDate?: string;
  birthTime?: string;
  birthPlace?: string;
  gender?: 'male' | 'female';
  askTime?: string;
  askLocation?: string;
  lang?: string;
  // 新增知识库参数
  knowledge_base_id?: string;      // 知识库 ID（可选，后端默认）
  retrieval_top_k?: number;        // 检索 top-k（默认 5）
  context_source?: 'rag' | 'prompt' | 'auto';  // 上下文来源
}): Promise<ResultResponse>
```

#### Step 2：扩展 UnifiedResult 类型
```typescript
interface UnifiedResult {
  // ... 现有字段
  citations?: Citation[];  // 新增：引用来源
}

interface Citation {
  id: string;
  source: string;       // 来源名称
  content: string;      // 引用内容摘要
  relevance: number;    // 相关度 0-1
  url?: string;         // 原文链接（可选）
}
```

#### Step 3：ResultPage 展示引用来源
- 在 `evidence_fold` 区域下方新增"引用来源"区块
- 仅当 `citations` 非空时显示
- 每条引用显示：来源名称 + 内容摘要 + 相关度进度条

#### Step 4：环境变量配置
- `.env.example` 添加 `VITE_DEFAULT_KB_ID`（默认知识库 ID）
- `src/config/feature-flags.config.ts` 添加 `rag_enabled` 开关

**验收标准**：
- [ ] analyze 接口支持 knowledge_base_id 等参数
- [ ] UnifiedResult 包含 citations 字段
- [ ] ResultPage 能展示引用来源（有数据时显示，无数据时隐藏）
- [ ] Feature Flag `rag_enabled` 控制是否启用
- [ ] `tsc` 0 errors，`vitest` 71/71 通过

**风险**：中（依赖后端支持，前端先做接口预留）
**回滚方式**：Feature Flag 关闭即回退到纯 Prompt 模式

---

### P1-6：硬编码图片 URL 清理

**目标**：清理 `avatar_replacement_enabled` Feature Flag 中硬编码的 Googleusercontent 图片 URL。

**文件**：`src/config/feature-flags.config.ts`

**修复方案**：
1. 将硬编码 URL 迁移到环境变量 `VITE_AVATAR_FALLBACK_URL`
2. 提供本地默认头像资源（`public/images/default-avatar.svg`）
3. Feature Flag 配置中使用 `import.meta.env.VITE_AVATAR_FALLBACK_URL || '/images/default-avatar.svg'`

**验收标准**：
- [ ] 无硬编码 `googleusercontent.com` URL
- [ ] 默认头像本地可访问
- [ ] `tsc` 0 errors

---

### P1-5：付费墙体验统一

**目标**：移除 `window.alert`，统一使用 toast/modal 通知。

**文件**：`src/pages/MembershipPage.tsx` 及其他使用 `setPaymentMockSuccess` 的位置

**修复方案**：
1. 搜索所有 `window.alert` 使用点
2. 替换为统一的 Toast 组件（复用现有 `Toast` 或新建）
3. 付费成功/失败均走 toast 通知

**验收标准**：
- [ ] 无 `window.alert` 调用（搜索 `window.alert` 返回 0 结果）
- [ ] 付费成功显示 toast 通知
- [ ] `tsc` 0 errors

---

### P1-1：首页信息层级重构

**目标**：将首页 6 层内容堆叠压缩到 3 屏以内。

**文件**：`src/pages/HomePage.tsx`

**依赖**：P0-3（入口扩展完成后，统一规划布局）

**修复方案**：
1. **Screen 1**：Hero + 核心入口（3 个大卡片：断事/取名/命运K线）
2. **Screen 2**：场景化入口（CriticalMoments，保留）+ 新增 6 个模块入口（P0-3 扩展）
3. **Screen 3**：引擎能力 + FAQ（折叠式，默认收起）
4. 移除 `AnimatedText` 独立层（合并到 Hero）
5. `PersonalizedRecommendations` 根据用户状态条件渲染

**验收标准**：
- [ ] 首页主要内容 3 屏内可见
- [ ] 核心入口（断事/取名/K线）首屏可见
- [ ] `tsc` 0 errors，`vitest` 71/71 通过

---

### P1-2：取名结果闭环

**目标**：FrontdeskChat 取名结果增加"查看完整分析报告"跳转。

**文件**：`src/components/frontdesk/FrontdeskChat.tsx`

**依赖**：P0-2（持久化完成后，跳转带 record_id）

**修复方案**：
1. 取名结果 Top3 展示后，新增"查看完整报告"按钮
2. 点击后调用 `saveConsultation` 保存记录，获取 recordId
3. 跳转 `/result?id=${recordId}&type=quming`
4. ResultPage 识别 quming 类型，渲染完整报告视图

**验收标准**：
- [ ] 取名结果有"查看完整报告"按钮
- [ ] 点击跳转到 ResultPage 且数据完整
- [ ] `tsc` 0 errors

---

### P1-3：K 线加载优化

**目标**：减少轮询依赖，增加骨架屏。

**文件**：`src/hooks/useKlinePolling.ts`

**依赖**：P0-1（已完成的 hook 抽取）

**修复方案**：
1. WebSocket 优先：监听 `kline_ready` 事件，收到即停止轮询
2. 骨架屏：加载中显示 K 线骨架（复用现有 Skeleton 组件或新建）
3. 轮询作为 WebSocket 失败的降级方案
4. 超时时间从 120s 调整为 90s（WebSocket 模式下可更快失败）

**验收标准**：
- [ ] WebSocket 连接时优先使用事件推送
- [ ] 加载中显示骨架屏
- [ ] 轮询作为降级方案正常工作
- [ ] `tsc` 0 errors

---

### P1-4：意图路由升级

**目标**：将 intentRouter 从纯关键词匹配升级为调用后端 `/consult/route` API。

**文件**：`src/lib/intentRouter.ts`

**修复方案**：
1. 保留本地关键词匹配作为降级方案（离线/API 失败时使用）
2. 新增 `routeIntentAsync` 函数，调用 `consultApi.route`
3. HomePage/ConsultPage 优先调用异步路由，失败则降级到本地
4. 添加 Feature Flag `ai_routing_enabled` 控制

**验收标准**：
- [ ] AI 路由启用时调用后端 API
- [ ] API 失败时降级到本地关键词匹配
- [ ] Feature Flag 控制开关
- [ ] `tsc` 0 errors

---

### P2-1：BottomNav 路由高亮

**文件**：`src/components/BottomNav.tsx`
- 使用 `useLocation` 获取当前路径，高亮匹配的导航项

### P2-2：暗色模式星空背景本地化

- 将外部图片 URL 替换为本地资源或 CSS 渐变

### P2-3：移除 ComponentPreview

**文件**：`src/App.tsx`, `src/pages/ComponentPreview.tsx`
- 删除 `/preview` 路由和 ComponentPreview 组件

### P2-4：PWA 支持

- 添加 `vite-plugin-pwa`
- 配置 manifest 和 service worker
- 缓存大六壬计算结果

### P2-5：Admin 检查逻辑统一

- 抽取 `checkIsAdmin` 到 `src/lib/admin.ts`
- 各组件统一引用

---

## 四、假设与决策

### 4.1 关键假设
1. 后端 `/consult/analyze` 接口已支持或即将支持知识库检索参数（P0-5）
2. 后端 `/consult/route` 接口已实现 AI 意图路由（P1-4）
3. WebSocket 服务端支持 `kline_ready` 事件推送（P1-3）
4. 现有 71 个单元测试覆盖核心逻辑，可作为回归基准

### 4.2 关键决策
1. **P0-2 保留 sessionStorage 兜底**：迁移期间双写，确保平滑过渡，降低风险
2. **P0-5 前端先做接口预留**：即使后端暂未实现 RAG，前端接口先扩展，通过 Feature Flag 控制
3. **P1-4 保留本地路由降级**：AI 路由失败时不能阻断用户流程，必须降级
4. **每步完成后立即提交**：遵循"小步修改 + 验证"原则，每个 P 任务完成后 commit

---

## 五、验证策略

### 5.1 每步验证（强制）
每个任务完成后必须通过：
```bash
cd apps/AWKN-LABlife/app
npm run typecheck   # tsc 0 errors
npm run test:run    # vitest 71/71 通过
```

### 5.2 集成验证
P0 全部完成后：
- 手动验证首页 → 咨询 → 结果页完整流程
- 验证刷新 ResultPage 数据不丢失
- 验证 6 个新入口可达
- 验证 Mock 模式 ziping 完整渲染

### 5.3 回归验证
- 类型债务管理机制自动拦截新错误
- pre-commit hook 阻止类型错误提交
- baseline.log 追踪错误数趋势

---

## 六、风险与回滚

### 6.1 风险矩阵
| 任务 | 风险等级 | 主要风险 | 回滚方式 |
|------|----------|----------|----------|
| P0-4 | 低 | Mock 数据结构不匹配 | git revert 单文件 |
| P0-2 | 中 | 多文件迁移遗漏 | 保留 sessionStorage 兜底 |
| P0-3 | 低 | 纯 UI 扩展 | git revert |
| P0-5 | 中 | 后端未支持 | Feature Flag 关闭 |
| P1-* | 低-中 | 体验优化 | 逐项回滚 |

### 6.2 回滚原则
- 每个任务独立 commit，可单独 revert
- P0-2 保留 sessionStorage 读取兜底，出问题可立即回退
- P0-5/P1-4 通过 Feature Flag 控制，可随时关闭

---

## 七、执行进度跟踪

| 任务 | 状态 | Commit | 验证结果 |
|------|------|--------|----------|
| P0-1 ResultPage 渐进抽取 | ✅ 完成 | `f4faa3fa` | tsc 0, vitest 71/71 |
| P0-4 Mock ziping 修复 | ⏳ 待办 | - | - |
| P0-2 sessionStorage 迁移 | ⏳ 待办 | - | - |
| P0-3 孤岛路由入口 | ⏳ 待办 | - | - |
| P0-5 知识库调用链路 | ⏳ 待办 | - | - |
| P1-6 硬编码 URL 清理 | ⏳ 待办 | - | - |
| P1-5 付费墙体验统一 | ⏳ 待办 | - | - |
| P1-1 首页信息层级重构 | ⏳ 待办 | - | - |
| P1-2 取名结果闭环 | ⏳ 待办 | - | - |
| P1-3 K 线加载优化 | ⏳ 待办 | - | - |
| P1-4 意图路由升级 | ⏳ 待办 | - | - |
| P2-1 BottomNav 高亮 | ⏳ 待办 | - | - |
| P2-2 星空背景本地化 | ⏳ 待办 | - | - |
| P2-3 移除 ComponentPreview | ⏳ 待办 | - | - |
| P2-4 PWA 支持 | ⏳ 待办 | - | - |
| P2-5 Admin 逻辑统一 | ⏳ 待办 | - | - |

---

## 八、下一步行动

立即开始执行 **P0-4：Mock ziping 空壳修复**（风险最低，与 P0-1 同一文件链路）。
