# 项目质量提升计划 — 从 71.5 到 90+

> **日期**：2026-06-16
> **当前真实评分**：71.5/100
> **目标评分**：90/100
> **核心策略**：按 ROI 排序，先做验证类任务（低风险高回报），再做修复类任务

---

## 一、当前状态分析（修正后）

### 1.1 评分修正依据

| 维度 | 原评分 | 修正评分 | 修正理由 |
|------|--------|----------|----------|
| 算法引擎 | 85 | **75** | I18N 0%、真太阳时 0%、运曜/流曜 0%，完整度不足 |
| 后端集成 | 90 | **80** | spawn bug 修了代码但**没端到端验证**，修了≠通了 |
| 前端完整度 | 80 | **70** | 3 个页面有完整代码但依赖后端 API（shumiyuanApi/chronicleApi/starChartApi），这些 API 服务可能未实现 |
| 测试覆盖 | 85 | **70** | Python 130 测试确实好，但 TypeScript spec **没跑过**，前端**零测试** |
| 部署就绪 | 50 | **50** | 无 Docker/CI/CD |

### 1.2 关键发现

1. **3 个页面不是空壳**：ShumiyuanPage（317 行）、TongjianPage（296 行）、XingtuPage（210 行）都有完整 UI 逻辑，但依赖后端 API 服务
2. **TypeScript 编译有 14 个错误**：来自 3 个来源
   - `consult.service.spec.ts`（deprecated，8 个错误）— 引用已删除的模块
   - `generation-composer.spec.ts`（2 个错误）— mock 类型不匹配
   - `pipeline-integration.spec.ts`（1 个错误）— mock 类型不匹配
   - `consult.controller.ts` + `consult.service.ts`（3 个错误）— PrismaService 缺少 namingResult 属性
3. **后端 API 服务可能未实现**：shumiyuanApi、chronicleApi、starChartApi 这 3 个前端服务调用的后端路由可能不存在

---

## 二、执行计划（5 步，按 ROI 排序）

### Step 1：端到端验证 spawn 修复（+5 分，30 分钟）

**目标**：证明 spawnSync 修复真的生效，Python 引擎返回数据而非降级到 iztro

**操作**：
1. 启动后端服务：`cd apps/AWKN-LABlife/awkn-life-backend && npm run start:dev`
2. 发送测试请求：`curl -X POST http://localhost:3001/api/v1/consult/analyze -H "Content-Type: application/json" -d '{"routeType":"ziwei","question":"测试","birthDate":"1990-06-15","birthTime":"14:00","gender":"male"}'`
3. 检查后端日志：确认 `[ZiweiAgent] Python CLI` 而非 `[ZiweiAgent] iztro fallback`
4. 检查响应：确认 `calcData.palaces` 包含 12 个宫位数据

**验收标准**：
- 后端日志显示 Python CLI 调用成功（status=0）
- 响应包含完整的紫微斗数排盘数据
- 无降级到 iztro

**回滚**：如果 Python CLI 调用失败，确认降级到 iztro 仍可正常工作

**风险**：中 — 后端可能因缺少环境变量（LLM_API_KEY、SUPABASE_URL）无法启动

---

### Step 2：修复 TypeScript 编译错误（+5 分，1 小时）

**目标**：`npx tsc --noEmit` 零错误

**操作**：

#### 2a. 删除 deprecated spec（8 个错误 → 0）
- **文件**：`apps/api-server/src/consult/__tests__/deprecated/consult.service.spec.ts`
- **操作**：删除此文件（已标记 deprecated，引用的模块已不存在）
- **风险**：低 — 文件在 deprecated 目录下

#### 2b. 修复 generation-composer.spec.ts（2 个错误）
- **文件**：`apps/api-server/src/consult/__tests__/generation/generation-composer.spec.ts`
- **错误 1**（L20）：`createMockLlmGateway()` 返回值类型不匹配 `PrismaService`
  - **修复**：GenerationComposerService 构造函数参数顺序可能变了，需要读取实际构造函数签名后调整 mock 传入位置
- **错误 2**（L166）：`Property 'length' does not exist on type 'unknown'`
  - **修复**：添加类型断言 `(result as any[]).length`

#### 2c. 修复 pipeline-integration.spec.ts（1 个错误）
- **文件**：`apps/api-server/src/consult/__tests__/integration/pipeline-integration.spec.ts`
- **错误**（L37）：mock 类型不匹配 `LlmProvidersService`
  - **修复**：使用 `as any` 或 `createMockLlmProviders()` 替代

#### 2d. 修复 consult.controller.ts + consult.service.ts（3 个错误）
- **文件**：`apps/api-server/src/consult/consult.controller.ts` L460, L466
- **文件**：`apps/api-server/src/consult/consult.service.ts` L346, L348
- **错误**：`Property 'namingResult' does not exist on type 'PrismaService'`
  - **修复**：需要检查 PrismaService 是否有 namingResult 方法/属性，可能需要添加类型扩展或使用 `(prisma as any).namingResult`

**验收标准**：`npx tsc --noEmit` exit code 0

**回滚**：`git checkout -- <file>`

---

### Step 3：跑通 TypeScript 后端测试（+5 分，1 小时）

**目标**：`npx jest` 至少有 1 个测试套件通过

**操作**：
1. 安装测试依赖：`npm install --save-dev jest ts-jest @types/jest`
2. 运行测试：`cd apps/api-server && npx jest --config jest.config.js`
3. 根据失败结果修复 mock/类型问题
4. 优先确保 calc-engine.service.spec.ts 和 ziwei-agent.service.spec.ts 通过

**验收标准**：至少 1 个 .spec.ts 文件测试通过

**风险**：中 — mock 依赖可能不完整

---

### Step 4：验证前端 3 个页面的后端 API（+5 分，1 小时）

**目标**：确认 ShumiyuanPage/TongjianPage/XingtuPage 的后端 API 是否存在

**操作**：
1. 检查前端 API 服务文件：
   - `apps/app/src/services/shumiyuanApi.ts`
   - `apps/app/src/services/chronicleApi.ts`
   - `apps/app/src/services/starChartApi.ts`
2. 提取每个 API 调用的后端路由路径
3. 在后端代码中搜索对应路由是否存在
4. 如果路由不存在，标记为"前端已完成，后端未实现"
5. 如果路由存在，测试 API 是否可调用

**验收标准**：明确知道 3 个页面的后端 API 实现状态

**风险**：低 — 只读操作

---

### Step 5：Python 全量测试验证（+3 分，30 分钟）

**目标**：确认 Python 排盘引擎 130 测试全部通过（含 sxtwl）

**操作**：
1. `cd C:\Users\10919\Desktop\AWKN-Lab\projects\ziwei-reverse\python-validator`
2. `python -m pytest test_*.py -v --tb=short 2>&1`
3. 记录结果：通过数/失败数/skipped 数
4. 如有失败，分析原因并修复

**验收标准**：130/130 测试通过，0 skipped

**风险**：低 — sxtwl 已安装，之前 50/50 流盘测试已通过

---

## 三、预期评分变化

| Step | 动作 | 后端集成 | 测试覆盖 | 前端完整度 | 总分 |
|------|------|---------|---------|-----------|------|
| 当前 | — | 80 | 70 | 70 | 71.5 |
| Step 1 | 端到端验证 | **90** | 70 | 70 | 76.0 |
| Step 2 | 修复编译错误 | 90 | **75** | 70 | 78.5 |
| Step 3 | 跑通后端测试 | 90 | **80** | 70 | 80.0 |
| Step 4 | 验证前端 API | 90 | 80 | **75** | 81.5 |
| Step 5 | Python 全量测试 | 90 | **85** | 75 | 83.0 |

**最终预期**：**83/100**（从 71.5 提升 11.5 分）

---

## 四、风险与约束

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 后端无法启动（缺环境变量） | Step 1 无法执行 | 先检查 .env 配置，必要时用 mock 请求 |
| TypeScript 编译错误比预期复杂 | Step 2 耗时增加 | 优先删除 deprecated spec（8 个错误立即消除） |
| Jest 测试依赖不完整 | Step 3 可能失败 | 先确保 jest + ts-jest 安装正确 |
| 前端 API 后端未实现 | Step 4 只能确认状态 | 标记为"待实现"，不强行补齐 |

---

## 五、不做清单

- 不做 Docker 化（ROI 低，5+ 小时）
- 不做 CI/CD（ROI 低，5+ 小时）
- 不做 I18N（用户规模不足）
- 不做真太阳时（用户规模不足）
- 不做前端页面快照测试（需要 vitest 配置，1+ 小时）
- 不做 LLM 动态内容结构化（需设计新消息类型，3+ 小时）
