# P0-P1 修复授权清单

> **文档类型**：Authorization List（一次性授权清单）
> **创建日期**：2026-07-12
> **状态**：待用户确认
> **作用**：用户确认后，视为授权在已列明范围内连续执行，不再逐步骤询问
> **上游依据**：
> - `docs/03开发过程稿/P0-P1修复与闭环分阶段执行计划-20260712.md`
> - `docs/02开发PRD与工程文档/工程交接/P0-P1修复工程交接包-20260712.md`
> - `docs/02开发PRD与工程文档/工程交接/P0-P1修复工程文档-技术参考-20260712.md`

---

## 0. 现状核对结论（基于真实代码核对，非文档描述）

| 项 | 现状 | 核对证据 |
|----|------|---------|
| P0-1 K线 /seed Guard | **未修复** | `kline-tide.controller.ts` L281-292 裸路由无 @UseGuards；`kline-tide.service.ts` L761-773 无 ALLOW_SEED_WRITE 检查 |
| P0-2 取名 DTO 契约 | **未修复** | `consult/dto/index.ts` L267-269 仍 `@IsString() stylePreference?: string` |
| P0-3 取名 service 静默默认 | **未修复** | `consult.service.ts` L280-282 仍 `birthDate: dto.birthDate \|\| ''`；L288-292 仍 `JSON.parse(raw)` |
| P0-4 BrandStrategy Math.random | **未修复** | `BrandStrategy.tsx` L30-32 三处 `Math.floor(Math.random() * N)` |
| P1-1 KlineBar/StateSnapshot 字段 | **未修复** | `schema.prisma` L737-762 / L767-798 无 source/dataVersion/generatedAt |
| P1-2 取名三表 | **未修复** | `schema.prisma` 无 NamingProject/NamingCandidate/NamingIteration（仅 NamingResult L255-269） |
| P1-3 CEO 12 指标接口 | **部分存在** | AdminController 已有 `stats/enhanced`（L1323-1374），但不是 12 指标；AdminPage 已有 OperatingMetric 接口框架（L72-87），需扩展到 12 项 |
| P1-10 git 提交纪律 | **未收口** | 本地 HEAD=a20ef465，领先 origin/main 40 commits，工作区有大量未提交变更 |
| P1-11 spec 与实现脱节 | **未修复** | `kline-tide.service.spec.ts` L22-40 期望 ForbiddenException，但 seed() 实现未抛 → spec 当前会失败 |
| AdminGuard 存在性 | **已存在** | `auth/guards/admin.guard.ts` L1-28 正确实现 |

**结论**：4 个 P0 + 6 个 P1 全部未完成，需要执行 Phase 0-11 全部任务。

---

## 1. 将要修改的文件清单

### 1.1 后端代码修改（NestJS）

| 文件 | Phase | 修改性质 | 修改内容 |
|------|-------|---------|---------|
| `apps/api-server/src/kline-tide/kline-tide.controller.ts` | 1 | 修改 | L281 `@Post('seed')` 加 `@UseGuards(JwtAuthGuard, AdminGuard)` |
| `apps/api-server/src/kline-tide/kline-tide.service.ts` | 1, 6 | 修改 | L761 seed() 入口加 ALLOW_SEED_WRITE + NODE_ENV 检查；写入时设置 source/dataVersion/generatedAt |
| `apps/api-server/src/kline-tide/kline-tide.service.spec.ts` | 1 | 验证 | 让 L22-40 的 ForbiddenException spec 通过（service 修复后自动通过） |
| `apps/api-server/src/consult/dto/index.ts` | 2 | 修改 | L267-269 `stylePreference` 改为 `@IsArray() @IsString({ each: true }) string[]`；同时改 `style_preference` |
| `apps/api-server/src/consult/consult.service.ts` | 2, 3 | 修改 | L280-282 移除静默默认；L288-292 移除 JSON.parse；按 namingType 分叉（baby 要 birthDate，adult/brand 走场景化） |
| `apps/api-server/prisma/schema.prisma` | 6, 7 | 修改 | KlineBar/StateSnapshot 加 source/dataVersion/generatedAt；新建 NamingProject/NamingCandidate/NamingIteration 三模型 |
| `apps/api-server/src/naming/naming.service.ts` | 7 | **新建** | 取名项目管理 + 候选词收藏/淘汰/选中服务 |
| `apps/api-server/src/naming/naming.controller.ts` | 7 | **新建** | GET/POST /api/v1/naming/projects + candidates 收藏接口 |
| `apps/api-server/src/naming/naming.module.ts` | 7 | **新建** | NestJS 模块定义 |
| `apps/api-server/src/admin/admin-metrics.controller.ts` | 9 | **新建** | GET /api/v1/admin/metrics（12 指标聚合） |
| `apps/api-server/src/admin/admin-metrics.service.ts` | 9 | **新建** | 12 指标计算逻辑 |
| `apps/api-server/src/app.module.ts` | 7, 9 | 修改 | 注册 NamingModule + AdminMetricsModule |
| `apps/api-server/scripts/backfill-kline-source.ts` | 6 | **新建** | 一次性回填脚本：UPDATE KlineBar SET source='unknown' |

### 1.2 前端代码修改（React）

| 文件 | Phase | 修改性质 | 修改内容 |
|------|-------|---------|---------|
| `app/src/components/naming/BrandStrategy.tsx` | 4 | 修改 | L30-32 移除 Math.random，改固定映射 + "商标/域名未核验"提示 |
| `app/src/pages/AdminPage.tsx` | 9 | 修改 | 扩展 OperatingMetric 到 12 项，对接 /api/v1/admin/metrics |
| `app/src/api/admin.ts` | 9 | 修改 | 加 getAdminMetrics() 调用 |

### 1.3 文档修改

| 文件 | Phase | 修改性质 |
|------|-------|---------|
| `docs/01产品定位与PRD/功能方案/命运K线丰富方案-v2.md` 或同级 PRD | 8 | 升 v1.1，裁决 0-100岁 vs 36个月 + OHLCV 定义 |
| `docs/文档索引.md` | 10 | 新增 6 列，状态拆 6 档 |
| `docs/02开发PRD与工程文档/接口文档/README.md` | 10 | 标注权威源 |
| `docs/02开发PRD与工程文档/数据库文档/README.md` | 10 | 标注权威源 |
| `docs/02开发PRD与工程文档/测试用例/README.md` | 10 | 标注权威源 |
| `docs/01商业计划/D30-用户验证计划-20260712.md` | 11 | **新建** |
| `docs/02开发PRD与工程文档/运营/CEO-12指标数据字典-20260712.md` | 9 | **新建** |

---

## 2. 将要新增/删除/覆盖/迁移的内容

### 2.1 新增

| 类别 | 内容 | Phase |
|------|------|-------|
| 后端服务 | NamingService（取名项目管理） | 7 |
| 后端接口 | GET/POST /api/v1/naming/projects | 7 |
| 后端接口 | POST /api/v1/naming/candidates/:id/{favorite,eliminate,select} | 7 |
| 后端接口 | GET /api/v1/admin/metrics（CEO 12 指标） | 9 |
| 数据库表 | NamingProject | 7 |
| 数据库表 | NamingCandidate | 7 |
| 数据库表 | NamingIteration | 7 |
| 数据库字段 | KlineBar.source / dataVersion / generatedAt | 6 |
| 数据库字段 | StateSnapshot.source / dataVersion / generatedAt | 6 |
| 环境变量 | ALLOW_SEED_WRITE（生产默认 false） | 1 |
| 回填脚本 | scripts/backfill-kline-source.ts | 6 |
| 文档 | D30-用户验证计划、CEO-12指标数据字典 | 9, 11 |

### 2.2 删除

**无任何代码/文件删除操作。** 所有修改均为"加字段、加表、加接口、改实现"，无 DROP 操作。

### 2.3 覆盖

| 文件 | 覆盖内容 | 风险 |
|------|---------|------|
| `kline-tide.controller.ts` L281-292 | 加 @UseGuards 装饰器 | 低（仅加守卫，不改业务） |
| `kline-tide.service.ts` L761-773 | seed() 入口加环境检查 | 中（生产环境会抛 ForbiddenException） |
| `consult/dto/index.ts` L267-269 | stylePreference 类型改 string[] | 中（前端需同步） |
| `consult.service.ts` L280-298 | 移除静默默认 + 按 namingType 分叉 | 高（接口合同变化） |
| `BrandStrategy.tsx` L30-32 | 移除 Math.random | 低（评分变确定） |

### 2.4 迁移

| 类别 | 内容 | Phase |
|------|------|-------|
| 数据迁移 | KlineBar 存量数据回填 source='unknown' | 6 |
| 数据迁移 | StateSnapshot 存量数据回填 source='unknown' | 6 |
| schema 迁移 | ALTER TABLE 加 6 字段 + CREATE 3 表 | 6, 7 |

---

## 3. 涉及的敏感操作分类

### 3.1 数据库结构修改 ✅ 涉及

- **Phase 6**：ALTER TABLE KlineBar / StateSnapshot 各加 3 字段（SQLite 支持 ADD COLUMN）
- **Phase 7**：CREATE TABLE NamingProject / NamingCandidate / NamingIteration
- **风险**：低（仅加字段+加表，无 DROP）
- **回滚**：DB 备份恢复（dev.db.bak.20260712.schema）

### 3.2 环境变量 ✅ 涉及

- **新增**：`ALLOW_SEED_WRITE`（生产 .env 需添加，默认 false）
- **不涉及**：JWT_SECRET / LLM_API_KEY 等密钥不变
- **生产 .env 修改**：需要（加一行 `ALLOW_SEED_WRITE=false`）
- **风险**：低

### 3.3 密钥和凭证 ❌ 不涉及

本次修复不涉及任何密钥、凭证、token 的修改或新增。

### 3.4 依赖安装 ❌ 不涉及

本次修复不引入新依赖。NestJS/JwtAuthGuard/AdminGuard/Prisma 均已存在。
（如 NamingService 需要新工具库，会在 Phase 7 单独询问，但预计不需要）

### 3.5 系统服务 ❌ 不涉及

不涉及 Docker / Nginx / 防火墙 / cron 的修改。

### 3.6 PM2 ✅ 涉及（仅部署时）

- **部署时**：`pm2 restart awkn-life-backend --update-env`
- **不涉及**：PM2 配置文件 `ecosystem.config.js` 不修改
- **风险**：低（重启时短暂 500，选择低峰期）

### 3.7 生产环境 ✅ 涉及（部署时）

- **scp dist 上传**：需要服务器 SSH 凭证
- **服务器 DB 操作**：Phase 6-7 部署时需 sqlite3 执行 ALTER/CREATE
- **风险**：中（DB 变更需备份）

### 3.8 外部接口 ❌ 不涉及

本次修复不涉及任何外部 API 调用变化。QumingAgent 调用方式不变（仅按 namingType 分叉）。

### 3.9 付费服务 ❌ 不涉及

本次修复不涉及任何付费服务变更。

### 3.10 Git 提交/推送 ✅ 涉及

- **Phase 5**：分批 commit + push origin main
- **本地领先 40 commits**：需先整理工作区，分批提交
- **风险**：中（需确保不提交敏感文件）
- **回滚**：`git revert <sha>`

### 3.11 代码覆盖 ✅ 涉及

见 §2.3 覆盖清单。

---

## 4. 可能造成的影响

### 4.1 高影响

| 影响 | 触发 Phase | 影响范围 |
|------|-----------|---------|
| K线 /seed 生产环境返回 403 | 1 | 任何生产环境调 /seed 的客户端（应该没有，但需确认） |
| 取名接口 stylePreference 类型变化 | 2 | 前端取名流程（已发数组，DTO 改后反而正常） |
| 取名 service 按 namingType 分叉 | 3 | adult/brand 走场景化命名（新分支） |
| DB schema 变更 | 6, 7 | 生产 DB 需备份 + ALTER |

### 4.2 中影响

| 影响 | 触发 Phase |
|------|-----------|
| BrandStrategy 评分变确定值 | 4 |
| 40 commits push 到 origin | 5 |
| AdminPage 增加 12 指标卡片 | 9 |

### 4.3 低影响

| 影响 | 触发 Phase |
|------|-----------|
| 文档索引扩展 | 10 |
| D30 验证计划文档 | 11 |

---

## 5. 回滚方式

### 5.1 代码回滚

```bash
# 单 Phase 回滚
cd /opt/awkn-life/awkn-life-backend/apps/api-server
rm -rf dist && cp -r dist.bak.20260712.p0 dist  # 回到部署前
pm2 restart awkn-life-backend --update-env

# Git 回滚
git revert <commit-sha>
```

### 5.2 DB 回滚

```bash
cd /opt/awkn-life/awkn-life-backend/apps/api-server/prisma
cp dev.db.bak.20260712.schema dev.db  # 丢失 Phase 6-7 后业务数据
```

### 5.3 阶段性回滚点

| 回滚点 | 时机 | 回滚动作 |
|--------|------|---------|
| RP-0 | Phase 0 WIP commit 后 | `git reset --soft HEAD~1`（保留工作区） |
| RP-1 | Phase 1-4 完成后 | `git revert` P0 修复 commits |
| RP-2 | Phase 5 push 后 | `git revert` + `git push`（需用户授权 force push 才能彻底回退） |
| RP-3 | Phase 6-7 部署后 | DB 备份恢复 + dist 回退 |
| RP-4 | Phase 9 部署后 | dist 回退（无 DB 变更） |

---

## 6. 需要用户作出的选择

### 6.1 必须选择（影响执行路径）

| # | 决策项 | 选项 | 建议默认 |
|---|--------|------|---------|
| Q1 | **服务器 SSH 凭证**（部署时需要） | a) 现在提供 b) 部署时再提供 c) 我自己部署 | **c) 用户自己部署**（AI 只准备 dist 包 + 部署脚本） |
| Q2 | **Phase 5 git push 是否授权** | a) 授权 push origin main b) 只 commit 不 push c) 不 commit | **a) 授权 push**（需收口 40 commits 脱节） |
| Q3 | **P0-2 stylePreference 旧前端兼容** | a) 严格模式（string 报 400） b) 兼容模式（string 自动转 [string]） | **b) 兼容模式**（避免破坏旧前端） |
| Q4 | **Phase 3 场景化命名分支**（NamingService.scenarioBased） | a) 现在实现完整场景化 b) 先返回"暂不支持成人/品牌八字取名，请用宝宝取名"提示 | **b) 先返回提示**（最小闭环，场景化留 v0.2） |
| Q5 | **Phase 7 取名三表 selected 状态** | a) 现在实现 select 接口 b) 只实现 favorite/eliminate，select 留 v0.2 | **b) 只实现 favorite/eliminate**（最小闭环） |
| Q6 | **Phase 9 CEO 12 指标计算口径** | a) 按工程文档 §2.3 定义 b) 用户自定义 | **a) 按工程文档定义**（用户可在审查时调整） |
| Q7 | **Phase 8 PRD 裁决**（0-100岁 vs 36个月） | a) 0-100岁 b) 36个月 c) 两者并存 | **c) 两者并存**（K线 36 月 + 潮汐 0-100岁） |
| Q8 | **本地 40 commits 处理** | a) 全部保留并 push b) 整理后选择性 push c) squash 成 1 个 | **a) 全部保留并 push**（保留历史） |

### 6.2 默认方案（如用户不逐项回答，按以下执行）

- Q1=c（用户自己部署）
- Q2=a（授权 push）
- Q3=b（兼容模式）
- Q4=b（先返回提示）
- Q5=b（只 favorite/eliminate）
- Q6=a（按工程文档）
- Q7=c（两者并存）
- Q8=a（全部保留）

---

## 7. 建议的默认方案（综合）

### 7.1 执行策略

**最小闭环 + 分批部署**：
1. Phase 0-5 先完成 P0 修复 + 提交收口（不涉及 DB）
2. Phase 6-7 完成 DB schema 变更（需 DB 备份）
3. Phase 8-11 完成治理（文档 + 指标 + 验证计划）

### 7.2 部署策略

**3 批次部署**：
- 部署 1（Phase 0-5）：仅 dist 替换，无 DB 变更
- 部署 2（Phase 6-7）：dist + DB schema 变更
- 部署 3（Phase 8-11）：仅 dist 替换（Phase 9 接口）

### 7.3 风险控制

- 每个 Phase 完成后必须 typecheck + test + build 三连绿
- 部署前必须 DB 备份
- 部署后必须烟测（/seed 401 + 取名 200 + AdminPage 12 指标）

---

## 8. 本次授权的明确边界

### 8.1 授权范围内（用户确认后可自主执行）

- ✅ 修改 §1.1 列明的后端代码文件
- ✅ 修改 §1.2 列明的前端代码文件
- ✅ 修改 §1.3 列明的文档文件
- ✅ 新建 §2.1 列明的新文件/新表/新接口
- ✅ 运行 typecheck / test / build
- ✅ 运行 git add / commit（Phase 5）
- ✅ 运行 git push origin main（Phase 5，如 Q2=a）
- ✅ 修改本地 .env 文件（添加 ALLOW_SEED_WRITE）
- ✅ 运行 prisma generate（schema 变更后）
- ✅ 运行 sqlite3 本地 DB 操作（ALTER TABLE / CREATE TABLE）

### 8.2 授权范围外（必须再次询问）

- ❌ 修改生产服务器 .env 文件
- ❌ 修改生产服务器 DB（dev.db）
- ❌ ssh 到生产服务器执行任何命令
- ❌ pm2 restart 生产服务
- ❌ 修改 ecosystem.config.js / Nginx 配置
- ❌ 删除任何已存在文件
- ❌ 修改 JWT_SECRET / LLM_API_KEY 等密钥
- ❌ 安装新的 npm 依赖（如需要，单独询问）
- ❌ 修改 .trae/rules/ 下的规则文件
- ❌ git push --force / git reset --hard（破坏性 Git 操作）
- ❌ 修改 origin/main 之外的分支
- ❌ 触发付费服务调用

### 8.3 暂停条件（遇到以下情况必须暂停询问）

1. 出现授权清单未覆盖的新高风险操作
2. 需要删除或覆盖无法恢复的重要数据
3. 需要使用未授权的账号、密钥、付费资源或生产环境权限
4. 工程文档存在互相冲突且无法通过代码和上下文判断
5. 继续执行可能明显扩大项目范围
6. 关键依赖或外部服务不可用，且没有安全替代方案
7. 测试连续失败 3 次且无法定位根因

---

## 9. 执行计划概览（12 Phase）

| Phase | 任务 | 预估时间 | 风险 | 依赖 |
|-------|------|---------|------|------|
| 0 | 核对与基线锁死 + WIP 备份 | 30min | 低 | 无 |
| 1 | P0-1 K线 /seed 双层 Guard | 30min | 中 | 0 |
| 2 | P0-2 取名 DTO 契约修复 | 15min | 低 | 0 |
| 3 | P0-3 取名 service 按 namingType 分叉 | 30min | 高 | 2 |
| 4 | P0-4 BrandStrategy 移除 Math.random | 15min | 低 | 0 |
| 5 | 提交纪律收口（commit + push） | 1h | 中 | 1,2,3,4 |
| 6 | P01 K线数据契约（schema + 回填） | 2h | 中 | 5 |
| 7 | P03 取名三表（schema + 服务 + 接口） | 2h | 高 | 5 |
| 8 | PRD 裁决（0-100岁 vs 36个月） | 30min | 低 | 5 |
| 9 | CEO 12 指标看板（接口 + 前端） | 4h | 中 | 5 |
| 10 | 文档索引收口 | 1h | 低 | 8,9 |
| 11 | D+30 用户验证准备 | 1h | 低 | 10 |

**总计**：约 13.5 小时纯执行时间（不含等待用户确认）

---

## 10. 确认请求

请用户确认以下三项：

1. **授权清单是否通过**（是 / 否 / 需调整）
2. **§6.1 的 8 个选择项**（如全部同意默认方案，回复"全部默认"即可）
3. **是否立即启动 Phase 0**（是 / 否）

确认后，AI 将在授权范围内连续执行 Phase 0-11，不再逐步骤询问。

---

## 11. 修订记录

| 日期 | 版本 | 修订 | 修订人 |
|------|------|------|--------|
| 2026-07-12 | v0.1 | 首次生成（基于真实代码核对 + 工程文档） | 天火（tianhuo-sop-project）+ awkn-工程文档 |
