# 历史文档迁移核验

> 日期：2026-07-11
> 核验命令：`npm run audit:moves`
> 核验方式：比较原文件在 `HEAD` 中的 Git Blob 哈希与目标文件当前 Blob 哈希。

## 一、核验结果

| 指标 | 数量 |
|---|---:|
| 删除文档 | 41 |
| 纯移动 | 41 |
| 移动后更新 | 0 |
| 同名冲突 | 0 |
| 目标缺失 | 0 |

结论：41 个删除项均有唯一目标位置，文件内容完全一致。

## 二、迁移分组

### 1. 已完成执行计划归档

共 8 个文件，从：

`docs/03开发过程稿/进行中执行计划/`

迁移至：

`docs/03开发过程稿/已完成执行计划/`

包括：

- `golden-case-full-result.md`
- `golden-case-mvp0-result.md`
- `golden-case-step6-weak-llm-result.md`
- `v0.2-step4-regression-result.md`
- `叙事化解读跨模块迁移-执行计划-v1-2026-06-27.md`
- `叙事化解读跨模块迁移-执行计划-v2-2026-06-27.md`
- `张半山闭环工程化-v0.2-候选改进-执行计划-2026-06-27.md`
- `张半山闭环工程化-待办执行计划-v2-2026-06-27.md`

### 2. TRAE 历史 documents 归档

共 21 个文件，从：

`docs/06IDE配置与记忆/TRAE配置/documents/`

迁移至：

`docs/03开发过程稿/历史归档/06IDE-documents-STALE-20260711/`

覆盖旧执行计划、旧集成方案、旧修复方案、旧工程记录和写作管线过程文档。

### 3. TRAE 历史 specs 归档

共 12 个文件，从：

`docs/06IDE配置与记忆/TRAE配置/specs/`

迁移至：

`docs/03开发过程稿/历史归档/06IDE-specs-STALE-20260711/`

归档四组规格：

- `destiny-kline-v1/`
- `repo-restructure-git-boundary/`
- `shared-component-library-migration/`
- `zhangbanshan-benchmark-upgrade/`

每组均包含：

- `spec.md`
- `tasks.md`
- `checklist.md`

## 三、同名文件处理

规格目录内存在多个同名 `spec.md / tasks.md / checklist.md`。核验脚本已按原始子目录层级映射目标路径，避免仅按文件名匹配产生冲突。

## 四、提交条件

该迁移批次已经满足内容完整性条件。提交前检查：

1. `docs/文档索引.md` 已反映新的归档位置。
2. 当前有效文档没有引用旧路径。
3. Git 重命名识别结果合理。
4. 该批次不混入产品代码、知识数据和根目录物理迁移。

建议核验命令：

```bash
npm run audit:moves
git diff --summary --find-renames=50%
git diff --name-status --find-renames=50% -- docs/03开发过程稿 docs/06IDE配置与记忆
```

## 五、验收结论

历史文档迁移的内容完整性核验通过，可以作为独立文档迁移批次收口。
