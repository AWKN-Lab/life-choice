# 提交批次计划

> 日期：2026-07-11
> 当前有效内容变更：217 项
> Git stat-only 噪声：60 项
> 迁移核验：41 项纯移动，Blob 哈希完全一致

## 一、当前批次地图

执行：

```bash
npm run audit:batches
```

最近结果：

- 总有效项：217。
- 已进入明确批次：217。
- 待人工归类：0。
- 严格模式通过。

按单批次生成安全暂存命令：

```bash
powershell -NoProfile -ExecutionPolicy Bypass \
  -File scripts/maintenance/workspace-batch-map.ps1 \
  -Batch D1 -GitAddCommand
```

命令仅打印，不修改暂存区。

## 二、总原则

1. 每个提交只服务一个工程目标。
2. 产品代码、知识数据、历史迁移、质量报告、目录治理分开收口。
3. `FrontdeskChat.tsx`、多语言文件、`docs/文档索引.md` 属于共享热点，提交前按功能核对差异。
4. Prisma Schema、锁文件、构建脚本、资产清单进入独立高风险批次。
5. 根目录物理整理等待业务批次收口后执行。

## 三、建议顺序

| 顺序 | 批次 | 主要目标 | 当前状态 |
|---:|---|---|---|
| 0 | G0 可验证杂物 | 清理乱码重复文件、Office 锁文件、时间戳备份 | 已完成预演 |
| 1 | D1 历史文档迁移 | 41 个文档与规格归档 | 内容核验通过 |
| 2 | K1 知识导入与索引 | 知识导入脚本、测试、索引数据 | 暂存区已有 5 项 |
| 3 | K2 向量检索 v3 | Knowledge Service、v3 评估数据与切换测试 | 待独立验证 |
| 4 | P1 经营看板与前端错误采集 | CEO 周看板、API 指标采集、前端异常上报 | 待测试收口 |
| 5 | P2 统一前台联动 | K 线节点问事、取名候选收藏/对比/淘汰/最终选择 | 共享文件较多 |
| 6 | P3 产品与工程文档 | PRD、接口、数据库、部署、测试基线 | 待对齐代码状态 |
| 7 | Q1 质量治理 | 冻结决策、健康度、发布清单、取证报告 | 待独立收口 |
| 8 | B0 后端边界修复 | Schema、资产清单、npm workspace、构建链 | 当前 5 BLOCK，独立实施 |
| 9 | G1 项目治理 | START-HERE、目录治理、审计脚本、换行规则 | 本轮新增 |
| 10 | R1 根目录归位 | 构建包、日志、临时脚本、跨项目资产 | 等待工作区降载 |

---

## 四、G0｜可验证杂物

### 范围

- 三个乱码文件名的六壬 Prompt 副本。
- Office `~$` 临时锁文件。
- `bootstrap-production.js.bak_20260626_221436` 时间戳备份。

### 当前证据

三个乱码文件与以下正常文件在统一换行后逐字符一致：

| 异常文件 | 基准文件 |
|---|---|
| `хЕнхгмчеЮчЕЮцА╗шзИ.md` | `六壬神煞总览.md` |
| `хИЖч▒╗хНацЦнч║▓шжБ.md` | `分类占断纲要.md` |
| `шКВц░ФцЬИх░Жхп╣чЕзшби.md` | `节气月将对照表.md` |

### 验证命令

```bash
npm run audit:junk
```

### 验收

- 三个乱码副本通过正文一致性检查。
- Office 锁文件仅匹配 `~$` 前缀且小于 1MB。
- 时间戳备份进入 `_archive/backups/`，不进入源码提交。

---

## 五、D1｜历史文档迁移

### 范围

- `docs/03开发过程稿/进行中执行计划/` → `已完成执行计划/`
- `docs/06IDE配置与记忆/TRAE配置/documents/` → `docs/03开发过程稿/历史归档/06IDE-documents-STALE-20260711/`
- `docs/06IDE配置与记忆/TRAE配置/specs/` → `docs/03开发过程稿/历史归档/06IDE-specs-STALE-20260711/`

### 当前证据

- 迁移文件：41 项。
- 纯移动：41 项。
- 内容变化：0 项。
- 同名冲突：0 项。
- 目标缺失：0 项。

### 验证命令

```bash
npm run audit:moves
git diff --summary --find-renames=50% -- docs/03开发过程稿 docs/06IDE配置与记忆
```

### 禁止混入

- 产品代码。
- 知识索引数据。
- PRD 内容重写。
- 根目录物理整理。

---

## 六、K1｜知识导入与索引

### 当前暂存区

```text
knowledge/processed/classics_index.jsonl
scripts/__tests__/test_knowledge_ingest.py
scripts/_fix_null_bytes_first2.py
scripts/ingest_md_converted.py
scripts/knowledge_ingest.py
```

### 处理要求

1. 核对 `classics_index.jsonl` 变化规模和记录数。
2. 修复脚本保留输入、输出、幂等性和失败回滚说明。
3. 导入测试覆盖空字节、重复记录和路径编码。
4. 暂存区只保留本批次文件。

### 验证建议

```bash
python -m pytest scripts/__tests__/test_knowledge_ingest.py
python scripts/_check_manifests.py
```

---

## 七、K2｜向量检索 v3

### 范围

```text
apps/AWKN-LABlife/services/knowledge-service/main.py
knowledge/processed/l8_v3_evaluation.jsonl
scripts/eval_v3_vs_v2.py
scripts/__tests__/test_v3_switch.py
```

### 核心变化

- `KNOWLEDGE_EMBED_VERSION` 支持 v2 / v3 切换。
- v3 读取 BGE-small-zh 512 维 memmap。
- v2 保留 TF-IDF 路径。
- 健康接口返回当前 embedding 版本。

### 闸门

- v2 默认路径回归通过。
- v3 文件缺失时错误信息明确。
- 索引记录数与 embedding 行数一致。
- 服务器内存占用符合生产约束。
- `sentence-transformers` 依赖和离线模型路径有部署方案。

---

## 八、P1｜经营看板与前端错误采集

### 前端

```text
apps/AWKN-LABlife/app/src/lib/analytics.ts
apps/AWKN-LABlife/app/src/lib/analytics.test.ts
apps/AWKN-LABlife/app/src/main.tsx
apps/AWKN-LABlife/app/src/pages/AdminPage.tsx
```

### 后端

```text
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/src/analytics/api-metrics-collector.service.ts
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/src/analytics/api-metrics-collector.service.spec.ts
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/src/analytics/analytics.service.spec.ts
```

当前若需纳入 controller、module、service，先确认文件存在真实内容差异。stat-only 文件不进入提交。

### 验证建议

```bash
cd apps/AWKN-LABlife/app
npm run test:run -- src/lib/analytics.test.ts
npm run typecheck

cd ../awkn-life-backend
npm test -- analytics
npm run typecheck
```

---

## 九、P2｜统一前台联动

### 功能范围

1. K 线节点携带走势证据进入问事前台。
2. 取名候选支持收藏、对比、淘汰、恢复和最终选择。
3. 三语言文案同步。
4. 行为事件写入正式咨询记录。

### 主要文件

```text
apps/AWKN-LABlife/app/src/api/consult.ts
apps/AWKN-LABlife/app/src/components/frontdesk/FrontdeskChat.tsx
apps/AWKN-LABlife/app/src/components/frontdesk/frontdeskAdapters.ts
apps/AWKN-LABlife/app/src/components/frontdesk/frontdeskAdapters.test.ts
apps/AWKN-LABlife/app/src/pages/KlinePage.tsx
apps/AWKN-LABlife/app/src/locales/en/translation.json
apps/AWKN-LABlife/app/src/locales/th/translation.json
apps/AWKN-LABlife/app/src/locales/zh-CN/translation.json
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/src/consult/dto/naming-contract.spec.ts
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/src/kline-tide/kline-tide.service.spec.ts
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/src/quming-agent/quming-agent.service.spec.ts
```

### 共享热点

`FrontdeskChat.tsx` 同时承载两条功能。建议作为统一前台联动批次整体提交，避免手工拆 hunk 后造成状态机失配。

### 验证建议

```bash
cd apps/AWKN-LABlife/app
npm run test:run -- src/components/frontdesk/frontdeskAdapters.test.ts
npm run typecheck

cd ../awkn-life-backend
npm test -- naming-contract
npm test -- kline-tide
npm test -- quming-agent
npm run typecheck
```

---

## 十、P3｜产品与当前工程文档

### 范围

- `docs/01产品定位与PRD/`
- `docs/01商业计划/`
- `docs/02开发PRD与工程文档/`

### 要求

1. PRD 只声明已确认产品目标。
2. 工程基线只记录已验证事实。
3. 本地完成、待部署、生产完成使用三种状态。
4. API、Schema、部署和测试文档与同一代码版本对齐。
5. `docs/文档索引.md` 最后收口，统一更新路径和状态。

---

## 十一、Q1｜质量治理

### 范围

- `docs/05审核与质量/`
- Release Checklist
- PRD 冻结决策
- docs 健康度报告
- 生产取证报告
- v3 切换评估

### 要求

- 评审结论引用明确证据。
- 日期快照与当前实时状态分开。
- 生产取证报告保留命令、时间和环境。
- 质量报告不夹带产品需求改写。

---

## 十二、B0｜后端边界修复

### 当前状态

```bash
npm run audit:backend
```

当前结果：

- BLOCK：5
- WARN：3
- INFO：1
- PASS：3

### 独立范围

```text
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/prisma/schema.prisma
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/assets-manifest.json
apps/AWKN-LABlife/awkn-life-backend/apps/api-server/package-lock.json
apps/AWKN-LABlife/awkn-life-backend/package-lock.json
apps/AWKN-LABlife/awkn-life-backend/yarn.lock
apps/AWKN-LABlife/awkn-life-backend/package.json
.github/workflows/ci.yml
apps/AWKN-LABlife/DEPLOY.md
```

### 处理顺序

1. 恢复 Schema 与代码、迁移的一致性。
2. 恢复知识绑定资产清单项。
3. 评审 `ADR-后端依赖安装与构建边界-20260711.md`。
4. 在干净分支迁移 npm workspaces。
5. 统一 CI、生产、Staging 和 README 命令。
6. 运行严格后端闸门。

### 验收

```bash
powershell -NoProfile -ExecutionPolicy Bypass \
  -File scripts/maintenance/audit-backend-boundaries.ps1 -Strict
```

严格模式返回 0 后，B0 批次才可合并。

---

## 十三、G1｜项目治理

### 范围

```text
.gitattributes
.gitignore
START-HERE.md
README.md
ONBOARDING.md
REPO-MAP.md
package.json
docs/00项目治理/
scripts/maintenance/
```

### 本轮新增

- 统一工程入口。
- 目录审计。
- 变更分流。
- 文档迁移核验。
- 可验证杂物清理。
- 根目录整理预演。
- LF/CRLF 属性规则。

### 验证命令

```bash
npm run audit:changes
npm run audit:moves
npm run audit:junk
npm run audit:backend
npm run audit:workspace
npm run organize:workspace
```

---

## 十四、R1｜根目录归位

执行条件：

1. 产品代码与知识数据批次已收口。
2. 有效内容变更降到 25 项以内。
3. 暂存区只包含当前批次。
4. 历史迁移独立提交完成。
5. 整理预演和清理清单一致。

目标：

- 部署脚本进入 `scripts/deploy/legacy-root/`。
- 构建包进入 `_archive/builds/`。
- 临时技能和脚本进入 `_archive/tmp/`。
- 跨项目资产迁回所属项目。
- 根目录只保留工程入口、规则、配置和一级领域目录。
