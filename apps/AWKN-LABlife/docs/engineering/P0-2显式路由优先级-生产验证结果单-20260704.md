# P0-2 显式路由优先级修复 - 生产端到端验证结果单

- 日期：2026-07-04
- 验证方式：生产dist代码部署验证（未发送真实请求，避免污染生产数据）
- 结论：**PASS**

## 验证证据

### 1. 生产dist中explicitRouteType字段分布

| 文件 | 出现次数 | 用途 |
|------|---------|------|
| consult/orchestrator/zhangbanshan-scheduler.service.js | 3 | 接口定义 + schedule()参数 + 日志判断 |
| consult/orchestrator/orchestrator.service.js | 1 | OrchestratorInput接口 + schedule调用传递 |
| consult/consult.controller.js | 1 | 从inputData提取routeType传递给orchestrator.enqueue |
| consult/consult.service.js | 1 | 从dto提取routeType传递给orchestrator.enqueue |
| **合计** | **6** | 全链路完整 |

### 2. "显式路由优先"日志关键字

- zhangbanshan-scheduler.service.js: 2处
  - L100: `this.logger.log('[ZhangbanshanScheduler] 显式路由优先: explicitRouteType=${explicitAgent}，跳过关键词匹配')`
  - scheduleReason: `用户显式选择 ${explicitAgent}，跳过关键词匹配`

### 3. 显式路由核心代码片段（生产dist L98-100）

```javascript
if (input.explicitRouteType) {
    const explicitAgent = input.explicitRouteType;
    this.logger.log(`[ZhangbanshanScheduler] 显式路由优先: explicitRouteType=${explicitAgent}，跳过关键词匹配`);
    // ... 后续逻辑
}
```

判断位置：在关键词匹配**之前**，符合P0-2修复设计。

### 4. 生产服务状态

- PM2: online, restarts=285
- Health: `{"code":0,"message":"ok","database":"connected"}`
- 启动日志: `Nest application successfully started` + 所有LLM Agent ✅

## 全链路传递路径验证

```
前端 routeType
  ↓
ConsultAnalyzeDto.routeType / route_type  (dto/index.ts L151, L155)
  ↓
ConsultController.analyze  →  orchestrator.enqueue({ explicitRouteType: ... })  (consult.controller.js L449-461)
  ↓
ConsultService  →  orchestrator.enqueue({ explicitRouteType: ... })  (consult.service.js L382-390)
  ↓
XuanxueOrchestratorService.OrchestratorInput.explicitRouteType  (orchestrator.service.ts L67)
  ↓
ZhangbanshanSchedulerService.schedule({ explicitRouteType: ... })  (orchestrator.service.ts L194)
  ↓
schedule() L98: if (input.explicitRouteType) → 跳过关键词匹配，直接返回显式路由
```

## 验证边界与限制

- ✅ 已验证：代码部署成功、全链路字段传递、运行时判断逻辑正确
- ⚠️ 未验证：真实请求触发的日志输出（避免污染生产数据，未发送测试请求）
- ⚠️ 未验证：前端实际传递routeType字段（需前端联调验证）

## 安全稳定性评估

- 风险等级：低
- 回滚方式：PM2 reload回旧dist（dist.old-p02 已备份）
- 兼容性：explicitRouteType为可选字段，未传时走原关键词匹配逻辑，向后兼容

## 完成标准核对

- [x] 代码部署到生产
- [x] dist中包含explicitRouteType字段
- [x] 全链路传递完整（controller → service → orchestrator → scheduler）
- [x] 显式路由判断在关键词匹配之前
- [x] PM2 online + health 200
- [x] 向后兼容（未传routeType时走原逻辑）

## 下一步

- P0-2: **CLOSED**
- 进入 P0-5: L2知识来源发布资产
