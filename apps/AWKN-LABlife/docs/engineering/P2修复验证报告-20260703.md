# P2 修复验证报告

**修复日期**: 2026-07-03
**修复执行人**: AI Agent (GLM-5.2)
**验证环境**: /tmp/runtime-verify-20260703081933 (端口 30001, PID 927352)
**关联报告**: 生产代码独立运行时验证报告-20260703.md

---

## 1. 修复目标

| # | 问题 | 优先级 |
|---|---|---|
| P2-1 | ZipingAgent TypeError: 六壬路由下 preCalculatedBazi.yearPillar 为 undefined | P2 |
| P2-2 | npm run build 失败: FIVE_LAYER_KEYS 类型含 symbol 导致 quality-gate L114 报错 | P2 |

---

## 2. 修改范围（3 文件）

### 文件1: `apps/api-server/src/ziping-agent/ziping-agent.service.ts` L482
```typescript
// 修改前
if (input.preCalculatedBazi) {

// 修改后
if (input.preCalculatedBazi && input.preCalculatedBazi.yearPillar && input.preCalculatedBazi.dayPillar) {
```
**目的**: 防御性检查字段完整性，避免六壬/六爻等非八字路由传入空八字导致 TypeError

### 文件2: `apps/api-server/src/llm-gateway/llm-gateway.service.ts` L374
```typescript
// 修改前
if (input.calcResult?.calcData) {

// 修改后
if (input.calcResult?.calcData && input.calcResult.calcData.yearPillar) {
```
**目的**: 根因修复，只在 calcData 真正含八字数据时才传 preCalculatedBazi

### 文件3: `apps/api-server/src/consult/schemas/output-schema.ts` L30
```typescript
// 修改前
export const FIVE_LAYER_KEYS: (keyof FiveLayerOutput)[] = [...]

// 修改后
export const FIVE_LAYER_KEYS = [...] as const;
```
**目的**: 用 as const 收窄为字面量元组类型，消除 symbol，修复 quality-gate L114 的 "Type 'symbol' cannot be used as an index type"

---

## 3. 验证结果

### 3.1 TypeScript 类型检查: ✅ PASS
```bash
cd /tmp/runtime-verify-20260703081933/apps/api-server && npx tsc --noEmit
# 输出: 空（0 错误）
```

### 3.2 Nest Build: ✅ PASS
```bash
npx nest build
# exit code: 0
# dist/main.js 生成成功
```

### 3.3 服务启动: ✅ PASS
- PID: 927352
- 端口 30001 监听正常
- 所有 LLM Agent ✅ (QimenAgent, LiuyaoAgent, ZiweiAgent, MeihuaAgent, ZipingAgent, LiurenAgent, QumingAgent)
- Nest application successfully started

### 3.4 P2-1 核心验证: ✅ PASS

**测试用例**: 六壬路由 + 重大决策问题（触发 deep_consult, primary=liuren, secondary=ziping）

| 验证项 | 结果 | 证据 |
|---|---|---|
| TypeError 出现次数 | **0** ✅ | `grep -c "TypeError" /tmp/verify-restart.log` → 0 |
| ZipingAgent 调度 | ✅ | `mode=deep_consult confidence=0.95 primary=liuren secondary=ziping` |
| ZipingAgent LLM 调用 | ✅ | `[ZipingAgent] LLM分析成功，耗时86792ms` |
| ZipingAgent 完整执行 | ✅ | `八字分析完成，耗时 86792ms` |
| LlmGateway 并行结果 | ✅ | `primary=liuren(failed=false) secondary=ziping(failed=false) consistency=consistent` |
| ReAct Engine | ✅ | iter 1 + iter 2 多轮推理（THINK/ACT/OBSERVE/REFLECT） |

**对比修复前**:
- 修复前: `TypeError: Cannot read properties of undefined (reading '0')` at ziping-agent.service.js:413
- 修复后: ZipingAgent 正常完成 LLM 分析，无 TypeError

### 3.5 P2-2 核心验证: ✅ PASS

| 验证项 | 结果 |
|---|---|
| tsc --noEmit | 0 错误 ✅ |
| nest build | 成功 ✅ |
| FIVE_LAYER_KEYS 类型 | `as const` 字面量元组 ✅ |
| quality-gate L114 | 无 symbol index 错误 ✅ |

---

## 4. 修复前后对比

| 维度 | 修复前 | 修复后 |
|---|---|---|
| 六壬路由 + ZipingAgent | ❌ TypeError 崩溃 | ✅ LLM 分析成功（86792ms） |
| npm run build | ❌ 3 个 TS 错误 | ✅ 0 错误 |
| LlmGateway 并行调用 | ⚠️ ZipingAgent failed | ✅ liuren + ziping 都 failed=false |
| ReAct Engine | ⚠️ 证据不完整 | ✅ 多轮推理正常执行 |

---

## 5. 既有问题（非本次修复引入）

| # | 问题 | 说明 |
|---|---|---|
| 1 | 内存紧张 | 73MB free, swap 2845/5120 used（既有问题） |
| 2 | Python OOM | dmesg 显示 python3.11 (1.3GB) 被 OOM Killer 杀掉（与本次修改无关） |
| 3 | KnowledgeRetriever hybrid_search 超时 | 既有问题，降级到 /retrieve |
| 4 | LLM JSON schema invalid | ZipingAgent 第一次 LLM 返回不完整，第二次成功（LLM 输出质量问题） |

---

## 6. 未提交状态说明

本次修改的 3 个文件**未 commit**（遵循"NEVER commit unless explicitly asked"规则）。

**回滚方式**:
```bash
git checkout -- apps/api-server/src/ziping-agent/ziping-agent.service.ts
git checkout -- apps/api-server/src/llm-gateway/llm-gateway.service.ts
git checkout -- apps/api-server/src/consult/schemas/output-schema.ts
```

**当前 git 状态**: 3 个文件 modified，未 staged。

---

## 7. 验证环境状态

- **验证环境保留**: /tmp/runtime-verify-20260703081933 (PID 927352, 端口 30001)
- **新 dist 已部署**: 包含 P2 修复的 dist 已在验证环境运行
- **生产未受影响**: 本次修改仅在验证环境，生产 dist MD5 仍为 42ff65de...（未变）

---

## 8. 最终结论

**P2 修复验证通过**。

- ✅ P2-1: ZipingAgent TypeError 已修复，六壬路由下 ZipingAgent 正常完成 LLM 分析
- ✅ P2-2: npm run build 已修复，typecheck + nest build 均通过
- ✅ L2 Pipeline 完整执行：LiurenAgent + ZipingAgent 并行成功，ReAct Engine 多轮推理
- ⚠️ 既有问题（内存紧张、Python OOM、hybrid_search 超时）非本次修复范围

**建议下一步**:
1. 用户确认后 commit 这 3 个文件
2. 考虑部署到生产（含 P2 修复）
3. 既有问题（内存、OOM）单独排查

---

*报告生成时间: 2026-07-03 12:25 (UTC+8)*
