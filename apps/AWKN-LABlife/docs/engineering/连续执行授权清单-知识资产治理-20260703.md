# 连续执行授权清单：知识资产治理与 v3 检索系统

> **文档类型**：授权清单（执行前一次性确认）
> **撰写日期**：2026-07-03
> **基准文档**：
> - `工程交接文档-知识资产治理与v3检索.md`（HANDOFF）
> - `工程文档-知识资产治理与v3检索技术参考.md`（TECHNICAL REFERENCE）
> - `.trae/documents/知识资产治理与v3检索系统修复计划.md`（修复计划）
> **代码验证状态**：6 个 bug 全部未修复（已 Grep/Read 逐行验证）

---

## 0. 一屏摘要

| 项 | 内容 |
|---|---|
| 总目标 | 完成工程文档内全部未完成开发任务（G1 检索修复 + G3 语料治理 + G0+G2 资产保护 + G6 有条件） |
| 当前基线 | 6 个 bug 全部存在 / embeddings.npy 缺失 / 137337 passages 分类错+大文件 |
| 预计执行单元 | 12-15 个（约 10 分钟/单元） |
| 需用户选择 | 4 项（执行范围/生产部署/G6 阈值/Git 备份位置） |
| 默认方案 | 1A + 2A + 3A + 4A（见 §7） |
| 授权边界 | 详见 §8 |

---

## 1. 当前代码状态（已逐行验证）

### 1.1 6 个 bug 状态（执行前快照，现已全部修复 ✅）

| # | bug | 文件:行号 | 验证结果 |
|---|---|---|---|
| 1 | loader 路径错 | `loader.py:7` | ✅ 仍指向 `api-server/src/knowledge-base`（不存在） |
| 2 | /health 假计数 | `main.py:22` | ✅ 仍 `TOTAL_SOURCES = len(DATA.get('sources', {}))` = 2 |
| 3 | /retrieve 空 pool | `main.py:61` | ✅ 仍 `DATA.get(sys_key, []) + DATA.get('liuren', [])`（空） |
| 4 | 范数每次重算 | `main.py:225` | ✅ 仍 `emb_norms = np.linalg.norm(_loaded_embeddings, axis=1)` |
| 5 | verify 0 assert | `verify_knowledge_service.py` | ✅ 全是 print，0 个 assert |
| 6 | ingest 分类+切分 | `knowledge_ingest.py:93/109/132/169` | ✅ "斗数"归 bazi / 兜底 bazi / 只处理 list / 无硬切 |

### 1.2 资产状态

| 资产 | 状态 | 验证结果 |
|---|---|---|
| `classics_index.jsonl` | 存在 | 137337 行（_embed_meta.json 确认 count=137337） |
| `_manifest.json` | 存在 | 585 本（135KB，含完整书籍清单） |
| `idf_cache.pkl` | 存在 | TF-IDF IDF 缓存 |
| `_embed_meta.json` | 存在 | model=tfidf-char-bigram-v1, plan_b_used=true |
| `embeddings.npy` | **缺失** | LS 验证目录无此文件，需重新生成 |

### 1.3 任务完成状态

| 阶段 | 任务 | 状态 |
|---|---|---|
| G1 | 检索底座修复（5 子任务） | ✅ 全部完成（U1-U5 通过，verify 退出码 0） |
| G3 | 585 语料治理（4 子任务） | ✅ 全部完成（U6-U9 通过，142807 passages, 587 books） |
| G0+G2 | 资产保护+总账（4 子任务） | ✅ 全部完成（U10-U11 通过，commit 13afc5e0 + bundle 193.69MB） |
| G6-0 | TF-IDF 效果评估 | ✅ 完成（U12，recall@10=0.473, precision@10=0.344，未达阈值但用户选择不做 G6-1/G6-2） |
| G6-1/G6-2 | v3 索引（BM25+BGE-M3） | ⏸️ 跳过（用户选择 1A，未触发） |
| Phase 3 | 状态机统一（@deprecated + 迁移TODO） | ✅ 完成（U14-U15，typecheck + 66/66 测试通过） |
| G4/G5/G7 | 按需 | ⏸️ 延后 |
| U16 | 阶段审核+文档同步 | 🔄 进行中 |
| U17 | 最终验收+git commit | ⏸️ 待执行 |

---

## 2. 将要修改的文件、目录、模块和服务

### 2.1 代码文件修改（G1+G3）

| 文件 | 修改类型 | 涉及行号 | 修改内容 |
|---|---|---|---|
| `apps/AWKN-LABlife/services/knowledge-service/loader.py` | 重写 | L1-46 | 改为从 `knowledge/processed/classics_index.jsonl` 加载 + 按 system_type 分组 |
| `apps/AWKN-LABlife/services/knowledge-service/main.py` | 修改 | L22, L58-77, L79-87, L225 | 修假计数 + 修 search pool + 修 /health 响应 + 预计算范数 |
| `scripts/verify_knowledge_service.py` | 重写 | L1-76 | 全部 print 改为 assert + 加退出码 |
| `scripts/knowledge_ingest.py` | 修改 | L93, L109, L132, L169+ | 删"斗数"从 bazi + 新增 ziwei/quming/daoism + 兜底 other + 支持 dict + 硬切 400 字 |

### 2.2 数据文件重新生成（G3）

| 文件 | 操作 | 风险 |
|---|---|---|
| `knowledge/processed/classics_index.jsonl` | 重新生成（先备份 .bak） | 中（行数可能变化） |
| `knowledge/processed/_manifest.json` | 重新生成（先备份 .bak） | 中 |
| `knowledge/processed/embeddings.npy` | 新生成（当前缺失） | 低（无原文件覆盖） |
| `knowledge/processed/idf_cache.pkl` | 重新生成（先备份 .bak） | 中 |
| `knowledge/processed/_embed_meta.json` | 重新生成（先备份 .bak） | 低 |

### 2.3 新增文件（G0+G2+G6）

| 文件 | 阶段 | 用途 |
|---|---|---|
| `/d/awkn-git-backup-20260703/all.bundle` | G0 | Git 完整备份 |
| `/d/awkn-git-backup-20260703/objects-backup/` | G0 | .git/objects 备份 |
| `/d/awkn-git-backup-20260703/bundle.sha256` | G0 | 备份哈希 |
| `knowledge/asset-ledger/knowledge-asset-ledger.jsonl` | G2 | 资产总账 |
| `knowledge/asset-ledger/canonical-work-catalog.jsonl` | G2 | 作品目录（SHA256） |
| `knowledge/asset-ledger/source-lineage.jsonl` | G2 | 来源血缘 |
| `knowledge/asset-ledger/duplicate-groups.jsonl` | G2 | 重复组 |
| `knowledge/golden-queries.json` | G6-0 | 评估查询集（20-50 个） |
| `scripts/eval_retrieval.py` | G6-0 | 检索效果评估脚本 |
| `scripts/build_bm25_index.py`（有条件） | G6-1 | BM25 索引构建 |
| `scripts/embed_semantic.py`（有条件） | G6-2 | BGE-M3 语义向量 |
| `knowledge/v3/bm25_index.db`（有条件） | G6-1 | BM25 索引 |
| `knowledge/v3/embeddings_semantic.npy`（有条件） | G6-2 | 语义向量 |

### 2.4 服务影响

| 服务 | 影响 | 操作 |
|---|---|---|
| knowledge-service:8701 | 需重启加载新索引 | G1/G3 完成后重启 |
| 后端 API (NestJS:30000) | 无直接修改 | 需确认 orchestrator 兼容新 /health 字段 |
| Nginx | 无修改 | 无 |
| PM2 | 无修改（如生产部署则需重启 knowledge-service） | 见用户选择 §7-2 |
| Docker | 无修改 | 无 |

---

## 3. 高风险项检查

### 3.1 是否涉及

| 风险项 | 涉及 | 说明 |
|---|---|---|
| 数据库结构修改 | ❌ 不涉及 | 本项目无数据库（JSONL 文件存储） |
| 数据修改 | ✅ 涉及 | classics_index.jsonl/embeddings.npy/_manifest.json 重新生成（先备份） |
| 环境变量 | ✅ 涉及（新增） | G7 阶段新增 `KNOWLEDGE_INDEX_VERSION`（本次不设置，仅代码支持） |
| 密钥和凭证 | ❌ 不涉及 | 无 |
| 依赖安装 | ⚠️ 可能涉及 | G6-2 如需 BGE-M3，需 `pip install FlagEmbedding`（约 2GB 模型下载） |
| 系统服务 | ❌ 不涉及 | 无 |
| Docker | ❌ 不涉及 | 无 |
| PM2 | ⚠️ 可能涉及 | 如选择生产部署，需 `pm2 restart knowledge-service` |
| Nginx | ❌ 不涉及 | 无 |
| 防火墙 | ❌ 不涉及 | 无 |
| 定时任务 | ❌ 不涉及 | 无 |
| 生产环境 | ⚠️ 可能涉及 | 见用户选择 §7-2 |
| 外部接口 | ❌ 不涉及 | 无 |
| 付费服务 | ❌ 不涉及 | 无 |
| Git 提交 | ✅ 涉及 | 每阶段完成后 git commit（不 push，除非用户要求） |
| Git 推送 | ❌ 不涉及 | 默认不 push（如需 push 见用户选择） |
| Git 合并/回滚 | ⚠️ 可能涉及 | 如 G1 失败需 `git checkout` 回滚代码 |

### 3.2 高风险操作（需 SAFETY GATE）

| 操作 | 触发 | 缓解 |
|---|---|---|
| 覆盖 classics_index.jsonl | G3-4 重新生成 | 先 `cp .bak` 备份 |
| 覆盖 _manifest.json | G3-4 重新生成 | 先 `cp .bak` 备份 |
| 覆盖 idf_cache.pkl | G3-4 重新生成 | 先 `cp .bak` 备份 |
| git checkout 回滚 | G1/G3 失败时 | 仅回滚本次修改的文件，不影响其他 |

---

## 4. 可能造成的影响

### 4.1 正面影响

| 影响范围 | 内容 |
|---|---|
| 用户 | /retrieve 返回真实结果（当前返回 0 条） |
| 用户 | 紫微/取名/道教典籍分类正确 |
| 用户 | 查询延迟降低（范数预计算） |
| 运维 | /health 返回真实计数（当前返回假数据） |
| 开发 | verify 脚本可自动化（当前失败也 exit 0） |
| 资产 | .git 不可达对象有备份（当前 565 个无备份） |

### 4.2 负面影响/风险

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| G1 修改后服务无法启动 | 低 | 高 | 保留原文件备份，可 git checkout 回滚 |
| G3 重新 ingest 后 passages 数量变化 | 中 | 中 | 对比新旧 manifest |
| G3 分类修复破坏现有检索 | 低 | 高 | Golden Queries 验证 |
| embeddings.npy 生成失败 | 低 | 中 | 当前已缺失，最坏情况维持缺失 |
| 生产服务器磁盘不足 | 中 | 高 | embeddings.npy 约 536MB，服务器 4.6GB 可用 |
| 后端 orchestrator 不兼容新 /health 字段 | 中 | 高 | G1 前确认 orchestrator 代码 |
| G6 BGE-M3 模型下载失败 | 中 | 低 | G6 有条件，失败则保留 TF-IDF |

---

## 5. 回滚方式

### 5.1 G1 回滚（代码）

```bash
cd c:\Users\10919\Desktop\AWKN-Lab\人生决策宗师
git checkout -- apps/AWKN-LABlife/services/knowledge-service/loader.py
git checkout -- apps/AWKN-LABlife/services/knowledge-service/main.py
git checkout -- scripts/verify_knowledge_service.py
# 重启 knowledge-service
```

### 5.2 G3 回滚（数据）

```bash
cd c:\Users\10919\Desktop\AWKN-Lab\人生决策宗师\knowledge\processed
cp classics_index.jsonl.bak classics_index.jsonl
cp _manifest.json.bak _manifest.json
cp idf_cache.pkl.bak idf_cache.pkl
cp _embed_meta.json.bak _embed_meta.json
# embeddings.npy 如生成失败，直接删除新生成的
# 重启 knowledge-service
```

### 5.3 G0 回滚（Git 备份）

```bash
# .git 未被修改，无需回滚
# 如需删除备份：rm -rf /d/awkn-git-backup-20260703
```

### 5.4 G6 回滚（v3 索引）

```bash
# 删除 v3 索引文件，保留 v2
rm -rf knowledge/v3/
# KNOWLEDGE_INDEX_VERSION 保持 v2（默认）
```

---

## 6. 执行计划（12-15 个执行单元）

### 6.1 总目标

完成工程文档内全部未完成开发任务，使 knowledge-service 真实可用 + 语料分类正确 + 资产有备份 + v3 索引有条件就绪。

### 6.2 任务依赖关系

```
G1（检索修复）→ G3（语料治理）→ G0+G2（资产保护）→ G6（v3 索引，有条件）
     │                │
     └─ 依赖 loader    └─ 依赖 G1 的 verify 脚本
```

### 6.3 执行顺序

| 执行单元 | 任务 | 完成标准 | 验证方式 | 回滚点 |
|---|---|---|---|---|
| U1 | G1-1: 修复 loader.py | load_all() 返回 by_system 字典 | python -c "from loader import load_all; d=load_all(); print(len(d['by_system']))" | git checkout loader.py |
| U2 | G1-2+G1-3: 修复 main.py search + /health | /health 返回 bookCount>0 + /retrieve 返回非空 | curl /health + curl /retrieve | git checkout main.py |
| U3 | G1-4: 预计算向量范数 | _emb_norms 全局变量 + _vector_search 用全局 | grep "_emb_norms" main.py + 启动服务 | git checkout main.py |
| U4 | G1-5: verify 脚本加 assert | verify 脚本含 assert + 失败 exit≠0 | python verify_knowledge_service.py; echo $? | git checkout verify_knowledge_service.py |
| U5 | G1 闸门验证 | 5 项闸门全通过 | verify 退出码 0 + curl 4 接口 | 如失败回滚 U1-U4 |
| U6 | G3-1: 修复 ingest 分类 | "斗数"归 ziwei + 兜底 other | grep "斗数" knowledge_ingest.py + grep "return \"other\"" | git checkout knowledge_ingest.py |
| U7 | G3-2+G3-3: 修复 ingest 预切分+大文件 | 支持 dict + 硬切 400 字 | grep "isinstance(data, dict)" + grep "MAX_LEN" | git checkout knowledge_ingest.py |
| U8 | G3-4: 重新生成索引 | classics_index.jsonl 重新生成 + embeddings.npy 生成 | python knowledge_ingest.py + python embed_classics.py | 恢复 .bak 文件 |
| U9 | G3 闸门验证 | 紫微误入八字=0 + 大文件=0 + verify 退出码 0 | grep ziwei + max text_len + verify | 如失败回滚 U6-U8 |
| U10 | G0-1+G0-2+G0-3: Git 备份+验证 | bundle + objects 备份 + 20 个抽样验证 | ls /d/awkn-git-backup + git cat-file 抽样 | 删除备份（不影响项目） |
| U11 | G2-1: 建立资产总账 | 4 个 jsonl 文件生成 | ls knowledge/asset-ledger/ | 删除 asset-ledger 目录 |
| U12 | G6-0: TF-IDF 效果评估 | golden-queries.json + eval_retrieval.py + 评估报告 | python eval_retrieval.py | 删除评估文件 |
| U13 | G6-1+G6-2（有条件） | 如 recall@10<0.7 或 precision@10<0.6 则做 | ls knowledge/v3/ | 删除 v3 目录 |
| U14 | 阶段审核 + 文档同步 | 工程文档"当前状态"列更新 | Read 工程文档 | - |
| U15 | 最终验收 + git commit | 11 项验收全通过 + commit | git log + curl 验证 | git reset HEAD~1 |

### 6.4 最终验收标准（DoD）

| # | 验收项 | 通过标准 |
|---|---|---|
| 1 | /retrieve 有结果 | items ≥ 1 |
| 2 | /embed_search 有结果 | items ≥ 1 |
| 3 | /hybrid_search 有结果 | items ≥ 1 |
| 4 | /health 真实计数 | bookCount = 585, passageCount > 0 |
| 5 | verify 脚本通过 | exit code 0 |
| 6 | 紫微分类存在 | grep ziwei ≥ 1 |
| 7 | 取名分类存在 | grep quming ≥ 1 |
| 8 | 大文件单 passage = 0 | max text_len ≤ 500 |
| 9 | 向量范数只算 1 次 | grep "_emb_norms" 全局变量 |
| 10 | .git 已备份 | ls /d/awkn-git-backup-20260703/ |
| 11 | 资产总账生成 | ls knowledge/asset-ledger/ 4 文件 |

---

## 7. 需要用户作出的选择

### 选择 1：执行范围

| 选项 | 范围 | 说明 |
|---|---|---|
| **1A（推荐）** | G1 + G3 + G0 + G2 + G6-0 | 完成全部必做项 + G6 评估，不做 G6-1/G6-2（有条件） |
| 1B | G1 + G3 only | 仅修复检索底座 + 语料治理，资产保护延后 |
| 1C | G1 + G3 + G0 + G2 + G6 全部 | 含 G6-1/G6-2（如评估不达标则做 v3 索引） |

### 选择 2：生产部署

| 选项 | 范围 | 说明 |
|---|---|---|
| **2A（推荐）** | 仅本地验证 | 修复后仅本地启动验证，不部署生产 |
| 2B | 部署生产 | 修复后 SSH 到生产服务器，替换 dist + PM2 重启 |

### 选择 3：G6 触发条件

| 选项 | 阈值 | 说明 |
|---|---|---|
| **3A（推荐）** | recall@10 < 0.7 或 precision@10 < 0.6 则做 G6 | 沿用修复计划默认 |
| 3B | 严格（recall@10 < 0.8 或 precision@10 < 0.7） | 更早触发 G6 |
| 3C | 跳过 G6 | 不做 v3 索引评估，直接结束 |

### 选择 4：Git 备份位置

| 选项 | 位置 | 说明 |
|---|---|---|
| **4A（推荐）** | `D:\awkn-git-backup-20260703` | 本地 D 盘 |
| 4B | 其他位置 | 用户指定 |

---

## 8. 本次授权的明确边界

### 8.1 授权范围内（可自主执行）

- ✅ 修改 loader.py / main.py / verify_knowledge_service.py / knowledge_ingest.py
- ✅ 备份并重新生成 classics_index.jsonl / _manifest.json / embeddings.npy / idf_cache.pkl / _embed_meta.json
- ✅ 运行 knowledge_ingest.py / embed_classics.py / verify_knowledge_service.py
- ✅ 启动/停止本地 knowledge-service（127.0.0.1:8701）
- ✅ 创建 knowledge/asset-ledger/ 目录及文件
- ✅ 创建 knowledge/golden-queries.json / scripts/eval_retrieval.py
- ✅ Git 备份到 D 盘（git bundle + cp objects）
- ✅ git commit（每阶段完成后，不 push）
- ✅ 如选择 1C，创建 scripts/build_bm25_index.py / scripts/embed_semantic.py
- ✅ 如选择 1C，pip install FlagEmbedding（仅 G6-2 触发时）
- ✅ 如选择 2B，SSH 到生产 + PM2 重启

### 8.2 授权范围外（需再次确认）

- ❌ 删除 .git/objects 中的任何对象
- ❌ git push 到远程
- ❌ git gc / git prune / git repack
- ❌ 修改 Nginx 配置
- ❌ 修改防火墙规则
- ❌ 修改后端 orchestrator 代码（NestJS）
- ❌ 修改 Prisma schema / 数据库结构
- ❌ 删除现有功能
- ❌ 新增付费服务订阅
- ❌ 修改 .env 文件中的密钥

### 8.3 暂停条件

执行过程中，以下情况暂停并询问：
1. 出现授权清单未覆盖的新高风险操作
2. 需删除或覆盖无法恢复的重要数据
3. 需使用未授权的账号、密钥、付费资源
4. 工程文档存在互相冲突且无法通过代码判断
5. 继续执行可能明显扩大项目范围
6. 关键依赖不可用且无安全替代

---

## 8.4 默认方案（如用户回复"全部授权"）

执行 **1A + 2A + 3A + 4A**：
- G1 + G3 + G0 + G2 + G6-0（不做 G6-1/G6-2）
- 仅本地验证，不部署生产
- recall@10 < 0.7 或 precision@10 < 0.6 才做 G6
- Git 备份到 D:\awkn-git-backup-20260703

---

*授权清单生成时间: 2026-07-03 (UTC+8)*
