# 四闸执行结果单

- **执行时间**：2026-07-03 00:50 ~ 01:10 (北京时间)
- **执行人**：AI Agent (全部授权执行)
- **基线commit**：本地 6c385168 / 生产 02227c4 (落后52 commits)
- **生产服务器**：阿里云 8.148.245.29
- **域名**：awkn.cn (https://awkn.cn/life/)

## 四闸执行总结

### 第一闸：可复现性 — PASS
- ✅ git push 成功（6c385168 → origin/main）
- ✅ 根构建链路修复（postinstall 指向 API schema）
- ✅ rule-knowledge-bindings.json 已加入 assets-manifest.json
- ✅ dist 可复现（本地构建 SHA256 一致）

### 第二闸：核心运行时 — PASS
- ✅ rizhu-strength 已注册
- ✅ 三法融合代码正确（ziweiAnalysis.slice 是有效 string 方法）
- ✅ ReAct JSON 解析已有 try-catch 容错
- ✅ 紫微 CLI 有 fallback（缺失降级到 iztro JS）

### 第三闸：K线治理 — PASS
- ✅ ALLOW_SEED_WRITE 门禁已添加（kline-tide.service.ts 第354行 + 第412行）
- ✅ 生产 .env.prod 未设置 ALLOW_SEED_WRITE（正确，默认禁止自动 seed）
- ✅ 无 K线数据时返回空数组，不自动写库

### 第四闸：P0装甲激活 — PASS
- ✅ dist 含 P0 装甲全部文件：
  - rule-matcher.service.js (1814 bytes)
  - evidence-composer.service.js (4605 bytes)
  - knowledge-retriever.service.js (4271 bytes)
  - rule-knowledge-bindings.json (5282 bytes)
- ✅ EVIDENCE_PACKAGE_ENABLED=true 已写入 .env.prod
- ✅ PM2 重启成功（266 restarts, online, unstable_restarts=0）
- ✅ 模块加载验证：rule-matcher/evidence-composer/knowledge-retriever 全部 OK
- ✅ 调用链路完整：zhangbanshan-scheduler 第366行检查 EVIDENCE_PACKAGE_ENABLED → evidence-composer.compose → prompt-layers.formatEvidencePackage
- ✅ 失败降级机制存在（异常时 evidencePackage=undefined，走旧路径）

## 5件套验证结果

| 验证项 | 结果 | 证据 |
|--------|------|------|
| 1. 前端 HTML title | ✅ PASS | `<title>人生决策宗师</title>` |
| 2. 健康检查 API | ✅ PASS | `/api/v1/health` 返回 `{"code":0,"message":"ok","database":"connected"}` |
| 3. API 可达性 | ✅ PASS | `/api/v1/auth/login` 返回 400 (参数校验，路由存在) |
| 4. API 404 | ✅ PASS | 未知路径返回 404 + JSON 错误结构 |
| 5. PM2 unstable_restarts | ✅ PASS | 0 (online, 6m uptime) |

## UC1 冒烟结果

**请求**：POST /api/v1/consult/analyze
```json
{"question":"我最近事业顺利吗","birthDate":"1990-01-15","birthTime":"10:30","birthPlace":"北京","gender":"male"}
```

**响应**（关键字段）：
- ✅ route_type: ziping
- ✅ summary_line: 四柱己巳/丁丑/庚辰/辛巳，日主庚中和，用神参考水、火、木
- ✅ summary_body: 完整命理分析（五行分布/喜忌用神/大运/紫微命宫/四化）
- ✅ evidence_fold: 证据包（四柱/十神/五行/身强弱/喜忌/大运/紫微命宫/紫微五行局）
- ✅ actions: 行动建议（3条）
- ✅ risks: 风险提示（2条）
- ✅ time_window: 33-42岁大运区间
- ✅ paywall_modules: [morning, breakthrough, kline]
- ✅ record_id: 4d87a8cc-2f51-4adf-a94b-de54883b4295

**核心链路无降级**：
- ✅ 日志 0 errors / 0 fatal
- ✅ PM2 unstable_restarts=0
- ✅ 完整 evidence_fold 返回（证据包已在纯算法层生成）

## P0装甲运行时说明

P0装甲（RuleMatcher + EvidenceComposer）在 LLM 推理链路触发：
- 触发位置：zhangbanshan-scheduler.service.js 第366行
- 触发条件：`EVIDENCE_PACKAGE_ENABLED === 'true'` + evidenceComposerService 存在
- 当前UC1走纯算法初判路径（未调用LLM），P0装甲未触发属正常行为
- P0装甲将在"深入推演"等LLM场景中触发，注入证据包到prompt

## 回滚方案

如需回滚到部署前状态：
```bash
# 1. 恢复 dist
cd /opt/awkn-life/awkn-life-backend/apps/api-server
mv dist dist.failed-$(date +%Y%m%d)
mv dist.old-20260703 dist

# 2. 恢复 .env.prod（移除 EVIDENCE_PACKAGE_ENABLED）
cp .env.prod.bak-20260703 .env.prod

# 3. 恢复 package.json
cp package.json.bak-20260703 package.json
cp apps/api-server/package.json.bak-20260703 apps/api-server/package.json
npm install --omit=dev

# 4. PM2 重启
pm2 restart awkn-life-backend --update-env

# 5. 完整回滚（如有严重问题）
rm -rf /opt/awkn-life
cp -r /opt/awkn-life-backup-20260703_005316 /opt/awkn-life
pm2 restart awkn-life-backend --update-env
```

## 部署产物清单

| 产物 | 位置 | 状态 |
|------|------|------|
| 备份 | /opt/awkn-life-backup-20260703_005316 (2.4G) | ✅ 完整 |
| dist.old | /opt/awkn-life/awkn-life-backend/apps/api-server/dist.old-20260703 | ✅ 旧版本 |
| .env.prod.bak | /opt/awkn-life/awkn-life-backend/apps/api-server/.env.prod.bak-20260703 | ✅ 旧配置 |
| package.json.bak | /opt/awkn-life/awkn-life-backend/{,apps/api-server/}package.json.bak-20260703 | ✅ 旧依赖 |
| 本地zip | /tmp/dist-p0-armor.zip (1.6MB) | ✅ 部署包 |

## 遗留事项

- 🟡 Step 12-13（source标识+前端UI标注模拟数据来源）：中优先级，deferred
- 🟡 PM2 restarts=266（历史累计，非本次部署引入）：观察中
- 🟢 P0装甲 LLM 链路运行时触发验证：需真实"深入推演"场景，可通过前端实际操作触发

## 强制收尾

**下次遇到类似情况，先做哪3件事？**
1. 查看当前状态（git status / PM2 list / 日志）
2. 备份当前版本（cp -r /opt/awkn-life /opt/awkn-life-backup-$(date +%Y%m%d_%H%M%S)）
3. 读取完整文件并确认修改位置（Read → Edit，不盲目执行）
