# 工程文档连续执行 - 完整授权清单

> **文档类型**：EXECUTION（执行授权）
> **撰写日期**：2026-07-03
> **目标**：完成工程文档内全部未完成开发任务
> **执行模式**：工程文档连续执行模式（10分钟一个执行单元，自主决策）
> **状态**：待用户确认

---

## 1. 项目现状对照真实代码的核验结果

### 1.1 已完成任务（代码已落地，无需重做）

| 任务 | 文档来源 | 代码核验结果 |
|------|---------|-------------|
| 任务1 P0-Fix装甲通电 | v3任务包 | ✅ orchestrator.module.ts L14-21 import + L59-63 providers，4服务已注册 |
| 任务2 K线/潮汐接入问事 | v3任务包 | ✅ kline-tide.service.ts 含getCurrentStage；tide-inference.service.ts 含get12DimStatus；ZhangbanshanOutput.tsx 含LifeStageCard |
| 任务3 单事深推支付闭环 | v3任务包 | ✅ payment.service.ts 含createDeepDiveOrder；payment.controller.ts 含端点 |
| Step 1-5 可复现性 | 全量计划 | ✅ git push成功（6c385168），根构建链路修复 |
| Step 6-10 核心运行时 | 全量计划 | ✅ knowledge-base dist复制修复，rizhu-strength注册 |
| Step 11 ALLOW_SEED_WRITE门禁 | 全量计划 | ✅ kline-tide.service.ts L354+L412 已添加门禁 |
| Step 14-16 P0装甲部署激活 | 全量计划 | ✅ 生产EVIDENCE_PACKAGE_ENABLED=true，PM2 online |

### 1.2 未完成任务（本次需完成）

| 任务 | 文档来源 | 真实代码状态 | 工作量 |
|------|---------|-------------|--------|
| **Step 12 后端source标识** | 全量计划L302-316 | ⚠️ 部分完成。tide-inference有`source:'llm'\|'fallback'`，但kline-tide.service.ts的TidePackage/ParsedKlineBar/ParsedStateSnapshot接口**完全缺失source字段** | 中（3接口+1方法修改） |
| **Step 13 前端UI标注** | 全量计划L320-333 | ⚠️ 部分完成。KlinePage.tsx L484有`scores.source==='bazi'`显示"八字档案/默认兜底"，但**缺少完整source徽章**（模拟数据/LLM推导/降级数据） | 小（1组件增强） |
| **任务1 P0装甲LLM链路真实触发验证** | v3任务包L100-110 + P0-Fix验收报告 | ❌ 未做。需"深入推演"场景触发LLM链路，验证RuleMatcher/EvidenceComposer日志+matchedRules>=1+true/false差异化 | 小（验证为主） |
| **任务2 远程端到端验证** | 任务2验收报告L168-173 | ❌ 未做。验证zhangbanshan_output.life_stage字段返回+LifeStageCard渲染+KlinePage"问此事"按钮 | 小（验证为主） |
| **任务3 远程Stripe环境验证** | 任务3验收报告L196-203 | ❌ 未做。Stripe CLI触发webhook+真实¥1支付+追问额度提升+199元回归 | 中（涉及付费） |

### 1.3 部分完成需补齐的代码

**Step 12 - kline-tide.service.ts 缺失source字段**：
- `TidePackage` 接口（L191）无source字段
- `ParsedKlineBar` 接口（L64）无source字段
- `ParsedStateSnapshot` 接口（L106）无source字段
- `getKlineBars()` / `getStateSnapshots()` / `getTidePackage()` 返回值需附加source

**Step 13 - KlinePage.tsx 徽章不完整**：
- L484 `scores.source === 'bazi'` 只覆盖2种状态
- 缺少 `'simulated'` / `'llm_inferred'` / `'fallback'` 的完整枚举映射
- 缺少视觉徽章组件

---

## 2. 授权清单（一次性确认）

### 2.1 将要修改的文件清单

#### 后端代码修改（低风险）

| 文件 | 修改类型 | 变更内容 | 风险 |
|------|---------|---------|------|
| `apps/api-server/src/kline-tide/kline-tide.service.ts` | 修改 | TidePackage/ParsedKlineBar/ParsedStateSnapshot 接口新增source字段；getKlineBars/getStateSnapshots/getTidePackage 方法返回值附加source枚举 | 低（增量字段，向后兼容） |

#### 前端代码修改（低风险）

| 文件 | 修改类型 | 变更内容 | 风险 |
|------|---------|---------|------|
| `app/src/pages/KlinePage.tsx` | 修改 | 增强 source 显示逻辑，新增完整徽章组件覆盖 'real'/'simulated'/'llm_inferred'/'fallback' 四种状态 | 低（UI增强） |
| `app/src/services/klineTideApi.ts` | 修改 | 同步后端source字段类型定义 | 低（类型同步） |

#### 配置文件修改（中风险）

| 文件 | 修改类型 | 变更内容 | 风险 |
|------|---------|---------|------|
| 生产 `.env.prod` | 可能修改 | 若任务3 Stripe验证需要，可能新增 STRIPE_WEBHOOK_SECRET 等变量（待确认已配置） | 中 |

#### 文档更新（低风险）

| 文件 | 修改类型 | 变更内容 | 风险 |
|------|---------|---------|------|
| `docs/engineering/全量落地执行计划.md` | 修改 | 填写I.1四闸通过情况 + I.2卡点 + I.3下轮第一步；更新Step 12-13状态 | 低 |
| `docs/engineering/连续执行结果单-20260703.md` | 新增 | 最终结果单 | 低 |

### 2.2 涉及的系统服务与外部依赖

| 类别 | 具体内容 | 风险等级 | 必要性 |
|------|---------|---------|--------|
| **Git 提交+push** | 本地修改后 commit + push origin main | 中 | 必需（Step 12-13代码需同步远程） |
| **生产部署dist替换** | scp新dist到生产 + PM2重启 | 高（SAFETY GATE） | 必需（Step 12-13代码需部署才能验证） |
| **PM2 重启** | `pm2 restart awkn-life-backend --update-env` | 中（服务中断<30秒） | 必需（部署后激活新代码） |
| **Stripe 真实支付验证** | ¥1 真实支付 + Stripe CLI webhook | 高（涉及付费） | 可选（可降级为mock验证） |
| **数据库查询** | 验证K线source字段、深推订单 | 低（只读） | 必需（验证用） |

### 2.3 不涉及的操作（明确边界）

- ❌ 不涉及数据库结构变更（无新表、无字段迁移）
- ❌ 不涉及密钥/凭证新增（复用现有Stripe key）
- ❌ 不涉及Nginx配置修改
- ❌ 不涉及防火墙规则
- ❌ 不涉及Docker
- ❌ 不涉及定时任务
- ❌ 不删除任何现有功能
- ❌ 不重写RuleMatcher/EvidenceComposer（v3任务包禁止条款）

### 2.4 可能造成的影响

| 影响项 | 影响范围 | 缓解措施 |
|--------|---------|---------|
| K线接口响应新增source字段 | 前端需同步类型 | 增量字段，前端旧版本忽略不影响 |
| 生产部署导致服务中断30秒 | 用户短暂无法访问 | 选择低峰期部署，已有备份可回滚 |
| Stripe真实支付产生真实交易 | 账户余额变动¥1 | 可降级为mock验证，或事后退款 |
| P0装甲LLM链路触发可能改变输出风格 | LLM响应内容变化 | 已有false回退路径，可随时关闭 |

### 2.5 回滚方式

**代码回滚**：
```bash
git revert <new-commit-sha>
git push origin main
```

**生产部署回滚**：
```bash
cd /opt/awkn-life/awkn-life-backend/apps/api-server
mv dist dist.failed-$(date +%Y%m%d)
mv dist.old-20260703 dist  # 复用上一轮备份
pm2 restart awkn-life-backend --update-env
```

**完整回滚（含.env.prod）**：
```bash
cp /opt/awkn-life-backup-20260703_005316/awkn-life-backend/apps/api-server/.env.prod.bak-20260703 /opt/awkn-life/awkn-life-backend/apps/api-server/.env.prod
# + 上述dist回滚
```

### 2.6 需要用户作出的选择

#### 选择1：Stripe真实支付验证方式

**选项A**（推荐）：使用Stripe CLI在测试模式触发webhook，不产生真实交易
- 优点：无资金风险，验证逻辑完整
- 缺点：需安装Stripe CLI，测试模式 webhook 验证

**选项B**：真实¥1支付端到端验证
- 优点：最真实
- 缺点：产生真实交易（可事后退款），需真实信用卡

**选项C**：跳过Stripe远程验证，仅做本地mock验证
- 优点：零风险
- 缺点：任务3验收报告的"后续验证项"未完成

**建议默认方案**：选项A（Stripe CLI测试模式）

#### 选择2：部署窗口期

**选项A**（推荐）：立即执行（当前凌晨，流量低）
**选项B**：延后到指定时间
**建议默认方案**：选项A（立即执行）

#### 选择3：P0装甲LLM链路触发验证方式

**选项A**（推荐）：通过API发起1次"深入推演"问事，检查日志
**选项B**：通过前端浏览器操作触发
**建议默认方案**：选项A（API可控，可保留完整日志）

### 2.7 本次授权的明确边界

**授权范围内**：
1. 修改 kline-tide.service.ts 添加source字段（后端）
2. 修改 KlinePage.tsx + klineTideApi.ts 同步source显示（前端）
3. 本地 tsc + 测试验证
4. git commit + push origin main
5. scp dist 到生产 + PM2重启
6. 5件套验证 + UC1冒烟 + P0装甲LLM链路触发验证
7. 任务2远程端到端验证（zhangbanshan_output.life_stage）
8. Stripe CLI测试模式webhook验证（选项A）
9. 更新工程文档（全量计划复盘 + 结果单）

**授权范围外（需另行确认）**：
1. 真实¥1支付（选项B）
2. 数据库结构变更
3. 新增密钥/凭证
4. Nginx/防火墙修改
5. 删除现有功能或数据

---

## 3. 完整执行计划（连续执行）

### 3.1 总目标

完成工程文档内全部未完成开发任务：Step 12后端source标识 + Step 13前端UI标注 + 任务1/2/3远程验证 + 文档状态写回。

### 3.2 当前基线

- 本地HEAD: 6c385168（已push到origin/main）
- 生产: 6c385168（已部署，EVIDENCE_PACKAGE_ENABLED=true激活）
- PM2: online, unstable_restarts=0
- 备份: /opt/awkn-life-backup-20260703_005316 (2.4G)

### 3.3 执行顺序（按依赖关系）

```
单元1: 后端source字段（Step 12） → 单元2: 前端徽章（Step 13）
  → 单元3: 本地验证（tsc+测试） → 单元4: git commit+push
  → 单元5: 生产部署 → 单元6: 5件套+UC1验证
  → 单元7: P0装甲LLM链路触发验证（任务1） 
  → 单元8: 任务2远程端到端验证
  → 单元9: 任务3 Stripe CLI验证
  → 单元10: 文档写回+最终结果单
```

### 3.4 每个执行单元的完成标准与验证方式

#### 单元1：后端source字段（Step 12）
- **动作**：kline-tide.service.ts 的 TidePackage/ParsedKlineBar/ParsedStateSnapshot 接口新增 source 字段；getKlineBars/getStateSnapshots/getTidePackage 返回值附加source
- **source枚举**：`'real' | 'simulated' | 'llm_inferred' | 'fallback'`
- **完成标准**：tsc通过 + 接口定义含source字段
- **验证**：`npx tsc --noEmit -p apps/api-server/tsconfig.json`
- **回滚**：`git checkout kline-tide.service.ts`

#### 单元2：前端徽章（Step 13）
- **动作**：KlinePage.tsx 增强source显示；klineTideApi.ts 同步类型
- **完成标准**：前端tsc通过 + 4种source状态都有徽章
- **验证**：`cd app && npx tsc --noEmit`
- **回滚**：`git checkout KlinePage.tsx klineTideApi.ts`

#### 单元3：本地验证
- **动作**：后端tsc + 前端tsc + 相关测试套件
- **完成标准**：全部通过，0 errors
- **验证**：`npm test -- --testPathPattern="kline|prompt-layers|payment|membership|followup"`
- **回滚**：无需回滚（只验证）

#### 单元4：git commit+push
- **动作**：分组提交（Step 12 + Step 13）+ push origin main
- **完成标准**：git log显示新commit + origin/main同步
- **验证**：`git log --oneline -3` + `git status`
- **回滚**：`git reset --soft HEAD~1` + `git push -f`

#### 单元5：生产部署（SAFETY GATE高风险）
- **动作**：本地构建dist → scp上传 → 备份生产旧dist → 替换 → PM2重启
- **完成标准**：PM2 online + unstable_restarts=0 + 健康检查通过
- **验证**：5件套
- **回滚**：`mv dist.old dist && pm2 restart`

#### 单元6：5件套+UC1验证
- **动作**：执行5件套验证脚本 + UC1冒烟（问事）
- **完成标准**：5件套全✅ + UC1返回完整结果 + 0 errors
- **验证**：curl + pm2 logs
- **回滚**：单元5回滚

#### 单元7：P0装甲LLM链路触发验证（任务1）
- **动作**：通过API发起"深入推演"问事，检查日志中RuleMatcher/EvidenceComposer调用
- **完成标准**：日志含RuleMatcher + EvidenceComposer + matchedRules>=1 + evidencePackage注入
- **验证**：`pm2 logs --lines 200 | grep -iE "ruleMatcher|evidence|matchedRules"`
- **回滚**：无需回滚（只验证）

#### 单元8：任务2远程端到端验证
- **动作**：发起问事，检查响应中 zhangbanshan_output.life_stage 字段
- **完成标准**：life_stage字段存在 + 含klineStage或tideStatus
- **验证**：curl + 响应JSON检查
- **回滚**：无需回滚

#### 单元9：任务3 Stripe CLI验证
- **动作**：Stripe CLI触发checkout.session.completed webhook，验证主动解锁
- **完成标准**：webhook触发后unlockStatus=unlocked_full + deepDiveBonus=3
- **验证**：数据库查询 + 日志
- **回滚**：无需回滚（测试数据可清理）

#### 单元10：文档写回+最终结果单
- **动作**：更新全量计划复盘章节 + 生成连续执行结果单
- **完成标准**：文档状态与代码一致
- **验证**：人工核对
- **回滚**：无需回滚

### 3.5 最终验收标准（DoD）

1. ✅ Step 12后端source字段完整（4种枚举）
2. ✅ Step 13前端徽章显示4种source状态
3. ✅ 后端tsc + 前端tsc通过
4. ✅ 相关测试套件通过
5. ✅ git push成功
6. ✅ 生产部署成功（5件套通过）
7. ✅ P0装甲LLM链路真实触发（日志证据）
8. ✅ 任务2 life_stage字段远程返回
9. ✅ 任务3 Stripe webhook主动解锁验证
10. ✅ 文档状态写回（全量计划复盘 + 结果单）

---

## 4. 暂停并询问的触发条件

执行过程中，仅以下情况会暂停询问：

1. Stripe CLI安装失败或测试模式不可用（需决定是否选选项B/C）
2. P0装甲LLM链路触发后日志无RuleMatcher调用（需排查代码路径）
3. 生产部署5件套失败（需决定是否回滚）
4. 发现工程文档互相冲突且无法通过代码判断
5. 出现授权清单未覆盖的新高风险操作

**普通技术选择、代码实现细节、局部错误修复、测试调整，由AI自主决策，不询问。**

---

## 5. 等待用户确认

**请确认以下3项选择后，我将在授权范围内连续执行：**

1. **Stripe验证方式**：选项A（CLI测试模式）/ 选项B（真实¥1支付）/ 选项C（仅mock）
2. **部署窗口期**：选项A（立即）/ 选项B（指定时间）
3. **P0装甲触发验证方式**：选项A（API发起）/ 选项B（前端操作）

**默认方案**（若用户仅回复"全部授权"）：1A + 2A + 3A

---

**授权清单结束**
**等待用户确认后开始连续执行**
