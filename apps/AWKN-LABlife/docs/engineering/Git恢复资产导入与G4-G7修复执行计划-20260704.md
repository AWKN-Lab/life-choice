# Git 恢复资产导入与 G4-G7 修复执行计划

**文档类型**: EXECUTION（执行类）
**生成日期**: 2026-07-04
**基准文档**:
- `apps/AWKN-LABlife/docs/engineering/Git恢复计划完成情况盘点-20260704.md`（盘点报告）
- `apps/AWKN-LABlife/docs/engineering/工程交接文档-知识资产治理与v3检索.md`（原始 G0-G7 计划）
- `apps/AWKN-LABlife/docs/engineering/Phase3-5+G4-7执行计划-20260703.md`（Phase3-5 已执行，G4 未执行）
- `c:\Users\10919\Desktop\AWKN-Lab\记忆系统\02-evolution\git-dangling-assets-rule.md`（Git Dangling 硬规则）

**执行模式**: 工程文档连续执行模式（10 分钟执行单元）
**前置约束**: SYSTEM RULES > SAFETY GATE > 本计划

---

## 0. 范围与不做清单

### 0.1 本计划覆盖范围

| 优先级 | 任务 | 来源 |
|--------|------|------|
| **P0** | 602 个 docx + 5 个 txt + 4 个特殊文件导入知识库 | 盘点报告 §3.1 |
| **P1** | G4 治理 metaphysics.db 589 本恢复库（897370 passages） | 盘点报告 §3.2 |
| **P2** | G5 PDF 定向恢复（1639 个 PDF）评估 | 盘点报告 §3.2 |
| **P3** | G6-1/G6-2 v3 索引升级（BGE-M3） | 盘点报告 §3.2 |
| **P4** | G7 旁路切换（阻断项，需 G6 通过后才允许启动） | 盘点报告 §3.2 |
| **P5** | 非阻塞遗留事项 5 项 | 盘点报告 §3.3 |

### 0.2 不做清单（本计划严格禁止）

1. 不做 `git gc --prune=now` / `git reflog expire` / `git filter-repo` / BFG（Git Dangling 规则禁止）
2. 不做 G7 旁路切换（阻断项，需 G6 通过 + 灰度 24h）
3. 不删除现有 587 本知识库数据（仅追加）
4. 不修改 Nginx/防火墙/PM2 配置
5. 不引入新付费服务（BGE-M3 优先用本地模型）
6. 不修改已部署的 knowledge-service 代码（仅扩数据 + 索引）
7. 不在未备份前执行任何覆盖操作
8. 不在未抽样验证前批量执行 602 个文件

### 0.3 约束条件

- **磁盘约束**: 服务器 / 分区 90% 已用，本地 D 盘需保留 ≥ 20GB 可用空间
- **Git Dangling 规则**: 611 个资产默认视为"待盘点资产"，禁止 gc/prune
- **二进制安全约束**: 禁止 PowerShell `>`/`Out-File` 导出二进制，必须用 Python bytes 方式
- **幂等约束**: ingest 脚本必须幂等（相同源文件重跑结果相同）
- **回滚约束**: 每个执行单元必须有可执行回滚命令

---

## 1. P0 任务：602 个 docx + 5 txt + 4 特殊文件导入

### 1.1 任务分解（8 个执行单元）

#### U1: 抽样验证 10 个 docx 解压+文本提取流程
- **目标**: 从 small/ 目录随机选 10 个 zip，验证解压 → docx/xlsx/epub 文本提取 → 分类判断 → 切分 全流程
- **修改文件**: 新增 `scripts/_probe_docx_sample.py`（临时脚本，验证后删除）
- **依赖**: `python-docx`、`openpyxl`、`ebooklib`（如缺需 pip install）
- **动作**:
  1. 随机选 10 个 small/*.zip
  2. 解压到临时目录 `_tmp_probe/`
  3. 按 magic bytes 判断真实类型（docx/xlsx/epub）
  4. 提取文本（docx 用 python-docx，xlsx 用 openpyxl，epub 用 ebooklib）
  5. 调用 `knowledge_ingest.py` 的分类函数判断 system_type
  6. 输出 10 个文件的提取结果报告
- **验收标准**:
  - 10 个文件全部成功解压
  - 至少 7 个文件提取出非空文本（≥100 字）
  - 至少 5 个文件能正确判断 system_type
  - 失败用例有明确错误原因（如扫描件无文本、加密文件等）
- **验证方法**:
  ```bash
  python scripts/_probe_docx_sample.py
  # 期望: 输出 10 行结果，success_rate >= 70%
  ```
- **回滚方式**: `rm -rf _tmp_probe/ && rm scripts/_probe_docx_sample.py`
- **风险标记**: 低
- **Plan B**: 若 docx 提取失败率 > 30%，需评估是否需要 OCR；若 epub/xlsx 库缺失，降级为只处理 docx

#### U2: 编写批量解压+分类脚本
- **目标**: 创建 `scripts/ingest_recovered_docx.py`，批量处理 602 个 zip
- **修改文件**: 新增 `scripts/ingest_recovered_docx.py`
- **动作**:
  1. 读取 `manifest_import.csv`（602 行）
  2. 对每个 zip：解压 → magic bytes 判断 → 提取文本 → 分类 → 切分（400 字硬切）
  3. 输出到 `knowledge/processed/_recovered_import.jsonl`（独立文件，不污染 classics_index.jsonl）
  4. 失败用例记录到 `knowledge/processed/_recovered_import_failures.jsonl`
  5. 支持断点续跑（已处理的 sha 跳过）
- **验收标准**:
  - 脚本能 dry-run（只输出计划，不写文件）
  - 脚本能单文件测试（`--sha <short_sha>`）
  - 失败率 ≤ 10%
  - 输出 jsonl 格式与 classics_index.jsonl 一致
- **验证方法**:
  ```bash
  python scripts/ingest_recovered_docx.py --dry-run --sha 004e35ce5a72
  python scripts/ingest_recovered_docx.py --sha 004e35ce5a72
  # 期望: 输出 1 条 passage，system_type 合理
  ```
- **回滚方式**: `rm scripts/ingest_recovered_docx.py knowledge/processed/_recovered_import.jsonl`
- **风险标记**: 中（脚本错误可能导致 602 个文件全失败）
- **Plan B**: 若批量脚本失败，回退到 U1 抽样脚本逐个处理

#### U3: 批量执行 532 个 small/ + 70 个 big/（断点续跑）
- **目标**: 全量处理 602 个 zip，生成 `_recovered_import.jsonl`
- **依赖**: U2 完成
- **动作**:
  1. 备份当前 `classics_index.jsonl` → `.bak-pre-recovered-import`
  2. 运行 `python scripts/ingest_recovered_docx.py --batch`
  3. 监控进度（每 50 个输出一次状态）
  4. 失败用例自动跳过，记录到 failures.jsonl
  5. 完成后统计：成功数/失败数/总 passages 数/各分类 passages 数
- **验收标准**:
  - 成功率 ≥ 80%（≥ 482 个成功）
  - 失败用例数 ≤ 120
  - 新增 passages 数 ≥ 10000（按平均 20 passages/本估算）
  - 各分类分布合理（bazi/ziwei/qimen/liuren 等有数据）
- **验证方法**:
  ```bash
  wc -l knowledge/processed/_recovered_import.jsonl
  python -c "import json; lines=open('knowledge/processed/_recovered_import.jsonl',encoding='utf-8').readlines(); from collections import Counter; c=Counter(json.loads(l)['system_type'] for l in lines); print(dict(c))"
  ```
- **回滚方式**: `rm knowledge/processed/_recovered_import.jsonl`（不影响原 classics_index.jsonl）
- **风险标记**: 中
- **Plan B**: 若失败率 > 20%，暂停执行，分析失败原因（加密/扫描件/格式异常）

#### U4: 处理 5 个 small_docx_hits txt 文件
- **目标**: 将 5 个 txt（含六壬讲义等玄学内容）导入知识库
- **修改文件**: 追加到 `_recovered_import.jsonl`
- **动作**:
  1. 读取 5 个 txt 文件内容
  2. 按 400 字硬切
  3. 分类判断（六壬讲义 → liuren）
  4. 追加到 `_recovered_import.jsonl`
- **验收标准**:
  - 5 个 txt 全部成功导入
  - passages 数 ≥ 100
  - system_type 合理
- **验证方法**: `grep -c "六壬" knowledge/processed/_recovered_import.jsonl` 应 ≥ 1
- **回滚方式**: 从 `_recovered_import.jsonl` 删除 5 个 txt 对应的 passage
- **风险标记**: 低

#### U5: 解析 4 个特殊文件
- **目标**: 解析 sqlite/gzip×2/unknown bin，提取可救资产
- **修改文件**: 新增 `scripts/_probe_special_files.py`（临时脚本）
- **动作**:
  1. **SQLite** (`abb4f19c5d13.sqlite`, 50MB): `sqlite3 .tables` + `.schema` + 抽样查询
  2. **Gzip×2** (253MB + 345MB): `gunzip -l` 查看内容，若为 tar 则 `tar -tzf` 列文件
  3. **Unknown bin** (105MB, magic 0D 00 00 00 48 00 00 00): Python `numpy.load()` / `pickle.load()` 试探
  4. 输出每个文件的结构报告
- **验收标准**:
  - 4 个文件全部有结构报告
  - SQLite 表结构清晰，能判断是否含玄学典籍
  - Gzip 内容能列出文件清单
  - Unknown bin 能判断类型（numpy/pickle/其他）
- **验证方法**:
  ```bash
  python scripts/_probe_special_files.py
  # 期望: 输出 4 个文件的结构报告
  ```
- **回滚方式**: `rm scripts/_probe_special_files.py`（只读不写，无需回滚数据）
- **风险标记**: 低（只读操作）
- **Plan B**: 若 unknown bin 无法识别，标记为"待人工分析"，不阻塞 U6

#### U6: 合并 _recovered_import.jsonl 到 classics_index.jsonl
- **目标**: 将新导入的 passages 合并到主索引
- **依赖**: U3 + U4 完成
- **动作**:
  1. 备份 `classics_index.jsonl` → `.bak-pre-merge-<TS>`
  2. 备份 `_manifest.json` → `.bak-pre-merge-<TS>`
  3. `cat classics_index.jsonl _recovered_import.jsonl > classics_index.jsonl.new`
  4. `mv classics_index.jsonl.new classics_index.jsonl`
  5. 重新生成 `_manifest.json`（运行 ingest 脚本的 manifest 部分）
  6. 统计合并前后 passages 数、books 数
- **验收标准**:
  - 合并后 passages 数 = 原 142807 + 新增数
  - 合并后 books 数 ≥ 587
  - 无重复 passage_id
  - 12 分类（或 13 分类）覆盖完整
- **验证方法**:
  ```bash
  wc -l knowledge/processed/classics_index.jsonl  # 应 > 142807
  python -c "import json; lines=open('knowledge/processed/classics_index.jsonl',encoding='utf-8').readlines(); ids=[json.loads(l)['passage_id'] for l in lines]; print('duplicates:', len(ids)-len(set(ids)))"
  ```
- **回滚方式**: `cp classics_index.jsonl.bak-pre-merge-* classics_index.jsonl && cp _manifest.json.bak-pre-merge-* _manifest.json`
- **风险标记**: 高（覆盖主索引）
- **Plan B**: 若合并后 verify 失败，立即回滚到 .bak 文件

#### U7: 重新生成 embeddings + IDF 缓存
- **目标**: 为新增 passages 生成向量
- **依赖**: U6 完成
- **动作**:
  1. 备份 `embeddings.npy` → `.bak-pre-regen`
  2. 备份 `idf_cache.pkl` → `.bak-pre-regen`
  3. 运行 `python scripts/embed_classics.py`
  4. 验证 embeddings.npy shape 与 classics_index.jsonl 行数一致
- **验收标准**:
  - embeddings.npy 行数 = classics_index.jsonl 行数
  - shape=(N, 1024), float32
  - 文件大小合理（每 10 万 passages ≈ 390MB）
- **验证方法**:
  ```bash
  python -c "import numpy as np; e=np.load('knowledge/processed/embeddings.npy'); print(e.shape, e.dtype)"
  ```
- **回滚方式**: `cp embeddings.npy.bak-pre-regen embeddings.npy && cp idf_cache.pkl.bak-pre-regen idf_cache.pkl`
- **风险标记**: 中（耗时 5-15 分钟）
- **Plan B**: 若生成失败，回滚到 .bak，知识服务继续用旧 embeddings（少覆盖部分 passages）

#### U8: 本地验证 + 部署到生产
- **目标**: 验证知识服务 5 接口全 PASS，部署到生产
- **依赖**: U6 + U7 完成
- **动作**:
  1. 本地运行 `python scripts/verify_knowledge_service.py`，期望 exit code 0
  2. 本地 curl 5 接口验证
  3. scp 上传 6 个文件到服务器（classics_index.jsonl、_manifest.json、embeddings.npy、idf_cache.pkl、loader.py、main.py）
  4. 服务器 PM2 重启 knowledge-service
  5. 服务器内部 curl 5 接口验证
- **验收标准**:
  - 本地 verify exit code 0
  - 服务器 /health 返回 bookCount > 587, passageCount > 142807
  - 服务器 /retrieve、/embed_search、/hybrid_search 返回非空 items
- **验证方法**:
  ```bash
  ssh aliyun-awkn 'curl -s http://127.0.0.1:30000/api/v1/health' | python -m json.tool
  ssh aliyun-awkn 'curl -s -X POST http://127.0.0.1:8701/retrieve -H "Content-Type: application/json" -d "{\"routeType\":\"ziping\",\"question\":\"日主身弱\",\"limit\":3}"' | python -m json.tool
  ```
- **回滚方式**: 服务器 `/opt/awkn-life/knowledge/processed/_backup_pre_deploy_<TS>/` 已存在，回滚命令见部署报告
- **风险标记**: 高（生产部署）
- **Plan B**: 若生产验证失败，立即回滚到 _backup_pre_deploy 目录，本地继续用旧索引

### 1.2 P0 任务验收门禁

| 门禁 | 阈值 | 当前 |
|------|------|------|
| U1 抽样成功率 | ≥ 70% | 待测 |
| U3 批量成功率 | ≥ 80% | 待测 |
| U3 新增 passages | ≥ 10000 | 待测 |
| U6 合并无重复 | duplicates=0 | 待测 |
| U7 embeddings shape 一致 | shape[0]=jsonl 行数 | 待测 |
| U8 5 接口全 PASS | 5/5 | 待测 |

---

## 2. P1 任务：G4 治理 metaphysics.db 589 本恢复库

### 2.1 任务分解（3 个执行单元）

#### U9: 评估 589 库与当前 587 本的重叠度
- **目标**: 用 SHA256 比对 metaphysics.db 与 classics_index.jsonl 的 source_id
- **动作**:
  1. 从 metaphysics.db 导出 589 本的 sha256 列表
  2. 从 `assets_books.jsonl` 导出当前 587 本的 sha256 列表
  3. 计算交集（duplicate_binary 关系）
  4. 输出重叠报告
- **验收标准**:
  - 重叠数明确（0-589 之间）
  - 独有数 = 589 - 重叠数
- **验证方法**: `cat knowledge/processed/_g4_overlap_report.json`
- **回滚方式**: 无（只读操作）
- **风险标记**: 低

#### U10: 治理 589 库标题 + 去重
- **目标**: 清洗标题（< 4 字过滤）+ 去重（按 SHA256）
- **依赖**: U9 完成
- **动作**:
  1. 备份 `metaphysics.db` → `.bak-g4-<TS>`
  2. 清洗标题（短标题过滤、去重）
  3. 标记 duplicate_binary 关系
  4. 输出治理报告
- **验收标准**:
  - 短标题数 = 0
  - 重复标题数 = 0
  - 治理后 books 数 ≤ 589
- **验证方法**: `sqlite3 metaphysics.db "SELECT COUNT(*) FROM books WHERE length(title) < 4"`
- **回滚方式**: `cp metaphysics.db.bak-g4-* metaphysics.db`
- **风险标记**: 中（修改 SQLite）
- **Plan B**: 若治理破坏 db，立即回滚到 .bak

#### U11: 评估 589 库独立 passages 是否值得导入
- **目标**: 抽样 50 本，评估内容质量
- **依赖**: U9 完成
- **动作**:
  1. 从 589 库独立部分（非重叠）随机抽 50 本
  2. 人工/AI 评估内容质量（乱码/碎片/完整度）
  3. 输出质量评估报告
- **验收标准**:
  - 质量评估报告完成
  - 给出"全量导入/部分导入/不导入"建议
- **验证方法**: `cat knowledge/processed/_g4_quality_report.json`
- **回滚方式**: 无（只读）
- **风险标记**: 低

### 2.2 P1 任务决策点

U11 完成后需用户决策：
- 选项 A: 全量导入 589 库独立部分（推荐，若质量 ≥ 60%）
- 选项 B: 部分导入（仅高质量部分）
- 选项 C: 不导入（若质量 < 30%）

---

## 3. P2 任务：G5 PDF 定向恢复评估

### 3.1 任务分解（2 个执行单元）

#### U12: 评估 1639 个 PDF 的术数占比
- **目标**: 扫描 PDF 文件名/元数据，判断术数相关占比
- **动作**:
  1. 扫描 `knowledge/` 目录下所有 PDF
  2. 按文件名关键词（八字/紫微/六壬/奇门/梅花/风水/相术/取名/道教/周易）分类
  3. 输出 PDF 清单 + 分类报告
- **验收标准**:
  - PDF 总数明确
  - 术数相关占比明确
- **验证方法**: `cat knowledge/processed/_g5_pdf_report.json`
- **回滚方式**: 无
- **风险标记**: 低

#### U13: OCR 可行性评估
- **目标**: 抽样 10 个术数 PDF，评估 OCR 效果
- **依赖**: U12 完成
- **动作**:
  1. 随机选 10 个术数 PDF
  2. 用 `pytesseract` 或 `paddleocr` 试 OCR
  3. 评估识别准确率
- **验收标准**:
  - OCR 报告完成
  - 给出"全量 OCR/部分 OCR/不 OCR"建议
- **验证方法**: `cat knowledge/processed/_g5_ocr_report.json`
- **回滚方式**: 无
- **风险标记**: 低

### 3.2 P2 任务决策点

U13 完成后需用户决策：
- 选项 A: 全量 OCR（若准确率 ≥ 80%）
- 选项 B: 部分 OCR（仅高质量扫描件）
- 选项 C: 不 OCR（延后到 v0.2）

---

## 4. P3 任务：G6-1/G6-2 v3 索引升级

### 4.1 前置条件

- P0 完成（602 + 5 + 4 资产已导入）
- P1 完成（589 库已治理）
- P2 完成（PDF 已决策）
- 当前 recall@10=0.473 < 0.7 阈值（已验证）

### 4.2 任务分解（3 个执行单元）

#### U14: BGE-M3 模型本地部署
- **目标**: 在服务器部署 BGE-M3 模型
- **动作**:
  1. 下载 `BAAI/bge-large-zh-v1.5` 模型（约 1.3GB）
  2. 部署到 `/opt/awkn-life/models/bge-large-zh/`
  3. 测试模型加载 + 单句编码
- **验收标准**:
  - 模型加载成功
  - 单句编码耗时 < 500ms
- **验证方法**: `python -c "from sentence_transformers import SentenceTransformer; m=SentenceTransformer('/opt/awkn-life/models/bge-large-zh/'); print(m.encode('测试').shape)"`
- **回滚方式**: `rm -rf /opt/awkn-life/models/bge-large-zh/`
- **风险标记**: 中（磁盘空间）
- **Plan B**: 若磁盘不足，降级为 `bge-small-zh`（约 100MB）

#### U15: 生成 v3 embeddings + BM25 索引
- **目标**: 用 BGE-M3 重新编码所有 passages
- **依赖**: U14 完成 + P0/P1 完成
- **动作**:
  1. 备份 v2 embeddings.npy
  2. 运行 `python scripts/embed_classics.py --model bge-m3 --output embeddings_v3.npy`
  3. 构建 BM25 索引
  4. 评估 recall@10、precision@10
- **验收标准**:
  - recall@10 ≥ 0.7
  - precision@10 ≥ 0.6
  - embeddings_v3.npy 生成成功
- **验证方法**: `python scripts/eval_tfidf_g6_0.py --index v3`
- **回滚方式**: `rm embeddings_v3.npy`（v2 仍可用）
- **风险标记**: 中
- **Plan B**: 若 recall 仍 < 0.7，分析失败原因，考虑混合检索（v2 + v3）

#### U16: v3 索引旁路部署 + 灰度
- **目标**: v3 索引作为旁路运行，不影响主链路
- **依赖**: U15 完成
- **动作**:
  1. 启动 v3 索引服务（独立进程，端口 8702）
  2. 5% 流量灰度到 v3
  3. 监控 24h
- **验收标准**:
  - v3 服务健康
  - 灰度 24h 无异常
  - 用户反馈无退化
- **验证方法**: `curl http://127.0.0.1:8702/health`
- **回滚方式**: 关闭 v3 服务，流量回到 v2
- **风险标记**: 中
- **Plan B**: 若灰度异常，立即关闭 v3 旁路

---

## 5. P4 任务：G7 旁路切换（阻断项）

### 5.1 前置条件（全部满足才能启动）

- P3 完成（v3 索引 recall@10 ≥ 0.7）
- 灰度 24h 无异常
- 用户明确授权切换

### 5.2 任务分解（2 个执行单元）

#### U17: 切换主链路到 v3
- **目标**: `KNOWLEDGE_INDEX_VERSION=v3`
- **依赖**: P3 完成 + 用户授权
- **动作**:
  1. 服务器 `.env` 改 `KNOWLEDGE_INDEX_VERSION=v3`
  2. PM2 重启 knowledge-service
  3. 验证 5 接口
- **验收标准**:
  - 5 接口全 PASS
  - 主链路使用 v3 索引
- **验证方法**: `ssh aliyun-awkn 'curl -s http://127.0.0.1:8701/health-v2 | grep index_version'`
- **回滚方式**: 改回 `KNOWLEDGE_INDEX_VERSION=v2` + PM2 重启
- **风险标记**: 高（主链路变更）
- **Plan B**: 若切换失败，立即回滚到 v2

#### U18: 24h 生产监控 + 结果单
- **目标**: 监控生产 24h，输出最终结果单
- **依赖**: U17 完成
- **动作**:
  1. 监控 5 接口可用性
  2. 监控 L2 引用典籍数
  3. 监控用户反馈
  4. 输出最终结果单
- **验收标准**:
  - 24h 无 P0 故障
  - L2 引用典籍数提升
  - 用户反馈无退化
- **验证方法**: 日报
- **回滚方式**: 若 24h 内出现 P0，回滚到 v2
- **风险标记**: 中

---

## 6. P5 任务：非阻塞遗留事项

### 6.1 任务清单（5 项，可并行处理）

| 编号 | 任务 | 优先级 | 预估 |
|------|------|--------|------|
| L1 | fengshui 查询 precision@10=0.2 优化 | 低 | 1 单元 |
| L2 | 495 条短文本(<30字) 过滤 | 低 | 1 单元 |
| L3 | 八宅明镜误归 bazi 修正 | 低 | 1 单元 |
| L4 | 康熙字典误归 other 修正 | 低 | 1 单元 |
| L5 | project-deploy-standards.md 端口更新（3002→30000） | 低 | 0.5 单元 |

### 6.2 执行规则

- P5 任务不阻塞 P0-P4
- 可在 P0-P4 执行间隙穿插处理
- 每个 L 任务独立 commit

---

## 7. 执行顺序与依赖关系

```
P0 (U1-U8) ──必做──> P1 (U9-U11) ──必做──> P3 (U14-U16) ──阻断──> P4 (U17-U18)
                              │
                              └──可选──> P2 (U12-U13)

P5 (L1-L5) 可在任意阶段穿插
```

### 7.1 推荐执行顺序

1. **立即执行**: U1 抽样验证（10 分钟，低风险）
2. **U1 通过后**: U2 编写脚本（10 分钟）
3. **U2 通过后**: U3 批量执行（30-60 分钟）
4. **U3 通过后**: U4 + U5 并行（10 分钟）
5. **U4+U5 通过后**: U6 合并索引（10 分钟）
6. **U6 通过后**: U7 重新生成 embeddings（15 分钟）
7. **U7 通过后**: U8 本地验证 + 部署（20 分钟）
8. **P0 完成后**: U9-U11 评估 589 库（30 分钟）
9. **P1 决策后**: U12-U13 评估 PDF（30 分钟）
10. **P2 决策后**: U14-U16 v3 索引升级（60 分钟）
11. **P3 完成后**: U17-U18 G7 切换（24 小时监控）

---

## 8. 高风险项与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| U3 批量提取失败率 > 20% | 中 | P0 延期 | 抽样验证 + 断点续跑 + 失败不阻塞 |
| U6 合并破坏主索引 | 低 | 知识服务不可用 | 双重备份 + 立即回滚 |
| U7 embeddings 生成失败 | 低 | 向量检索不可用 | 回滚到旧 embeddings |
| U8 生产部署失败 | 低 | 线上服务不可用 | 服务器有 _backup_pre_deploy 目录 |
| U10 治理破坏 metaphysics.db | 低 | 589 库不可用 | .bak 备份 + 立即回滚 |
| U14 BGE-M3 磁盘不足 | 中 | v3 升级延期 | 降级为 bge-small-zh |
| U17 G7 切换后主链异常 | 低 | 主咨询链不可用 | 立即回滚到 v2 |
| 服务器磁盘 90% 已用 | 高 | 多个任务受阻 | 启动前先清理 /opt/awkn-life/knowledge/_backup_* |

---

## 9. 执行规则

### 9.1 通用规则

1. 每个单元完成后立即 git commit（不 push）
2. 每 3 个单元做一次文档同步
3. 高风险单元（U6/U8/U10/U17）失败时暂停并记录
4. 全部完成后做最终验收 + 结果单
5. 命中 SAFETY GATE 触发词（删除/覆盖/重写）必须先输出 5 项（修改范围/风险点/备份方式/验证方法/回滚方式）

### 9.2 备份规则

- **大修改前**（U6/U8/U10/U17）: 立即 `git add -A && git commit -m "WIP 备份: $(date +%Y%m%d_%H%M%S)"`
- **小修改**: 累计 10 次后统一备份
- **数据文件**: 每次修改前 `.bak-pre-<task>-<TS>`

### 9.3 验证规则

- 每个单元必须有客观验收标准（禁止"感觉正常"）
- 每个单元必须有 10 分钟内可完成的验证方法
- 每个单元必须有可执行回滚命令

### 9.4 决策点规则

- U11 完成后需用户决策 589 库导入策略
- U13 完成后需用户决策 PDF OCR 策略
- U17 启动前需用户明确授权 G7 切换

---

## 10. 最终验收标准（DoD）

### 10.1 P0 完成 DoD

- [ ] 602 个 docx 成功导入 ≥ 80%
- [ ] 5 个 txt 全部导入
- [ ] 4 个特殊文件有结构报告
- [ ] classics_index.jsonl 行数 > 142807
- [ ] books 数 > 587
- [ ] embeddings.npy shape 与 jsonl 一致
- [ ] 本地 verify exit code 0
- [ ] 生产 5 接口全 PASS

### 10.2 P1 完成 DoD

- [ ] 589 库重叠度报告完成
- [ ] 治理后 metaphysics.db 标题无重复
- [ ] 质量评估报告完成
- [ ] 用户决策导入策略

### 10.3 P3 完成 DoD

- [ ] BGE-M3 模型部署成功
- [ ] v3 embeddings 生成成功
- [ ] recall@10 ≥ 0.7
- [ ] precision@10 ≥ 0.6
- [ ] 灰度 24h 无异常

### 10.4 P4 完成 DoD

- [ ] 主链路切换到 v3
- [ ] 24h 生产监控无 P0
- [ ] L2 引用典籍数提升
- [ ] 最终结果单输出

---

## 11. 用户可见性验证（每阶段完成后）

| 阶段 | 用户可见指标 |
|------|-------------|
| P0 完成 | /health bookCount > 587, passageCount > 142807 |
| P1 完成 | metaphysics.db 治理报告可查 |
| P3 完成 | /health-v2 index_version=v3 |
| P4 完成 | 主咨询链引用典籍数提升 |

---

## 12. 文档索引

| 文档 | 用途 |
|------|------|
| `Git恢复计划完成情况盘点-20260704.md` | 盘点报告（输入） |
| `工程交接文档-知识资产治理与v3检索.md` | 原始 G0-G7 计划 |
| `知识资产治理与v3检索-最终结果单-20260704.md` | 已完成项结果单 |
| `知识服务部署报告-20260704.md` | 上次部署报告 |
| `git-dangling-assets-rule.md` | Git Dangling 硬规则 |
| **本计划** | **执行计划（输出）** |

---

## 13. 启动口令

用户确认后，按以下顺序启动：

1. **用户**: "启动 P0，按计划执行"
2. **执行**: U1 抽样验证 10 个 docx
3. **验收**: U1 通过后立即执行 U2
4. **连续执行**: U2 → U3 → U4+U5 → U6 → U7 → U8
5. **P0 完成后**: 询问用户是否启动 P1

---

**下次遇到类似情况先做 3 件事**：
1. 查看当前状态（`git status` + 目录 LS + manifest CSV）
2. 备份当前版本（代码 `git commit` + 数据 `.bak`）
3. 读取完整文件并确认修改位置（Grep 验证行号，Read 全文）
